---
phase: quick-260621-k37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/patents/appendix-self-healing-networks.md
  - .planning/research/patents/appendix-self-healing-networks.html
  - patents-site/build_appendix.py
  - docs/build_site.py
  - .planning/research/patents/INDEX.md
  - .planning/research/patents/INDEX.html
  - docs/architecture/INDEX.html
autonomous: true
requirements: [QUICK-260621-k37]
must_haves:
  truths:
    - "A standalone, self-contained styled HTML appendix on self-healing distribution networks exists, content driven by the two source PDFs"
    - "The new appendix is registered in build_appendix.py and renders via build_appendix.py with device_links=False"
    - "The new appendix HTML is wired into the site build (source map, page list, architecture hub card) and appears in the patents INDEX (md + html)"
    - "The site build runs green with zero broken links and the new HTML is self-contained (no external/network refs)"
  artifacts:
    - path: ".planning/research/patents/appendix-self-healing-networks.md"
      provides: "House-style markdown appendix (H1 title + one H2 per card), PDF-driven"
      contains: "## "
    - path: ".planning/research/patents/appendix-self-healing-networks.html"
      provides: "Self-contained styled HTML built from the markdown"
      contains: "self-healing"
    - path: "patents-site/build_appendix.py"
      provides: "APPENDICES registry entry for the new appendix"
      contains: "appendix-self-healing-networks"
  key_links:
    - from: ".planning/research/patents/appendix-self-healing-networks.md"
      to: "appendix-distribution-observability-sources.html / appendix-virtual-sensing-module.html"
      via: "light relative 'see also' cross-links"
      pattern: "appendix-(distribution-observability-sources|virtual-sensing-module)\\.html"
    - from: "docs/build_site.py"
      to: "appendix-self-healing-networks.html"
      via: "_TRIO_SRCS entry + _ARCH_CARDS hub card"
      pattern: "appendix-self-healing-networks\\.html"
---

<objective>
Author a STANDALONE third patents appendix on **self-healing distribution networks**,
content driven by reading two source PDFs, in the same self-contained ORACS/AGMS-styled
"card" house style as the two existing appendixes — but standalone, not pitched as part of
the deployment-proposal family. Then register it in the appendix converter and wire it into
the site build + patents INDEX so it ships as another page of Juan's HTML study site.

Purpose: Give Juan an interview-speakable study card on self-healing / FLISR — the ACTION
layer that sits on top of the observability + state-estimation appendixes — tied to the
AGMS Operation Loop Formation patent and operating-cell / island-mode resilience.

Output:
- New `appendix-self-healing-networks.md` (PDF-driven) + built `.html` (self-contained)
- `build_appendix.py` registry entry (device_links=False)
- `docs/build_site.py` wiring (source map + hub card)
- patents INDEX entries (`INDEX.md` + `INDEX.html`) listing it as a standalone appendix
- Light reciprocal "see also" cross-links to the two existing appendixes
- Green site build, zero broken links, self-contained HTML
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Source PDFs (the EXECUTOR must read these first — they drive scope, structure, emphasis)
@.planning/research/articles/self_healing_1.pdf
@.planning/research/articles/self_healing_2.pdf

# EXEMPLARS — match this house style exactly (structure, tone, tables, framing, closers)
@.planning/research/patents/appendix-distribution-observability-sources.md
@.planning/research/patents/appendix-virtual-sensing-module.md

# Converter + site build + INDEX — all wiring spots
@patents-site/build_appendix.py
@docs/build_site.py
@.planning/research/patents/INDEX.md
@.planning/research/patents/INDEX.html
@docs/architecture/INDEX.html

<interfaces>
<!-- The converter contract — write the markdown to fit it, no codebase exploration needed. -->

build_appendix.py (patents-site/) — converter contract:
- Source markdown lives in `.planning/research/patents/`.
- First `# H1` line becomes the masthead title; everything after is split on `^## ` into
  cards (ONE `.card` section per H2). At least one H2 is REQUIRED or the build fails.
- Standalone `---` horizontal-rule lines inside a card body are stripped; blockquotes
  (`>`) render as the family's amber callouts; fenced code (```) and tables render in
  house style. `###`/`####` become styled subheads.
- Self-containment is ENFORCED: any external/network `href`/`src` (a URL scheme, `//`, or
  leading `/`) FAILS the build. Allowed: `#anchors`, `data:` URIs, and **relative same-site**
  links — so `appendix-virtual-sensing-module.html` and `appendix-distribution-observability-sources.html`
  cross-links are fine; do NOT use absolute http(s) links.
- The APPENDICES registry is a list of dicts: `md`, `out`, `subtitle` (HTML-escaped,
  end with `&middot; Juan Carlos Oviedo Cepeda`), `device_links` (set **False** here),
  optional `diagram` (OMIT — no diagram for this appendix).

docs/build_site.py — three wiring spots for a third appendix (mirror the two existing ones):
- `_TRIO_SRCS` dict (~line 182): add `"appendix-self-healing-networks.html": RESEARCH / "patents" / "appendix-self-healing-networks.html"`.
- `_ARCH_CARDS` list (~line 749): add a `(title, desc, "architecture/appendix-self-healing-networks.html")` tuple.
- (The page list / copy loop iterates `_TRIO_SRCS`, so no separate page-list edit is needed —
  confirm by reading the loop at ~line 311; if a separate explicit page list also references
  the appendixes by name, add the new one there too.)

Patents INDEX — two files, both list the existing appendixes:
- `.planning/research/patents/INDEX.md` (~line 32): the existing two are under a
  "Companion appendices (the two halves of the virtual-sensing deployment proposal)" block.
  Add the new one as a SEPARATE standalone entry (NOT folded into that matched-pair block) —
  e.g. a new "Standalone appendix" line/section — honoring the standalone decision.
- `docs/architecture/INDEX.html` (~line 143): mirror the same standalone framing in HTML.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read both PDFs and author the standalone self-healing appendix (markdown)</name>
  <files>.planning/research/patents/appendix-self-healing-networks.md</files>
  <action>
FIRST, READ BOTH SOURCE PDFs IN FULL — they DRIVE the content (per locked decision #2,
content is PDF-led, not a fixed template):
- `.planning/research/articles/self_healing_1.pdf`
- `.planning/research/articles/self_healing_2.pdf`
Extract the substantive self-healing / FLISR concepts, mechanisms, taxonomies, comparisons,
and any sharp distinctions the papers make. Let the PDFs determine the scope, the card
structure, the emphasis, and which tables earn their place — do NOT force a pre-set outline.

THEN author `.planning/research/patents/appendix-self-healing-networks.md` mapping that PDF
content into the EXISTING HOUSE STYLE (study both exemplars first):
- Start with a single `# H1` masthead title (e.g. "Appendix — Self-Healing Distribution
  Networks" or a sharper PDF-derived title).
- One `## H2` per styled `.card`. Open with a "Purpose & framing" card.
- Use comparison tables and/or a running spec table where the PDF content earns them
  (mirror the A1–A11 running-spec device and the 14-tier table device used in the exemplars,
  but ONLY if the source material justifies it — content-led).
- Interview-speakable framing throughout: "the trap is…", sharp distinctions, spoken
  connections, `> blockquote` callouts for the one-sentence anchors (matches the amber
  callout style).
- Tie to the role where NATURAL (self-healing / FLISR = the ACTION layer that consumes the
  observability + state estimate; the AGMS **Operation Loop Formation** patent
  US 12,596,341 B2 GRANTED to GE Vernova; operating-cell / island-mode resilience as the
  self-healing substrate) — but keep it STANDALONE.

CROSS-LINKS (locked decision #1 — STANDALONE, light only):
- Add a LIGHT "see also" pointing to the two existing appendixes via RELATIVE links
  `appendix-distribution-observability-sources.html` and
  `appendix-virtual-sensing-module.html` (one short "see also" mention is enough — do NOT
  thread them through every card, and do NOT frame this as "the third half of the
  deployment-proposal family"; the first two are a deliberate matched pair, this one stands
  alone).
- A closing card may note where self-healing sits relative to the other two, but framed as a
  standalone wrap-up, not as a deployment-proposal continuation.

SELF-CONTAINMENT: use only relative same-site links + `#anchors`. NO absolute http(s)/external
references anywhere (the build rejects them). Cite the PDFs by name/title in prose, not as
network hyperlinks.
  </action>
  <verify>
    <automated>test -f .planning/research/patents/appendix-self-healing-networks.md && grep -q '^# ' .planning/research/patents/appendix-self-healing-networks.md && [ "$(grep -c '^## ' .planning/research/patents/appendix-self-healing-networks.md)" -ge 3 ] && grep -Eq 'appendix-(distribution-observability-sources|virtual-sensing-module)\.html' .planning/research/patents/appendix-self-healing-networks.md && echo OK</automated>
  </verify>
  <done>Markdown exists with one H1, ≥3 H2 cards, a "Purpose & framing" opening card, PDF-derived content in house style, and a light relative "see also" cross-link to both existing appendixes. No absolute/external links.</done>
</task>

<task type="auto">
  <name>Task 2: Register the appendix in the converter and wire it into the site build + INDEX</name>
  <files>patents-site/build_appendix.py, docs/build_site.py, .planning/research/patents/INDEX.md, docs/architecture/INDEX.html</files>
  <action>
1. `patents-site/build_appendix.py` — add a THIRD entry to the `APPENDICES` registry list:
   - `md`: `"appendix-self-healing-networks.md"`
   - `out`: `"appendix-self-healing-networks.html"`
   - `subtitle`: a one-line HTML-escaped description ending `&middot; Juan Carlos Oviedo Cepeda`
     (e.g. "Self-healing distribution networks — FLISR and the AGMS action layer &middot;
     Juan Carlos Oviedo Cepeda"; tune to match the authored content).
   - `device_links`: `False`
   - Do NOT add a `diagram` key (no diagram for this appendix).
   - Update the module docstring's "Appendices (see APPENDICES registry below)" list to mention
     the new one (keeps the file's self-documentation honest).

2. `docs/build_site.py`:
   - `_TRIO_SRCS` (~line 182): add
     `"appendix-self-healing-networks.html": RESEARCH / "patents" / "appendix-self-healing-networks.html",`
   - `_ARCH_CARDS` (~line 749): add a tuple
     `("Appendix: Self-Healing Networks", "<one-line desc of FLISR / self-healing as the AGMS action layer>", "architecture/appendix-self-healing-networks.html")`.
   - Read the copy loop (~line 311) and the index-card loop to confirm both are driven by
     `_TRIO_SRCS` / `_ARCH_CARDS` (they are). If any separate explicit page list names the
     appendixes individually, add the new one there too.

3. `.planning/research/patents/INDEX.md` (~line 32): add the new appendix as a SEPARATE
   STANDALONE entry — NOT inside the "two halves of the virtual-sensing deployment proposal"
   companion block. Add a short standalone line/section (e.g. "**Standalone appendix** —
   `appendix-self-healing-networks.md`: …") so it reads as its own thing.

4. `docs/architecture/INDEX.html` (~line 143): mirror the same standalone entry in HTML
   (a separate `<li>`/paragraph, not folded into the companion-appendices `<ol>`), linking
   `appendix-self-healing-networks.html`.
  </action>
  <verify>
    <automated>grep -q 'appendix-self-healing-networks' patents-site/build_appendix.py && grep -q 'device_links": False' patents-site/build_appendix.py && grep -c 'appendix-self-healing-networks.html' docs/build_site.py | grep -qE '^[2-9]' && grep -q 'appendix-self-healing-networks' .planning/research/patents/INDEX.md && grep -q 'appendix-self-healing-networks.html' docs/architecture/INDEX.html && echo OK</automated>
  </verify>
  <done>Converter registry has the new entry with device_links=False; build_site.py has the source-map entry + hub card; both INDEX files list it as a standalone appendix.</done>
</task>

<task type="auto">
  <name>Task 3: Build the appendix + site and confirm green, self-contained, zero broken links</name>
  <files>.planning/research/patents/appendix-self-healing-networks.html</files>
  <action>
Run the established build invocation (same as the two prior appendix quick tasks):
1. `python3 patents-site/build_appendix.py` — renders all three appendix HTMLs. This step
   ENFORCES self-containment: it fails loudly if any external/network reference leaked into
   the new HTML. If it fails, fix the offending link in the markdown (Task 1) and re-run.
2. `python3 docs/build_site.py` — rebuilds the full site, copies the new appendix HTML into
   `docs/architecture/`, regenerates the hub `index.html`, and runs the broken-link check.
   Confirm the printed summary shows the new arch card counted and reports **0 broken links**.

Confirm `.planning/research/patents/appendix-self-healing-networks.html` was produced and that
`docs/architecture/appendix-self-healing-networks.html` exists after the site build.
  </action>
  <verify>
    <automated>python3 patents-site/build_appendix.py && python3 docs/build_site.py && test -f .planning/research/patents/appendix-self-healing-networks.html && test -f docs/architecture/appendix-self-healing-networks.html && echo BUILD_OK</automated>
  </verify>
  <done>Both builds run without error; both the patents-side and docs/architecture/ HTMLs exist; site build reports 0 broken links and counts the new arch card. HTML is self-contained (no external refs — enforced by build_appendix.py).</done>
</task>

</tasks>

<verification>
- `appendix-self-healing-networks.md` exists, PDF-driven, house-style (H1 + ≥3 H2 cards, "Purpose & framing" opener), interview-speakable, standalone with only light relative cross-links.
- New appendix registered in `build_appendix.py` (device_links=False) and built to self-contained HTML.
- Wired into `docs/build_site.py` (`_TRIO_SRCS` + `_ARCH_CARDS`) and listed in both INDEX files as a STANDALONE appendix (not folded into the deployment-proposal companion pair).
- `python3 patents-site/build_appendix.py` and `python3 docs/build_site.py` both run green; 0 broken links; HTML self-contained.
</verification>

<success_criteria>
- Juan has a standalone, self-contained HTML self-healing-networks study card in the same house style as the two existing appendixes, content driven by the two PDFs, tied to the role (FLISR/action layer, Operation Loop Formation patent, island-mode) without being threaded into the matched-pair deployment-proposal appendixes.
- The appendix is fully integrated into the build and discoverable from the patents INDEX and the architecture hub.
- The site rebuilds cleanly with zero broken links and a self-contained new page.
</success_criteria>

<output>
After completion, create `.planning/quick/260621-k37-self-healing-networks-appendix/260621-k37-SUMMARY.md`.
</output>
