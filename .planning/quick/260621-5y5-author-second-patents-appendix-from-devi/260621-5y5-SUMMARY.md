---
quick_id: 260621-5y5
description: Author second patents appendix "From Devices to Network State" (virtual sensing module architecture) and integrate it with the distribution-observability appendix
date: 2026-06-21
status: complete
---

# Quick Task 260621-5y5 — Summary

## What was built

A **second** patents appendix — `appendix-virtual-sensing-module.md/.html` — capturing the
virtual-sensing state-estimator architecture, in the same self-contained styled pattern as the
first appendix and **integrated with it** (two-way cross-links, shared build, joint INDEX + hub
listing).

**Content (9 card sections):** framing (reconstruct nodal-voltage state; 4 JD targets;
covariance = ORACS observability index; T&D scope, edge is the hard case); load+DER forecasting
as the FASE predict-step + pseudo-measurements (with the corrections — zero-injection = HIGH-weight
virtual; AMI/DER telemetry are real measurements; low-weight-but-essential; right-size);
real-time topology via ADMS/CIM (GridOS) + edge GOOSE/DNP3 island cache; Kalman/FASE vs WLS,
multi-rate single coupled estimator, power flow in h(x), validation-in/chi-square-out; the three
deep threads (EKF/UKF/EnKF + 3-phase state; behind-meter disaggregation; federated multi-area
DSSE); a running architecture spec **A1–A11**; and an explicit **OPEN DECISIONS** section that
keeps **D1** (state formulation), **D2** (disaggregation placement), **D3** (federation mode)
open and explains each.

## Integration

- `patents-site/build_appendix.py` generalized to build **both** appendices from a registry
  (device image-links only on appendix #1); self-containment check now allows internal
  relative cross-links while still rejecting true external/network refs.
- Two-way cross-links: appendix #1 ↔ appendix #2 ("companion appendix" notes).
- `docs/build_site.py`: new HTML added to `_TRIO_SRCS` + a new `_ARCH_CARDS` hub card.
- Patents `INDEX.md`/`INDEX.html`: both appendices listed together as "the two halves of the
  virtual-sensing deployment proposal."

## Files

| File | Role |
|---|---|
| `.planning/research/patents/appendix-virtual-sensing-module.md` | new MD source (9 H2 sections) |
| `.planning/research/patents/appendix-virtual-sensing-module.html` | self-contained HTML (33 KB) |
| `.planning/research/patents/appendix-distribution-observability-sources.md/.html` | added companion cross-link |
| `patents-site/build_appendix.py` | generalized to build both appendices |
| `docs/build_site.py` | `_TRIO_SRCS` + `_ARCH_CARDS` for the new appendix |
| `.planning/research/patents/INDEX.md`/`INDEX.html` | both appendices listed |
| `docs/architecture/…`, `docs/index.html` | regenerated by the site build |

## Verification

- `build_appendix.py` → wrote both appendices (56 KB / 21 cards; 33 KB / 9 cards), 0 leaks.
- `docs/build_site.py` → exit 0; `validate_links: OK — 33 pages, 0 broken links, 0 noindex-missing`.
- Hub shows 6 architecture cards; INDEX lists both appendices; cross-links resolve on disk.
- Diagram-export churn (draw.io re-serialization) reverted to keep the commit appendix-only.

## Note

Three architecture decisions remain **open by design** (D1 state formulation, D2 disaggregation
placement, D3 federation mode) — to be resolved per feeder archetype during the deployment
proposal. The spine (FASE predict + multi-rate coupled estimator + power flow in h(x) +
topology + observability-index output) is settled.
