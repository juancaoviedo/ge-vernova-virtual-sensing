---
quick_id: 260621-5y5
description: Author second patents appendix "From Devices to Network State" (virtual sensing module architecture) and integrate it with the distribution-observability appendix
date: 2026-06-21
status: in-progress
---

# Quick Task 260621-5y5 — Virtual-Sensing Module Appendix (integrated)

## Objective

Author a **second** patents appendix, `appendix-virtual-sensing-module.md/.html`, capturing
the virtual-sensing state-estimator architecture developed in discussion, in the SAME
self-contained styled pattern as the first appendix
(`appendix-distribution-observability-sources`), and **integrated with it** (cross-links,
shared build, INDEX + hub).

## Content

1. Framing — reconstruct nodal-voltage state x; 4 JD targets; covariance = ORACS
   observability index; T&D scope, edge is the hard case.
2. Forecasting modules (load + DER) = FASE predict-step + pseudo-measurements, with the
   established corrections (zero-injection = HIGH-weight virtual; AMI/DER telemetry are real
   measurements; low-weight-but-essential; right-size).
3. Real-time topology — ADMS/CIM (GridOS hook) + edge GOOSE/DNP3 deltas, island cache.
4. Kalman/FASE vs WLS — multi-rate asynchronous, ONE coupled network estimator, power flow
   in h(x), validation-in / chi-square-out.
5. Three deep threads — EKF/UKF/EnKF + 3-phase state; behind-meter disaggregation;
   federated multi-area DSSE.
6. Running architecture spec A1..A11.
7. **OPEN DECISIONS** (kept open, explained): D1 state formulation; D2 disaggregation
   placement; D3 federation mode.

## Tasks

1. **Author MD** → `.planning/research/patents/appendix-virtual-sensing-module.md` + add a
   "companion appendix" cross-link into the first appendix's MD.
2. **Generalize converter** → refactor `patents-site/build_appendix.py` to build BOTH
   appendices from a registry (device image-links only on appendix #1); emit both HTMLs.
3. **Wire site build** → add new HTML to `docs/build_site.py` `_TRIO_SRCS` + a new
   `_ARCH_CARDS` hub card; link both appendices from patents `INDEX.md`/`INDEX.html`.
4. **Build + validate** → run `build_appendix.py` then `docs/build_site.py`; validate_links
   must pass.

## Verify / Done

- `appendix-virtual-sensing-module.md` + `.html` exist; cross-linked with appendix #1 both ways.
- `build_appendix.py` builds both appendices idempotently.
- `docs/architecture/appendix-virtual-sensing-module.html` emitted; hub shows the card;
  INDEX links both appendices.
- `docs/build_site.py` exits 0 with `validate_links: OK`.
- OPEN DECISIONS section present and explains D1/D2/D3.
