---
phase: 03-director-s-patents-deep-read
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md
autonomous: true
requirements: [PAT-01, PAT-02, PAT-03]

must_haves:
  truths:
    - "Juan can say a 2-3 sentence summary of each of the six AGMS patents from one document"
    - "Juan can state one concrete OSED/HEMS/SI-MAPPER connection per patent"
    - "Juan has one architecture-level question per patent to ask the director"
    - "Juan can deliver a ~90-second whole-family 'walk the assembly line' pitch"
    - "Juan can close with the unified 'one assembly line = my stack' master narrative"
    - "Juan knows the Operation Loop patent is GRANTED, is GE Vernova's own IP, and is the keystone"
  artifacts:
    - path: ".planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md"
      provides: "One consolidated patent rehearsal deck: 6 patent sections (summary + connection + question each) in pipeline order, plus 90s pitch and closing master narrative"
      contains: "12,596,341"
      min_lines: 120
  key_links:
    - from: "deck patent sections"
      to: ".planning/research/patents/{per-patent}.md"
      via: "distillation of Problem Addressed + Sharpest bridge story + Question to Ask the Director"
      pattern: "Summary|Connection|Question"
    - from: "deck closing section"
      to: "INDEX.md interview master narrative"
      via: "first-person spoken refinement"
      pattern: "assembly line"
---

<objective>
Produce ONE consolidated AGMS patent rehearsal deck (a single markdown study doc) that
Juan rehearses aloud before the GE Vernova interview. The deck distills the *already-complete*
deep-read research (INDEX.md + six per-patent files) into the three things Juan must deliver
per patent (PAT-01 summary, PAT-02 connection, PAT-03 question), ordered as the AGMS
"assembly line," plus a deck-level ~90s pitch and a closing master narrative.

Purpose: Give Juan one performance-layer artifact to rehearse from — distinct from the
reference docs (INDEX.md / AGMS-architecture.md), which stay untouched (D-03/D-04).
Output: `.planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md`

This is an EDITORIAL phase. The deep reading is done. Do NOT re-read source PDFs, do NOT
re-derive connections, do NOT invent new questions. Distill, voice, and order the existing
material. Any element written from scratch instead of lifted from source is wasted work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-director-s-patents-deep-read/03-CONTEXT.md
@.planning/phases/03-director-s-patents-deep-read/03-RESEARCH.md

<source_map>
<!-- Exactly where each deck element comes from. The executor distills from THESE. -->

PRIMARY SOURCE (lift pitch, master narrative, pipeline order, glossary from here):
@.planning/research/patents/INDEX.md
  - "The Formation Pipeline" diagram → the pipeline order + one-liner owners
  - "One-Line Summary Per Patent" → seed each 2-3 sentence summary
  - "How Juan Connects His Work to This Patent Family" table → connections
  - "The interview master narrative" paragraph (bottom) → 90s pitch AND closing master narrative
  - "Key Technical Terms Cheat Sheet" → condensed 8-10 term glossary (optional)

PER-PATENT SOURCES (each has: Problem Addressed, Sharpest bridge story, Question to Ask the Director):
@.planning/research/patents/adaptive-power.md      (Parent; Q at line ~98; bridge story line ~94)
@.planning/research/patents/logistician-module.md  (Q at line ~99; bridge story line ~95)
@.planning/research/patents/asset-portfolio.md      (Q at line ~128; bridge story line ~124)
@.planning/research/patents/operation-loop.md       (GRANTED ★; Q at line ~107; bridge story line ~103)
@.planning/research/patents/scout-command.md        (Q at line ~108; bridge story line ~106)
@.planning/research/patents/data-management.md       (Q at line ~114; bridge story line ~110)
</source_map>

<style_reference>
<!-- Phase 2 oral-rehearsal voice precedent. Note: this deck is ONE file, NOT the per-note split. -->
@.planning/phases/02-transmission-virtual-sensing/notes/TVS-01-voltage-stability.md
</style_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the consolidated AGMS patent rehearsal deck</name>
  <files>.planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md</files>

  <read_first>
    Read these BEFORE writing — distill from them, do not invent:
    - .planning/research/patents/INDEX.md — for the pipeline order, the one-liner per patent, the connection table, the "interview master narrative" (used for BOTH the 90s pitch and the closing), and the glossary cheat sheet.
    - .planning/research/patents/adaptive-power.md — "Problem Addressed", "Sharpest bridge story" (line ~94), "Question to Ask the Director" (line ~98).
    - .planning/research/patents/logistician-module.md — "Problem Addressed", "Sharpest bridge story" (line ~95), "Question to Ask the Director" (line ~99).
    - .planning/research/patents/asset-portfolio.md — "Problem Addressed", "Sharpest bridge story" (line ~124), "Question to Ask the Director" (line ~128).
    - .planning/research/patents/operation-loop.md — "Problem Addressed", granted-claim summary, "Sharpest bridge story" (line ~103), "Question to Ask the Director" (line ~107).
    - .planning/research/patents/scout-command.md — "Problem Addressed", "Sharpest bridge story" (line ~106), "Question to Ask the Director" (line ~108).
    - .planning/research/patents/data-management.md — "Problem Addressed", "Sharpest bridge story" (line ~110), "Question to Ask the Director" (line ~114).
    - .planning/phases/02-transmission-virtual-sensing/notes/TVS-01-voltage-stability.md — for spoken-rehearsal voice precedent only (this deck is ONE file in first-person).
  </read_first>

  <action>
Create the single file `.planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md` with this exact structure and ordering.

VOICE: First-person, for Juan to say aloud ("I built…", "My OSED platform…", "My CVXPY MPC…"). No LaTeX. Performance layer, not reference layer — link to source files for depth instead of repeating them.

HEADER + FRONT MATTER:
- Title `# AGMS Patent Rehearsal Deck`
- A `For:` / `Purpose:` header line in the Phase-1/2 oral-rehearsal style.
- `## How to Use This Deck` — one short paragraph instructing Juan to read each section ALOUD and move on; the 2-3 sentence summary IS the spoken track (D-06), there is no separate aloud block.
- `## The Assembly Line at a Glance` — the pipeline mnemonic in order (D-05): Parent → Logistician → Asset Portfolio → Operation Loop Formation ★ → Scout Command → Data Management, with the one-liner owners (which/procure → verify → assemble+simulate → deploy → data-plane-for-all). Source: INDEX.md "The Formation Pipeline".
- `## The ~90-Second Pitch` (D-07) — the whole-family "walk the assembly line" spoken narrative, lightly adapted from INDEX.md "interview master narrative" to a deliverable ~90s / 6-step form.

SIX PATENT SECTIONS, in pipeline order (D-05). For EACH patent use this template:
```
## Patent N: <Title>
**<Publication number> | Sharif-Askary | <Assignee>**
*(full reference: `.planning/research/patents/<file>.md`)*

### Summary
<2-3 sentences. S1: what it solves. S2: the key mechanism. S3 (optional): the payoff/distinctive claim. Must pass "Juan can say this in ~20s without notes." NO reference numbers like "1023A", NO application numbers inside the summary prose.>

### My Connection
<One sharp first-person standalone connection, distilled from that file's "Sharpest bridge story" to roughly one sentence. Each patent's connection MUST be distinct (see distinctness rule below).>

### Question for the Director
> "<the architecture-level question from that file's 'Question to Ask the Director', verbatim or lightly tightened — NO claim-number quoting (D-09)>"
```

The six sections, in order, with their distinct primary connection:
1. Patent 1: Adaptive Power Grid Management System (Parent) — WO 2023/064623 A1 | General Electric Company. Connection = OSED edge nodes are the runtime scouts land on (broadest: K8s/K3s, FastAPI, MQTT, CVXPY).
2. Patent 2: Logistician Module — US 2024/0337997 A1. Connection = OSED FastAPI orchestrator/control plane resolving an abstract job into placed, audited K3s workloads.
3. Patent 3: Asset Portfolio Manager — WO 2024/211758 A1. Connection = SI-MAPPER as the DNA-map: typed, relationship-aware asset fingerprint + knowledge-graph traversal (ASHRAE 223P).
4. Patent 4: Operation Loop Formation ★ (GRANTED — GE Vernova's own IP) — US 12,596,341 B2 | GE Vernova Infrastructure Technology LLC. Connection = CVXPY MPC solve-before-commit = claim 3 simulate-before-commit (the most literal analogy — anchor MPC HERE). Give this section a ★ in the heading and a brief "(GRANTED — GE Vernova's own IP)" label, and cue Juan to pause and let it land. Naming "claim 3" here is the talking point and IS allowed (it is the connection, not an exam-style question).
5. Patent 5: Scout Command — US 2024/0339835 A1. Connection = K3s scheduler IS the Scout Incubator (declarative desired state, role-typed pods, reconcile replicas).
6. Patent 6: Data Management — WO 2024/211800 A1. Connection = OSED FastAPI service layer = data management module (mediated role-scoped views; gRPC/MCP auth = ga-authenticationkey).

CLOSING + REFERENCE:
- `## Closing: The Master Narrative` (D-08) — the unified "one assembly line = my stack" narrative as a 60-90s first-person spoken block, refined from INDEX.md "interview master narrative". Must explicitly tie: OSED runtime = where scouts land; K3s scheduler = Scout Incubator; SI-MAPPER = DNA map; CVXPY MPC = simulate-before-commit gate of GE Vernova's GRANTED patent; Databricks = GWM data foundation.
- `## Quick-Reference Cheat Sheet` (optional, Claude's discretion) — condensed 8-10 essential terms only (AGMS, GWM/GA/GWCH, CaCSM, ORACS, scout roles, DNA/schema, POV file, ga-authenticationkey). Lifted/condensed from INDEX.md cheat sheet.

DISTINCTNESS RULE (avoid Pitfall 2): CVXPY/MPC may appear as the PRIMARY connection ONLY in Operation Loop Formation (and may be referenced once in the closing). The other five patents use their own distinct analog above. If CVXPY appears as the primary connection in more than one patent section, revise.

LENGTH GUARD (avoid Pitfall 4): No per-patent section should exceed ~250 words. This is a performance script, not a second INDEX.md.

DO NOT modify INDEX.md or AGMS-architecture.md (D-04). DO NOT create per-patent files (D-03 — one consolidated file only).
  </action>

  <verify>
    <automated>test -f .planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md && D=.planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md; test $(grep -cE '^## Patent [1-6]:' "$D") -eq 6 && grep -q '12,596,341' "$D" && grep -q 'GRANTED' "$D" && grep -q '★' "$D" && grep -qiE '## The ~?.?90' "$D" && grep -qi 'Master Narrative' "$D" && test $(grep -cE '^### Summary' "$D") -eq 6 && test $(grep -cE '^### My Connection' "$D") -eq 6 && test $(grep -cE '^### Question for the Director' "$D") -eq 6 && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    All checks run against the produced deck file D = `.planning/phases/03-director-s-patents-deep-read/notes/AGMS-patent-rehearsal-deck.md`:
    - File exists (`test -f "$D"`).
    - Exactly six patent sections: `grep -cE '^## Patent [1-6]:' "$D"` returns 6.
    - Patents appear in pipeline order (D-05): the literal sequence of `## Patent` headings is Adaptive Power / Parent, Logistician, Asset Portfolio, Operation Loop Formation, Scout Command, Data Management.
    - Each patent has all three elements: `grep -cE '^### Summary' "$D"` = 6, `grep -cE '^### My Connection' "$D"` = 6, `grep -cE '^### Question for the Director' "$D"` = 6.
    - Operation Loop flagged as keystone: file contains `12,596,341`, contains `GRANTED`, and contains the `★` marker.
    - Deck-level pitch present: a heading matching `## The ~90-Second Pitch` (grep `'## The ~?.?90'` or `'90-Second'`).
    - Closing master narrative present: file contains a heading matching `Master Narrative` (case-insensitive).
    - Assembly-line framing present: `grep -qi 'assembly line' "$D"`.
    - Distinctness: `CVXPY` appears as a `### My Connection` primary analog in at most the Operation Loop section (manual spot-check the five other connection lines do not lead with CVXPY/MPC).
    - Reference docs untouched: `git status --porcelain .planning/research/patents/INDEX.md .planning/research/patents/AGMS-architecture.md` is empty (no modifications).
    - No per-patent deck files created (only the single deck file is added under notes/).
  </acceptance_criteria>

  <done>
    One consolidated rehearsal deck exists with six patent sections in pipeline order, each
    carrying a 2-3 sentence summary, one distinct connection, and one architecture-level
    question; plus a ~90s family pitch and a closing master narrative; the Operation Loop
    patent is visually flagged (★ + GRANTED + 12,596,341); INDEX.md and AGMS-architecture.md
    are unmodified. All automated grep checks PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This deliverable is a static internal markdown study document. It has no runtime, no network surface, no user input, no secrets, and no code execution. There is no trust boundary to cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Patent rehearsal deck (markdown) | accept | The deck distills already-public patent filings and Juan's own CV; it is a personal interview-prep artifact committed to a private repo. No external attack surface; no secrets present. Nothing to mitigate. |

No threats meet the "high" block threshold. This phase produces a personal study document only.
</threat_model>

<verification>
- The single deck file exists at the path above and passes every grep check in Task 1's acceptance_criteria.
- All six patents are covered (PAT-01), each with a connection (PAT-02) and a question (PAT-03).
- Pipeline ordering (D-05) is preserved; Operation Loop is flagged as the granted GE Vernova keystone.
- The ~90s pitch (D-07) and the closing master narrative (D-08) are present.
- INDEX.md and AGMS-architecture.md remain unmodified (D-04).
</verification>

<success_criteria>
Maps to ROADMAP Phase 3 success criteria (now six patents per D-01/D-02):
1. Juan can summarize each patent in 2-3 sentences identifying the core claim — six `### Summary` blocks present.
2. Juan can name one concrete connection per patent to his OSED/HEMS/SI-MAPPER work — six `### My Connection` blocks, each distinct.
3. Juan has one prepared director-directed, architecture-level question per patent — six `### Question for the Director` blocks.
Plus deck-level: a ~90s family pitch and a unified closing master narrative.
</success_criteria>

<output>
After completion, create `.planning/phases/03-director-s-patents-deep-read/03-01-SUMMARY.md`
</output>
