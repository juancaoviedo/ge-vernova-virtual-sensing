---
phase: 02-transmission-virtual-sensing
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata_demo.py
  - .planning/phases/02-transmission-virtual-sensing/demo/README.md
  - .planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata.png
autonomous: true
requirements: [TVS-02, TVS-03]

must_haves:
  truths:
    - "A from-scratch NumPy demo solves the 3-bus DC power-flow angle WLS, injects one gross error, detects it via chi-squared, identifies it via the largest normalized residual, removes it, and re-solves"
    - "The script runs to completion (exit 0), prints a chi-squared / flagged-measurement / re-solved-angles console readout, and writes the output PNG"
    - "The demo concretizes TVS-02 (P = Bθ angle WLS) and TVS-03 (chi-squared + normalized-residual bad-data detection), reusing Phase 1's WLS/chi2 machinery"
    - "Honors CONTEXT.md decisions D-01 (one small ~80-line Python demo), D-02 (reuses Phase 1's WLS/Gauss-Newton machinery, self-contained with README mirroring Phase 1's demo/ layout), D-03 (demo scope focused on TVS-02 + TVS-03; TVS-01/TVS-04 stay notes-only)"
  artifacts:
    - path: ".planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata_demo.py"
      provides: "~80-line from-scratch 3-bus DC power-flow WLS + chi-squared/normalized-residual bad-data detection demo"
      contains: "chi2"
    - path: ".planning/phases/02-transmission-virtual-sensing/demo/README.md"
      provides: "Demo README (What it demonstrates / Prerequisites / How to Run / Interview value), mirroring Phase 1's demo README"
      contains: "normalized"
    - path: ".planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata.png"
      provides: "Generated output figure (normalized-residual bar chart + true-vs-estimated angles before/after removal)"
  key_links:
    - from: "dc_powerflow_baddata_demo.py"
      to: "scipy.stats.chi2 threshold + numpy WLS solve"
      via: "chi2.ppf detection and numpy.linalg.solve one-shot WLS"
      pattern: "chi2.ppf|np.linalg.solve"
    - from: "dc_powerflow_baddata_demo.py"
      to: "dc_powerflow_baddata.png"
      via: "script_dir + plt.savefig"
      pattern: "savefig"
---

<objective>
Build the single Phase 2 hands-on demo (D-01): a ~80-line from-scratch NumPy/SciPy/matplotlib
script that sets up a 3-bus DC power-flow network, estimates bus voltage angles via weighted
least squares from a redundant measurement set, injects one gross error, detects it with the
chi-squared test, identifies it via the largest normalized residual, removes it, and re-solves —
the canonical textbook illustration of TVS-02 + TVS-03 together. Paired with a README and the
generated output PNG, mirroring Phase 1's demo/ layout.

Purpose: Give Juan a concrete "I built this" whiteboard story that makes the TVS-02 angle-WLS
and TVS-03 chi-squared/normalized-residual notes tangible. The notes lead; this demo reinforces.

Output: demo/dc_powerflow_baddata_demo.py, demo/README.md, and demo/dc_powerflow_baddata.png.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transmission-virtual-sensing/02-RESEARCH.md
@.planning/phases/02-transmission-virtual-sensing/02-PATTERNS.md

This is an INTERVIEW-PREP project. The demo is illustrative — keep it from-scratch in
NumPy/SciPy/matplotlib (all already installed: numpy 1.26.4, scipy 1.11.4, matplotlib 3.6.3),
NO power-flow library (no pandapower/PYPOWER — the point is to SHOW the WLS math). Keep it
LINEAR DC ($h(\theta)=H\theta$) — do NOT use AC power flow. Reuse Phase 1's demo scaffolding
(headless Agg backend, np.random.seed(42), script_dir+savefig, banner console block, chi2.ppf).
The TVS-02 and TVS-03 notes (Plans 01–02) are the conceptual source; this demo concretizes them.

The exact specification (network, measurement set, bad-data flow, output) is in 02-RESEARCH.md
§"Demo — Exact Specification" and the build pattern is in 02-PATTERNS.md
§"demo/dc_powerflow_baddata_demo.py". Follow those — do not improvise the numbers.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the 3-bus DC power-flow WLS + bad-data detection demo script</name>
  <files>.planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata_demo.py</files>
  <read_first>
    - .planning/phases/02-transmission-virtual-sensing/02-RESEARCH.md §"Demo — Exact Specification (TVS-02 + TVS-03)" (network: buses 1/2/3, lines 1-2/1-3/2-3, b=10 p.u., slack=bus1 θ1=0, n=2; reduced B=[[20,-10],[-10,20]]; loads P2=-1.0 P3=-0.5; m=5 measurements [inj@2, inj@3, flow 1→2, flow 1→3, flow 2→3] → df=3; σ=0.01; np.random.seed(42); corrupt measurement 5 at +10σ..+20σ; solve#1 → χ² → rN argmax = msmt 5 → remove → solve#2; console + PNG outputs) and §"Code Examples" (WLS one-shot solve, χ² detection, normalized-residual identification, rank check)
    - .planning/phases/02-transmission-virtual-sensing/02-PATTERNS.md §"demo/dc_powerflow_baddata_demo.py" (module docstring + Run/Output header, headless-imports + seed block, constants block, WLS one-shot solve, χ²+rN block, function decomposition build_B_matrix/build_H/wls_solve/chi2_test/normalized_residuals/run_demo, console banner, PNG save pattern, anti-patterns)
    - .planning/phases/01-kalman-state-estimation/demo/ekf_line_temp_demo.py (the literal skeleton to mirror: module docstring with Run/Output lines, UPPER_CASE constants block with unit comments, `matplotlib.use('Agg')`, `np.random.seed(42)`, `nis_check` chi2.ppf usage, `run_demo()`, `print("="*62)` banner, `script_dir = os.path.dirname(os.path.abspath(__file__))` + `plt.savefig(out_path)`)
  </read_first>
  <action>
    Create demo/dc_powerflow_baddata_demo.py (~80 lines), from-scratch NumPy/SciPy/matplotlib,
    mirroring ekf_line_temp_demo.py's structure. Implement exactly the RESEARCH §Demo spec:

    Module docstring (mirror Phase-1): one-paragraph description (3-bus DC power-flow WLS angle
    estimation with chi-squared + largest-normalized-residual bad-data detection), then
    `Dependencies: numpy, scipy, matplotlib`, `Run: python3 dc_powerflow_baddata_demo.py`,
    `Output: dc_powerflow_baddata.png (saved beside this script) + console χ²/flagged/re-solved readout`,
    and a constants summary line (b12=b13=b23=10, slack=bus1, loads P2=-1.0 P3=-0.5).

    Imports/headless block: `import os`, `import numpy as np`, `import matplotlib; matplotlib.use('Agg')`,
    `import matplotlib.pyplot as plt`, `from scipy.stats import chi2`.

    Constants block (UPPER_CASE with unit comments): susceptances B12=B13=B23=10.0 p.u., loads
    P2=-1.0, P3=-0.5 p.u., measurement σ SIGMA=0.01, slack = bus 1.

    Functions (named, mirroring Phase-1 style):
    - `build_B_matrix()` → returns the reduced Bbus [[20,-10],[-10,20]] (susceptance Laplacian,
      slack row/col deleted).
    - `build_H()` → returns the m=5 × n=2 measurement matrix H whose rows are the linear
      sensitivities of [injection@2, injection@3, flow 1→2, flow 1→3, flow 2→3] to [θ2,θ3].
    - `wls_solve(H, W, z)` → one-shot WLS: `G = H.T @ W @ H`; `theta_hat = np.linalg.solve(G, H.T @ W @ z)`;
      `r = z - H @ theta_hat`; `J = r @ W @ r`; return theta_hat, r, J, G.
    - `chi2_test(J, df)` → `threshold = chi2.ppf(0.95, df)`; return threshold, bool(J > threshold).
    - `normalized_residuals(r, H, G, W)` → `Omega = np.diag(1/np.diag(W)) - H @ np.linalg.inv(G) @ H.T`;
      `rN = np.abs(r) / np.sqrt(np.diag(Omega))`; return rN.
    - `run_demo()` → `np.random.seed(42)`; build B and ground-truth angles via
      `theta_true = np.linalg.solve(B_red, [P2, P3])`; build H; `z_clean = H @ theta_true`; add
      Gaussian noise (σ=SIGMA); INJECT a gross error on measurement index 4 (the 5th, flow 2→3),
      e.g. `+15*SIGMA`; WLS solve #1 → chi2_test (df = m-n = 3) → expect bad data; compute rN →
      `suspect = int(np.argmax(rN))` (expect index 4, rN > 3); remove measurement 4 (slice H and z,
      df → 2) → WLS solve #2 → expect J below threshold and theta_hat near theta_true.

    Console output: a `print("="*62)` banner block reporting df, χ² threshold, J before/after,
    the flagged measurement index + its rN, and true vs estimated angles before/after removal.

    PNG output: `script_dir = os.path.dirname(os.path.abspath(__file__))`;
    `out_path = os.path.join(script_dir, 'dc_powerflow_baddata.png')`; two-panel figure —
    (a) bar chart of normalized residuals with a 3σ line and the flagged bar highlighted,
    (b) true vs estimated angles before/after bad-data removal; `plt.savefig(out_path, dpi=150, bbox_inches='tight')`
    and `print(f"Saved {out_path}")`. End with `if __name__ == "__main__": run_demo()`.

    Keep it linear DC (no AC power flow); no pandapower/PYPOWER. Target ~80 lines. Use the
    susceptance-rank observability check (`np.linalg.matrix_rank(H) == n`) as an optional one-line
    sanity assert if it stays within budget. Optional TVS-01 Thevenin touch: SKIP (keep tight).
  </action>
  <verify>
    <automated>F=.planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata_demo.py; test -f "$F" && grep -q 'chi2' "$F" && grep -qE 'np.linalg.solve' "$F" && grep -q 'savefig' "$F" && grep -q 'seed(42)' "$F" && grep -q "use('Agg')" "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains `chi2` (scipy χ² threshold); contains `np.linalg.solve` (one-shot WLS);
    contains `savefig` (PNG output); contains `seed(42)` (reproducibility); contains `use('Agg')`
    (headless backend). Command prints OK. (Runtime verification is Task 2's job.)
  </acceptance_criteria>
  <done>Demo script exists implementing the 3-bus DC WLS + chi-squared + normalized-residual bad-data flow from RESEARCH §Demo, from-scratch in NumPy/SciPy/matplotlib, with the named functions, the banner console readout, and the script_dir+savefig PNG output.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the demo, generate the PNG, and write the demo README</name>
  <files>.planning/phases/02-transmission-virtual-sensing/demo/README.md, .planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata.png</files>
  <read_first>
    - .planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata_demo.py (the script just written — run it; capture the real console output to paste into the README "Expected console output" block; confirm it exits 0, flags measurement 5, and J drops below threshold after removal)
    - .planning/phases/01-kalman-state-estimation/demo/README.md (the literal README template: # title + summary, ## What It Demonstrates numbered list, ## Prerequisites, ## How to Run with an Expected-console-output fenced block + expected PNG name, ## Interview Talking Points blockquote, ## Key Implementation Details table, ## Files tree)
    - .planning/phases/02-transmission-virtual-sensing/02-PATTERNS.md §"demo/README.md" (section order to reuse) and 02-RESEARCH.md §"Demo — Exact Specification" "Output" + "Interview value" line
  </read_first>
  <action>
    First RUN the demo to generate the PNG and capture real output:
    `python3 .planning/phases/02-transmission-virtual-sensing/demo/dc_powerflow_baddata_demo.py`
    Confirm: exit code 0; console reports bad data DETECTED on solve #1; the flagged measurement
    index = the corrupted line-flow 2→3 (measurement 5) with rN > 3; after removal, J is below the
    χ² threshold and the re-solved angles are near θ_true (≈ [-0.0833, -0.0667] rad). Confirm
    dc_powerflow_baddata.png was written beside the script. If the run fails or the expected
    detection does not occur, fix dc_powerflow_baddata_demo.py (e.g. measurement-matrix rows,
    injected-error magnitude, or df) until it does — the demo must demonstrate the full flow.

    Then create demo/README.md mirroring Phase 1's demo/README.md section order:
    1. `# TVS-02/03: 3-Bus DC Power-Flow Bad-Data Detection Demo` + one-paragraph summary.
    2. `## What It Demonstrates` — numbered list: build 3-bus DC network (P = Bθ, susceptance
       Laplacian, slack=bus1); WLS angle estimate from m=5 redundant measurements; inject one gross
       error on line-flow 2→3; chi-squared test detects bad data is present; largest-normalized-
       residual identifies WHICH measurement; remove it and re-solve to recover clean angles.
    3. `## Prerequisites` — numpy/scipy/matplotlib, "all already installed", no power-flow library.
    4. `## How to Run` — the `cd … && python3 dc_powerflow_baddata_demo.py` command, an
       **Expected console output** fenced block (paste the REAL captured output), and the expected
       PNG filename `dc_powerflow_baddata.png`.
    5. `## Interview Talking Points` — `> blockquote` points, including the RESEARCH "Interview value"
       line: "3-bus DC state estimator that detects and removes a corrupted measurement via
       chi-squared + largest-normalized-residual — the textbook TVS-02+TVS-03 illustration, built
       from scratch in NumPy." Note the χ²-detects / rN-identifies division of labor.
    6. `## Key Implementation Details` — a small table (one-shot linear WLS vs Gauss-Newton; χ² df=m-n;
       Ω = R − H G⁻¹ Hᵀ; from-scratch, no power-flow library).
    7. `## Files` — directory tree (script, README, png).

    Do NOT hand-author the PNG — it must be the generated output of the script run above.
  </action>
  <verify>
    <automated>D=.planning/phases/02-transmission-virtual-sensing/demo; python3 "$D/dc_powerflow_baddata_demo.py" && test -f "$D/dc_powerflow_baddata.png" && test -f "$D/README.md" && grep -qi 'normalized' "$D/README.md" && grep -qi 'chi-squared\|chi2\|chi²' "$D/README.md" && grep -qi 'How to Run' "$D/README.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    `python3 dc_powerflow_baddata_demo.py` exits 0; dc_powerflow_baddata.png exists beside the
    script; README.md exists and contains "normalized" (residual), a chi-squared reference, and a
    "How to Run" section. Command prints OK.
  </acceptance_criteria>
  <done>The demo runs to completion (exit 0), generates dc_powerflow_baddata.png (normalized-residual bar chart + before/after angles), correctly detects and identifies the corrupted measurement, and README.md documents it with a real captured console-output block and the interview value line.</done>
</task>

</tasks>

<threat_model>
No attack surface — this plan produces a local-only demo script, its README, and a generated PNG.
There is no untrusted input (all data is synthetic and seeded), no network, no auth, and no data
persistence beyond writing a figure beside the script. STRIDE threat modeling does not apply.
</threat_model>

<verification>
- dc_powerflow_baddata_demo.py exists, is from-scratch NumPy/SciPy/matplotlib (no power-flow library), and is linear DC.
- Running the script exits 0, prints the χ²/flagged-measurement/re-solved-angles readout, and writes dc_powerflow_baddata.png.
- The demo detects bad data (J above threshold on solve #1), identifies measurement 5 via the largest normalized residual, and recovers near-true angles after removal.
- README.md mirrors Phase 1's section order with a real captured console-output block and the interview-value line.
</verification>

<success_criteria>
Juan can run the demo, watch it detect and remove a corrupted measurement via chi-squared +
largest normalized residual, and use it as a concrete "I built this 3-bus state estimator"
whiteboard story that makes the TVS-02 and TVS-03 notes tangible in the interview.
</success_criteria>

<output>
After completion, create `.planning/phases/02-transmission-virtual-sensing/02-03-SUMMARY.md`
</output>
