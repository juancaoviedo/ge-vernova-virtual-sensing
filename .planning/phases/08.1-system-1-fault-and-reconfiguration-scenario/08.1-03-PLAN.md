---
phase: 08.1-system-1-fault-and-reconfiguration-scenario
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - system1-measurement-source/grafana/provisioning/dashboards/ieee33-fault-event.json
autonomous: true
requirements:
  - SPEC-8
  - D-02
  - D-03
  - D-13
user_setup: []

must_haves:
  truths:
    - "A second Grafana dashboard ieee33-fault-event.json exists, titled 'IEEE 33-Bus — Fault & Reconfiguration', auto-provisioned via the existing default.yml directory scan (no default.yml edit) (SPEC-8)."
    - "It has a unique uid (ieee33-fault-v1) distinct from the state dashboard's ieee33-state-v1 so neither silently replaces the other (SPEC-8)."
    - "All panel queries target from(bucket: fault_event) via the shared datasource uid ieee33-influxdb (SPEC-8, D-02/D-03)."
    - "SPEC-8 minimum panels present: bus-voltage envelope, OLTC tap, served-load/slack feed-in drop+recovery, and a 3-phase visual marker derived from the phase tag (SPEC-8)."
    - "All four D-13 extras present: dead-zone bus-voltage time-series (buses 8-17), dead-bus count from event.n_dead_buses, restored min-voltage stat with green>=0.95/amber>=0.94/red thresholds, and a topology/event state table from the event measurement (D-13, D-03)."
    - "The existing ieee33-state.json dashboard is byte-for-byte unchanged (SPEC-1)."
  artifacts:
    - path: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-fault-event.json"
      provides: "Auto-provisioned fault/reconfiguration dashboard over the fault_event bucket"
      contains: "ieee33-fault-v1"
  key_links:
    - from: "ieee33-fault-event.json panels"
      to: "fault_event bucket"
      via: "Flux from(bucket) in every target query"
      pattern: "fault_event"
    - from: "ieee33-fault-event.json"
      to: "shared InfluxDB datasource"
      via: "datasource uid"
      pattern: "ieee33-influxdb"
    - from: "default.yml directory scan"
      to: "ieee33-fault-event.json"
      via: "auto-provisioning (no default.yml change)"
      pattern: "/etc/grafana/provisioning/dashboards"
---

<objective>
Create `system1-measurement-source/grafana/provisioning/dashboards/ieee33-fault-event.json` — a
second, auto-provisioned Grafana dashboard rendering the 40-step fault/reconfiguration event over
the new `fault_event` bucket. It mirrors the structure, datasource UID, and schemaVersion of the
existing `ieee33-state.json` while targeting the fault dataset and adding the four D-13 extra panels.

Purpose: SPEC-8 requires a separate dashboard that shows the voltage collapse of the isolated zone
and its recovery on restoration, with zero manual setup. This is the visual proof of the scenario
for the interview narrative.
Output: one new dashboard JSON; the existing dashboard and default.yml are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-SPEC.md
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-CONTEXT.md
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-PATTERNS.md
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-01-PLAN.md

<interfaces>
<!-- The exact analog and the fault_event schema produced by Plan 01 + Plan 02. -->

ANALOG to copy structure from: grafana/provisioning/dashboards/ieee33-state.json (307 lines).
  - Top-level keys: __inputs, __requires, annotations, description, editable, fiscalYearStartMonth,
    graphTooltip, id (null), links, panels[], refresh, schemaVersion (39), tags, templating, time,
    timepicker, timezone ("utc"), title, uid, version.
  - Each panel target: {"datasource": {"type": "influxdb", "uid": "ieee33-influxdb"},
    "query": "<flux>", "refId": "A"}.
  - Datasource uid "ieee33-influxdb" is defined in grafana/provisioning/datasources/influxdb.yml;
    the dashboard overrides the bucket per-query with from(bucket: "fault_event"). Do NOT change the uid.

REQUIRED top-level deltas from ieee33-state.json (PATTERNS lines 250-280):
  - "uid": "ieee33-fault-v1"   (MUST differ from "ieee33-state-v1" — duplicate uids cause silent replacement)
  - "title": "IEEE 33-Bus — Fault & Reconfiguration"
  - "tags": ["ieee33","power-systems","fault","reconfiguration"]
  - "__requires" must include the panel types used: timeseries, stat, table.
  - "time": a window covering the 40-step 2-minute event. The anchor is 2017-06-07T18:00:00Z
    (FAULT_EVENING_PEAK_STEP=72 -> 18:00 UTC, verified by the planner). Set
    "from": "2017-06-07T17:59:00.000Z", "to": "2017-06-07T18:03:00.000Z".

fault_event schema produced by Plan 01/02 (query against these):
  measurement "bus"   tag bus_id (0..32), tag energised ("1"/"0"); fields vm_pu, va_degree
  measurement "line"  tag line_id, tag energised; fields p_from_mw,q_from_mvar,p_to_mw,q_to_mvar,loading_percent,pl_mw,ql_mvar
  measurement "sgen"  tag sgen_id; fields p_mw, q_mvar
  measurement "system"(no tag); fields total_load_mw,total_gen_mw,total_loss_mw,vmin_pu,vmax_pu,slack_p_mw,slack_q_mvar,tap_pos,shift_degree
  measurement "event" tag phase ("pre_fault"/"faulted_isolated"/"restored");
                      fields faulted_line_id(int), tie_closed(int), tie_id(int,-1 if open),
                             n_dead_buses(int), dead_buses(str csv)
  Dead zone = buses 8..17; healthy reference bus = 0 (slack-side, always energised).

Flux range token to use in most queries (matches the dashboard time window):
  range(start: 2017-06-07T17:59:00Z, stop: 2017-06-07T18:03:00Z)
JSON-escape the inner double-quotes exactly as ieee33-state.json does (e.g. from(bucket: \"fault_event\")).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ieee33-fault-event.json — SPEC-8 minimum panels + all four D-13 extras</name>
  <read_first>
    - system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json (THE analog — copy top-level metadata + panel JSON shapes verbatim, then retarget)
    - system1-measurement-source/grafana/provisioning/dashboards/default.yml (confirm it scans the whole dir — no edit needed)
    - system1-measurement-source/grafana/provisioning/datasources/influxdb.yml (datasource uid ieee33-influxdb; defaultBucket state — overridden per query)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (dashboard section lines 246-392: stat/table/dead-zone panel snippets pre-written)
    - .planning/phases/08.1-.../08.1-CONTEXT.md (D-02 phase tag, D-03 event fields, D-13 four extras)
  </read_first>
  <action>
    Create `grafana/provisioning/dashboards/ieee33-fault-event.json`. Start from a copy of
    `ieee33-state.json`'s top-level structure (schemaVersion 39, editable true, graphTooltip 1,
    timezone "utc", refresh "30s", templating {"list":[]}, annotations {"list":[]}, id null,
    links [], fiscalYearStartMonth 0), then apply the deltas from the interfaces block (uid
    `ieee33-fault-v1`, the new title, fault tags, the event time window, and `__requires` including
    timeseries + stat + table). Set a clear `description` summarizing the 40-step
    fault -> isolate -> restore event.

    Build the `panels` array with distinct integer `id`s and a clean gridPos layout. Every target is
    `{"datasource":{"type":"influxdb","uid":"ieee33-influxdb"},"query":"<flux>","refId":"A"}` and every
    query uses `from(bucket: \"fault_event\")` with
    `range(start: 2017-06-07T17:59:00Z, stop: 2017-06-07T18:03:00Z)` (unless noted).

    SPEC-8 MINIMUM panels:
    (1) id 1 — "Bus Voltage Profile / Envelope (pu)" timeseries. Target ALL buses (no bus_id filter)
        so the dead-zone collapse to 0 is visible against the healthy buses. Query:
        bus measurement, _field == vm_pu, sort by _time. Copy the fieldConfig (thresholds 0.95/1.05,
        min 0.90 / max 1.10, decimals 4) from ieee33-state.json panel id 1. gridPos {h:9,w:12,x:0,y:0}.
    (2) id 2 — "OLTC Tap Position (integer steps, -5 to +5)" timeseries. Copy ieee33-state.json panel
        id 6 fieldConfig (drawStyle line, lineInterpolation stepAfter, showPoints always, pointSize 4,
        min -6 / max 6, decimals 0). Query system measurement, _field == tap_pos. gridPos {h:9,w:12,x:12,y:0}.
    (3) id 3 — "Served Load & Slack Feed-in (MW)" timeseries showing the drop + recovery. Query
        system measurement filtered to `_field == \"total_load_mw\" or _field == \"slack_p_mw\"`, sort
        by _time. unit MW, decimals 4. gridPos {h:8,w:12,x:0,y:9}.
    (4) id 4 — "Event Phase / Dead-bus Pulse (phase-tag marker)" timeseries — the SPEC-8 three-phase
        visual marker. Query the `event` measurement `n_dead_buses` field (0 in pre_fault, 10 in
        isolated, 0 again in restored => a 0->N->0 pulse aligned to the three blocks), sort by _time.
        The legend carries the `phase` tag so each of the three blocks is labelled. unit short,
        decimals 0, min 0. gridPos {h:8,w:12,x:12,y:9}.

    D-13 EXTRAS (all four required):
    (5) id 5 — "Dead-Zone Bus Voltage (buses 8-17) — collapse to 0 -> recovery" timeseries. Query bus
        measurement, _field == vm_pu, with a bus_id filter restricted to
        `r.bus_id == \"8\" or r.bus_id == \"9\" or ... or r.bus_id == \"17\"` (all ten dead-zone buses),
        sort by _time. min 0 / max 1.10, decimals 4 (the zero-fill makes the collapse explicit).
        gridPos {h:8,w:12,x:0,y:17}.
    (6) id 6 — "Dead-Bus Count" timeseries from event.n_dead_buses (0 -> 10 -> 0). Query event
        measurement, _field == n_dead_buses, sort by _time. thresholds green at 0 / red above 0,
        decimals 0, min 0. gridPos {h:8,w:6,x:12,y:17}.
    (7) id 7 — "Restored Block — Min Bus Voltage (pu)" stat (PATTERNS lines 350-375). type "stat",
        options.reduceOptions.calcs ["min"], thresholds mode absolute steps
        red(null)/orange(0.94)/green(0.95), decimals 4. Query bus vm_pu filtered to `energised == \"1\"`
        (so zero-fills do not pull the min down) AND scope the range to the restored window:
        `range(start: 2017-06-07T18:01:00Z, stop: 2017-06-07T18:03:00Z)`. gridPos {h:8,w:6,x:18,y:17}.
    (8) id 8 — "Topology / Event State per Snapshot" table (PATTERNS lines 377-387). type "table".
        Query event measurement, pivot rowKey ["_time"] columnKey ["_field"] valueColumn "_value",
        sort by _time. gridPos {h:9,w:24,x:0,y:25}.

    Ensure valid JSON. Do NOT edit default.yml or ieee33-state.json.
  </action>
  <verify>
    <automated>cd system1-measurement-source && python -m json.tool grafana/provisioning/dashboards/ieee33-fault-event.json > /dev/null && grep -q 'ieee33-fault-v1' grafana/provisioning/dashboards/ieee33-fault-event.json && grep -q 'IEEE 33-Bus' grafana/provisioning/dashboards/ieee33-fault-event.json && test $(grep -c 'fault_event' grafana/provisioning/dashboards/ieee33-fault-event.json) -ge 8 && grep -q 'n_dead_buses' grafana/provisioning/dashboards/ieee33-fault-event.json && grep -q '"type": "table"' grafana/provisioning/dashboards/ieee33-fault-event.json && grep -q '"type": "stat"' grafana/provisioning/dashboards/ieee33-fault-event.json && grep -q 'energised' grafana/provisioning/dashboards/ieee33-fault-event.json && grep -q 'ieee33-influxdb' grafana/provisioning/dashboards/ieee33-fault-event.json && python -c "import json; d=json.load(open('grafana/provisioning/dashboards/ieee33-fault-event.json')); ids=[p['id'] for p in d['panels']]; assert len(d['panels'])>=8, len(d['panels']); assert len(ids)==len(set(ids)), 'duplicate panel ids'; assert d['uid']=='ieee33-fault-v1'; types={p['type'] for p in d['panels']}; assert 'table' in types and 'stat' in types and 'timeseries' in types, types; print(f'dashboard OK: {len(d[\"panels\"])} panels, unique ids, uid ieee33-fault-v1, types {sorted(types)}')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m json.tool grafana/provisioning/dashboards/ieee33-fault-event.json` exits 0 (valid JSON)
    - `grep -c 'ieee33-fault-v1' .../ieee33-fault-event.json` returns at least 1 and the uid is NOT `ieee33-state-v1`
    - dashboard has >= 8 panels with unique ids; panel types include timeseries, stat, and table
    - every panel query targets `fault_event` (`grep -c 'fault_event'` >= 8) and uses datasource uid `ieee33-influxdb`
    - D-13 coverage present: dead-zone bus filter (`bus_id == "8"`), `n_dead_buses`, an `energised == "1"` restored-min stat, and an `event` pivot table (grep each)
    - the python assertion prints `dashboard OK: ...`
    - existing dashboard UNCHANGED: `git diff system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json` is empty AND `git diff .../default.yml` is empty
  </acceptance_criteria>
  <done>ieee33-fault-event.json is valid JSON, auto-provisioned, uid-unique, queries the fault_event bucket, covers all SPEC-8 minimum panels and all four D-13 extras, and leaves ieee33-state.json and default.yml unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A static Grafana dashboard JSON auto-provisioned into the existing local Grafana container. No code executes, no untrusted input is parsed, no auth boundary is crossed, no PII. It reads the local `fault_event` bucket through the pre-existing local-dev InfluxDB datasource from System 1 (Phase 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-081-06 | Tampering | duplicate dashboard uid could silently replace the state dashboard | mitigate | uid is set to `ieee33-fault-v1`, explicitly distinct from `ieee33-state-v1`; an acceptance assertion verifies uniqueness and that ieee33-state.json is unchanged. |
| T-081-07 | Information Disclosure | Grafana anonymous access + local-dev datasource credentials | accept | Anonymous access and the local-dev token are pre-existing System 1 (Phase 8) choices, bound to 127.0.0.1; documented LOCAL DEV ONLY in influxdb.yml and README. No new exposure introduced by adding a dashboard. |

No HIGH threats. Adding a provisioned dashboard introduces no new external attack surface, no untrusted input parsing, and no new network exposure.
</threat_model>

<verification>
- `python -m json.tool` validates the new dashboard JSON.
- Dashboard uid is `ieee33-fault-v1` (distinct from the state dashboard); title is the locked fault title.
- All panel queries hit `fault_event`; SPEC-8 minimums + all four D-13 extras are present.
- `git diff` shows ieee33-state.json and default.yml unchanged (purely additive new file).
</verification>

<success_criteria>
- A second auto-provisioned dashboard renders the 40-step event over the fault_event bucket (SPEC-8).
- SPEC-8 minimum panels + the four D-13 extras present, querying the event/phase metadata (D-02/D-03/D-13).
- uid-unique; existing dashboard and default.yml untouched (SPEC-1, verifiable by git).
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-03-SUMMARY.md`
</output>
