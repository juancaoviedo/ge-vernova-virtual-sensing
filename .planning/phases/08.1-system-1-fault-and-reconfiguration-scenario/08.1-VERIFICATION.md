---
phase: 08.1-system-1-fault-and-reconfiguration-scenario
verified: 2026-06-24T00:00:00Z
status: human_needed
score: 12/12
overrides_applied: 0
human_verification:
  - test: "Open Grafana at http://localhost:3000, navigate to 'IEEE 33-Bus — Fault & Reconfiguration' dashboard, and confirm it renders with data from the fault_event bucket."
    expected: "Two dashboards listed. The fault dashboard shows: (1) bus voltage envelope with dead-zone collapse to 0 pu during faulted_isolated steps, (2) OLTC tap pinned at -2, (3) served-load drop ~3.29 -> ~2.69 MW then recovery, (4) dead-bus count 0->10->0 pulse, (5) dead-zone bus voltages panel (buses 8-17), (6) dead-bus count timeseries, (7) stat panel showing restored min ~0.997 pu in green, (8) topology/event table with all 40 rows. Time window should auto-set to 2017-06-07T17:59–18:03 UTC."
    why_human: "Grafana panel rendering, time-range auto-selection, and visual colour thresholds cannot be verified programmatically without a headless browser. The dashboard JSON and bucket data are verified; only the rendered view needs a human eyeball."
---

# Phase 08.1: System 1 Fault and Reconfiguration Scenario — Verification Report

**Phase Goal:** Produce a separate, re-runnable, quasi-steady-state fault-and-reconfiguration dataset on the existing System 1 IEEE 33-bus model — replicating the Section-8 fault → isolate → tie-restore sequence as a 40-step (2-min @ 3-s), three-block sequence (pre_fault → faulted_isolated → restored) at a frozen evening-peak operating point, with full power-flow state + topology/event metadata per snapshot, persisted to a dedicated InfluxDB bucket (fault_event) with its own provisioned Grafana dashboard, to stress-test the future System 2 estimator. Purely additive — System 1's day driver, buckets, and existing dashboard are untouched.

**Verified:** 2026-06-24
**Status:** human_needed (all automated checks VERIFIED; one Grafana visual render requires human)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A new entry point `uv run fault-sim` runs zero-arg, deterministically, and completes producing exactly 40 snapshots | VERIFIED | `uv run fault-sim` exits 0 twice; output confirms "fault-sim OK — 40 snapshots written to bucket 'fault_event'"; second run yields identical D-17 read-back (40 points) |
| 2 | Load/DER are frozen at the evening-peak step's profile values across all 40 steps; only topology changes | VERIFIED | Console table shows load_pu=3.2922 MW constant across all 40 steps for pre_fault and restored; vmin consistent per block; `run_fault_step()` applies frozen `load_pu/solar_pu/wind_pu` identically at every step |
| 3 | The faulted line (idx 7) and dead zone (buses 8-17) are set out-of-service during the isolated block; served load drops | VERIFIED | ISO block: vmin=1.0000 (healthy remainder only), n_dead=10, served_load=2.6941 MW (drops ~0.60 MW from 3.2922 MW); D-16 gate confirms drop; `_compute_dead_buses()` graph traversal asserts == range(8,18) |
| 4 | A normally-open tie (idx 34) is closed to restore the dead zone; faulted line stays open; network remains radial | VERIFIED | Restored block: vmin=0.9971, n_dead=0, load=3.2922 MW; `nx.is_tree` radiality gate passes on every restored step; `_select_restore_tie` selects line 34 with vmin=0.9861 pu probe; faulted line 7 stays `in_service=False` |
| 5 | Exactly 40 timestamped snapshots at 3-s spacing in three ordered blocks pre_fault(13)/faulted_isolated(7)/restored(20) | VERIFIED | Live InfluxDB query on event measurement: 13 pre_fault, 7 faulted_isolated, 20 restored; D-17 read-back confirms 40 bus_id=0 vm_pu points; timestamps anchored at 2017-06-07T18:00:00Z + 3s×i |
| 6 | Each snapshot stores full per-entity state AND topology/event metadata (faulted line id, tie state, dead buses, phase tag) | VERIFIED | `write_fault_step()` writes bus/line/sgen/system measurements + event measurement per step; event measurement carries phase tag + faulted_line_id/tie_closed/tie_id/n_dead_buses/dead_buses fields; confirmed in live InfluxDB: 40 event points, n_dead_buses transitions 0→10→0 |
| 7 | The new fault_event bucket holds the 40-snapshot series; state bucket with original 96-step day is untouched | VERIFIED | D-17 read-back: 40 bus_id=0 vm_pu in fault_event; direct InfluxDB query on state bucket: bus_id=17 vm_pu = 96 points unchanged |
| 8 | A second auto-provisioned Grafana dashboard renders the fault event with no manual setup | VERIFIED (automated portion) | `ieee33-fault-event.json` passes `python3 -m json.tool`; uid=ieee33-fault-v1 (distinct from ieee33-state-v1); 8 panels with unique ids; types include timeseries/stat/table; all 8 queries target fault_event via datasource uid ieee33-influxdb; auto-provisioned via existing default.yml directory scan (no default.yml edit); VISUAL RENDER needs human |
| 9 | Restored-block min voltage >= 0.95 pu (or >= 0.94 flagged); re-runs produce identical snapshots (deterministic) | VERIFIED | D-16 gate: "D-16 restored vmin OK (clean): 0.9971 pu >= 0.95"; shortfall_flag=False; second run produces identical 40 points (overwrite-in-place confirmed by D-17 = 40 on both runs) |
| 10 | OLTC tap is pinned during the isolated block (D-12); OLTC_REF_BUS=17 is inside the dead zone | VERIFIED | Console table: tap=-2 constant across all 40 steps; `run_fault_step()` uses `run_control=False` + `net.trafo.at[trafo_idx,"tap_pos"] = int(peak_tap)` during FAULT_PHASE_ISO; D-12 comment in config.py documents the landmine |
| 11 | Dead-zone sgens carry the energised tag (WR-02 fix applied); no double-write of dead sgens (WR-01 fix applied) | VERIFIED | Live query: sgen 0 has 40 points; energised values: pre=['1']*13, iso=['0']*7, rst=['1']*20; `write_fault_step` has `dead_sgen_set` skip logic in energised-sgen loop (WR-01); `.tag("energised", "1"/"0")` on sgen points (WR-02) |
| 12 | README documents the fault-scenario run steps and the qualitative article-comparison note | VERIFIED | `grep -q 'uv run fault-sim'`, `'Fault & Reconfiguration'`, `'fault_event'`, `'qualitative'`, `'PSO'`, `'6 & 32'`, `'uv run ingest'` all pass; existing sections (The Rhythm, Determinism, Security Note) intact |

**Score:** 12/12 truths verified (all automated checks pass)

---

### SPEC Requirements Coverage

| Requirement | Plan | Description | Status | Evidence |
|-------------|------|-------------|--------|----------|
| SPEC-1 | 01/02/03/04 | Separate runner; sim.py, state bucket, existing dashboard unchanged | VERIFIED | `git diff sim.py` empty; `git diff ieee33-state.json` empty; `git diff default.yml` empty; state bucket = 96 points |
| SPEC-2 | 02 | Frozen evening-peak operating point | VERIFIED | load_pu/solar_pu/wind_pu held constant; console table load MW identical across all 40 steps |
| SPEC-3 | 02 | Section-8 fault + isolation; de-energised zone out-of-service | VERIFIED | line idx 7 out-of-service in ISO; buses 8-17 out-of-service; served_load drops 3.29→2.69 MW |
| SPEC-4 | 02 | Tie-line reconfiguration restores zone; network radial | VERIFIED | Tie 34 closed in RST; faulted line stays open; nx.is_tree passes all 20 RST steps |
| SPEC-5 | 02 | 40-step, 3-block, 3-second timeline | VERIFIED | 40 timestamped snapshots at 3s spacing; 13/7/20 block split confirmed in live data |
| SPEC-6 | 01/02 | Full state + topology/event metadata per snapshot | VERIFIED | bus/line/sgen/system + event measurement per step; dead_buses comma-joined string in event |
| SPEC-7 | 01 | Dedicated fault_event InfluxDB bucket | VERIFIED | ensure_bucket(client, FAULT_EVENT_BUCKET) creates it; all writes target FAULT_EVENT_BUCKET; state bucket untouched |
| SPEC-8 | 03 | Provisioned Grafana dashboard for scenario | VERIFIED (automated) / HUMAN NEEDED (visual) | Valid JSON, uid ieee33-fault-v1, 8 panels, all D-13 extras; Grafana render needs human |
| SPEC-9 | 02 | Restoration heals voltages >= 0.95 (or >= 0.94 flagged); deterministic | VERIFIED | restored vmin = 0.9971 pu (clean); two consecutive runs = identical 40 snapshots |
| SPEC-10 | 04 | Runbook + qualitative article comparison in README | VERIFIED | All required content verified by grep; Meteab/Tousi/Omran 2025 qualitative framing with correct DG placement note |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `system1-measurement-source/src/ieee33/fault_sim.py` | Zero-arg runner with main(), 3-block walk, validation gates, console table | VERIFIED | 600 lines; main(), _compute_dead_buses(), run_fault_step(), _select_restore_tie() present; imports; runs cleanly |
| `system1-measurement-source/src/ieee33/config.py` | FAULT_EVENT_BUCKET + all FAULT_* constants | VERIFIED | All 14 FAULT_* constants present with correct values; 13+7+20=40 confirmed by import assertion |
| `system1-measurement-source/src/ieee33/influx.py` | write_fault_step() with energised flag, zero-fill, event measurement | VERIFIED | Function exists (183 lines); exact signature with dead_sgen_ids; energised tag on bus/line/sgen; event measurement; writes to FAULT_EVENT_BUCKET; write_state_step() untouched |
| `system1-measurement-source/pyproject.toml` | fault-sim console-script entry; networkx dependency | VERIFIED | `fault-sim = "ieee33.fault_sim:main"` present; `networkx>=3.0` in dependencies; existing entries intact |
| `system1-measurement-source/grafana/provisioning/dashboards/ieee33-fault-event.json` | Auto-provisioned fault dashboard, uid ieee33-fault-v1, 8 panels | VERIFIED | Valid JSON (412 lines); uid ieee33-fault-v1; 8 panels with unique ids [1-8]; types timeseries/stat/table; all queries target fault_event; datasource uid ieee33-influxdb throughout |
| `system1-measurement-source/README.md` | Fault-scenario runbook + qualitative comparison note (additive) | VERIFIED | 72 lines added, 0 deleted; all required content present; existing sections intact |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| fault_sim.py | influx.write_fault_step | per-step write call | VERIFIED | `influx.write_fault_step(` present at line 466; called for every step in the 40-step loop |
| fault_sim.py | config.FAULT_LINE_IDX / FAULT_TIE_IDX / FAULT_DEAD_BUS_IDX / FAULT_EVENING_PEAK_STEP | config import | VERIFIED | All FAULT_* constants imported from config and used in fault_sim.py |
| fault_sim.py | build_enhanced_33bus() | single network build then in_service mutation | VERIFIED | `build_enhanced_33bus()` called once in main(); also called in _select_restore_tie for fresh probe nets |
| fault_sim.py (isolated block) | pp.runpp(run_control=False) with pinned tap | D-12 OLTC pin | VERIFIED | `run_control=False` and `net.trafo.at[trafo_idx,"tap_pos"] = int(peak_tap)` in run_fault_step() for FAULT_PHASE_ISO |
| write_fault_step | config.FAULT_EVENT_BUCKET | bucket write argument | VERIFIED | `bucket=config.FAULT_EVENT_BUCKET` in write_api.write() call |
| ieee33-fault-event.json panels | fault_event bucket | Flux from(bucket) in every target query | VERIFIED | grep -c fault_event = 8 (one per panel); all 8 queries confirmed |
| ieee33-fault-event.json | shared InfluxDB datasource | datasource uid ieee33-influxdb | VERIFIED | All panel datasource blocks use uid "ieee33-influxdb" |
| default.yml directory scan | ieee33-fault-event.json | auto-provisioning (no default.yml change) | VERIFIED | default.yml scans `/etc/grafana/provisioning/dashboards`; ieee33-fault-event.json placed there; git diff default.yml = empty |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| fault_sim.py main() | 40-step snapshots | `pp.runpp()` Newton-Raphson power flow on pandapower IEEE 33-bus model with in_service flags mutated per block | Yes — converged NR solution per step; vmin/load values match planner-verified expected outputs | FLOWING |
| write_fault_step() | bus/line/sgen/system points | res_bus/res_line/res_sgen from run_fault_step() | Yes — filtered to in-service entities; zero-fills for dead entities | FLOWING |
| event measurement | n_dead_buses, phase, topology fields | dead_bus_ids list passed by fault_sim main() | Yes — 10 dead buses in ISO block, 0 in PRE/RST; confirmed in live InfluxDB | FLOWING |
| ieee33-fault-event.json panels | bus vm_pu, event n_dead_buses, system load_mw | fault_event InfluxDB bucket | Yes — 40 real NR power-flow snapshots; live queries return correct data | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| fault-sim runs and produces 40 snapshots | `uv run fault-sim` (run 1) | "fault-sim OK — 40 snapshots written to bucket 'fault_event'; chosen_tie=line_34; restored_min_vmin=0.9971 pu; shortfall_flag=False" | PASS |
| Determinism: second run overwrites identically | `uv run fault-sim` (run 2) | D-17 read-back: 40 points; D-16 and D-17 gates pass identically | PASS |
| Pre-fault block: vmin=0.9887, load=3.2922 MW, tap=-2 | Console table steps 0-12 | vmin=0.9887, load=3.2922, tap=-2 for all 13 pre_fault steps | PASS |
| ISO block: vmin=1.0000, load=2.6941 MW, n_dead=10 | Console table steps 13-19 | vmin=1.0000, load=2.6941, tap=-2 (pinned), dead=10 for all 7 ISO steps | PASS |
| Restored block: vmin=0.9971 (>= 0.95), load=3.2922 MW | Console table steps 20-39 | vmin=0.9971, load=3.2922, tap=-2, dead=0 for all 20 RST steps | PASS |
| D-05 continuity: line 7 has 40 points (energised=1 pre, energised=0 iso+rst) | InfluxDB query on line measurement, line_id=7 | 40 points; energised=['1']*13 + ['0']*27 | PASS |
| WR-02 fix: sgen 0 carries energised tag (1/0/1 per block) | InfluxDB query on sgen, sgen_id=0 | 40 points; energised=['1']*13+['0']*7+['1']*20 | PASS |
| State bucket untouched | InfluxDB query on state bucket, bus_id=17 vm_pu | 96 points | PASS |
| Grafana dashboard JSON valid | `python3 -m json.tool ieee33-fault-event.json` | Exit 0 | PASS |
| sim.py unchanged | `git diff sim.py` | Empty diff | PASS |

---

### Code-Review Fixes Verification (from 08.1-REVIEW.md)

| Finding | Fix Required | Status | Evidence |
|---------|-------------|--------|----------|
| CR-01: D-16 else-branch ValueError when iso_loads empty | Split f-string: `iso_min_str = f"{min(iso_loads):.4f}" if iso_loads else "N/A"` | VERIFIED | fault_sim.py line 531: `iso_min_str = f"{min(iso_loads):.4f}" if iso_loads else "N/A"`; the problematic `:.4f` is no longer applied to a potential str 'N/A' |
| WR-01: Dead-sgen double-write | Skip dead sgens in energised-sgen loop via `dead_sgen_set` | VERIFIED | influx.py lines 469-472: `dead_sgen_set = set(dead_sgen_ids)` + `if idx in dead_sgen_set: continue` |
| WR-02: sgen measurement lacks energised tag | Add `.tag("energised", "1"/"0")` to sgen points | VERIFIED | influx.py: energised tag on both energised-sgen loop and dead-zone zero-fill loop; live data confirms energised='1' pre/rst, '0' iso for sgen 0 |
| WR-03: networkx not declared as direct dependency | Add `networkx>=3.0` to pyproject.toml | VERIFIED | pyproject.toml line 7: `"networkx>=3.0"` |
| WR-04: _select_restore_tie swallows exceptions | Log exception before continuing | VERIFIED | fault_sim.py line 160: `print(f"  tie_idx={tie_idx}: exception during probe ({type(exc).__name__}: {exc})")` |

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| fault_sim.py lines 492-506 | Comment block describing a "design note" about tracking pre_fault served_load — ultimately resolved by a fresh probe run, but the comment chain is verbose | Info | No functional issue; code works correctly |

No blockers or warnings found. The verbose comment chain (lines 492-506 in fault_sim.py) is a stylistic note, not a stub or placeholder.

---

### Requirements Coverage

| Requirement | Source Plans | Status |
|-------------|-------------|--------|
| SPEC-1 | Plans 01, 02, 03, 04 | SATISFIED |
| SPEC-2 | Plan 02 | SATISFIED |
| SPEC-3 | Plan 02 | SATISFIED |
| SPEC-4 | Plan 02 | SATISFIED |
| SPEC-5 | Plan 02 | SATISFIED |
| SPEC-6 | Plans 01, 02 | SATISFIED |
| SPEC-7 | Plan 01 | SATISFIED |
| SPEC-8 | Plan 03 | SATISFIED (automated); HUMAN NEEDED (visual render) |
| SPEC-9 | Plan 02 | SATISFIED |
| SPEC-10 | Plan 04 | SATISFIED |
| D-01 through D-18 | Plans 01-04 | All 18 decisions satisfied (see truth table above) |

---

### Human Verification Required

#### 1. Grafana Dashboard Visual Render

**Test:** With the Docker stack running (`docker compose up -d`), open http://localhost:3000 in a browser. Confirm two dashboards are listed. Open "IEEE 33-Bus — Fault & Reconfiguration".

**Expected:**
- The time range auto-sets to 2017-06-07T17:59–18:03 UTC (the 40-step event window)
- Panel 1 (Bus Voltage Envelope): All 33 buses shown; the 10 dead-zone buses clearly collapse to 0 pu in the middle section (faulted_isolated block, steps 13-19) and recover to ~0.997 pu in the restored section
- Panel 2 (OLTC Tap): Flat line at -2 across all 40 steps
- Panel 3 (Served Load & Slack): Visible drop from ~3.29 MW to ~2.69 MW during isolation, recovery to ~3.29 MW
- Panel 4 (Phase Marker): 0→10→0 pulse shape clearly visible across the three blocks
- Panel 5 (Dead-Zone Bus Voltages 8-17): Ten lines all at 0 pu in isolation, recovering to ~0.997 pu
- Panel 6 (Dead-Bus Count): 0→10→0 timeseries
- Panel 7 (Restored Min-Voltage stat): Shows ~0.997 pu value in GREEN (>= 0.95 threshold)
- Panel 8 (Topology/Event Table): 40-row table with faulted_line_id=7, tie_closed transitions 0→0→1, n_dead_buses=0→10→0

**Why human:** Grafana panel rendering, colour threshold application (stat panel green/amber/red), visual axis scaling, and time-range auto-selection cannot be verified programmatically without a headless browser. The dashboard JSON structure and bucket data are fully verified; only the rendered visual needs confirmation.

---

### Gaps Summary

No gaps. All 12 observable truths are VERIFIED by automated checks. All 10 SPEC requirements are satisfied. All 18 decisions (D-01 through D-18) are implemented and confirmed in code and live data. All 5 code-review findings (CR-01, WR-01, WR-02, WR-03, WR-04) are fixed and verified. The purely-additive invariant holds: sim.py, ieee33-state.json, default.yml, profiles/state buckets are all unchanged.

The single human_verification item (Grafana visual render) is a display confirmation, not a functional gap — the dashboard JSON, bucket data, and auto-provisioning mechanism are all verified.

---

_Verified: 2026-06-24_
_Verifier: Claude (gsd-verifier)_
