---
phase: 08.1-system-1-fault-and-reconfiguration-scenario
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - system1-measurement-source/README.md
autonomous: true
requirements:
  - SPEC-10
user_setup: []

must_haves:
  truths:
    - "The README documents how to run the fault scenario end-to-end (uv run fault-sim) and how to open its dashboard at http://localhost:3000 (SPEC-10)."
    - "The README states the qualitative article comparison: Meteab/Tousi/Omran (2025) Section-8 fault -> isolate -> tie-restore is replicated qualitatively, with NO numerical match expected because our DGs are at buses 18/22/25/33 with a +-5% OLTC vs the paper's PSO DGs at buses 6 & 32 (SPEC-10)."
    - "Following the README from a clean state reproduces the 40-snapshot dataset and renders the dashboard (SPEC-10)."
    - "The existing 96-step-day README sections (Quick Start, the Rhythm steps 1-6, Data Model, Determinism, Security Note) are left intact — only additive content is appended (SPEC-1)."
  artifacts:
    - path: "system1-measurement-source/README.md"
      provides: "Fault-scenario runbook + qualitative article-comparison note"
      contains: "uv run fault-sim"
  key_links:
    - from: "README fault section"
      to: "uv run fault-sim runner (Plan 02)"
      via: "documented command"
      pattern: "uv run fault-sim"
    - from: "README fault section"
      to: "ieee33-fault-event.json dashboard (Plan 03)"
      via: "documented dashboard title"
      pattern: "Fault & Reconfiguration"
---

<objective>
Extend `system1-measurement-source/README.md` with a new additive section documenting the
fault/reconfiguration scenario: the one-command run (`uv run fault-sim`), where to view it (the new
Grafana dashboard), the `fault_event` bucket, and the qualitative article-comparison note. The
existing 96-step-day documentation is left fully intact.

Purpose: SPEC-10 closes the phase — the scenario is reproducible from the docs and is honestly
framed against the source paper (qualitative replication, no numerical match). This is the doc Juan
points the interview panel to.
Output: README.md with one new section; all prior sections unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-SPEC.md
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-CONTEXT.md
@.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-PATTERNS.md

<interfaces>
<!-- The exact run sequence + comparison content the README must capture. -->

VERIFIED scenario facts (from Plans 02/03 — state them in the README):
  - Command: `uv run fault-sim` (zero-arg, deterministic). Prerequisite: the `profiles` bucket must
    already be populated (`uv run ingest`) because the runner freezes the evening-peak operating
    point read from the 96-step profiles. So the run order is: docker compose up -> uv run ingest ->
    uv run fault-sim.
  - Writes 40 snapshots at 3-s spacing to the new `fault_event` InfluxDB bucket, in three blocks:
    pre_fault (13) -> faulted_isolated (7) -> restored (20).
  - Scenario shape: a 3-phase permanent fault on pandapower line idx 7 (article branch 8->9) is
    isolated (line + downstream zone, buses 8-17, out of service), then normally-open tie idx 34
    (article 12<->22) is closed to back-feed the de-energised zone; the network stays radial.
  - Verified outcome on the frozen evening-peak operating point: served load drops ~3.29 -> ~2.69 MW
    during isolation and recovers to ~3.29 MW after restoration; restored min bus voltage ~0.997 pu
    (in band, no shortfall flag needed).
  - Dashboard: Grafana at http://localhost:3000 -> "IEEE 33-Bus — Fault & Reconfiguration"
    (auto-provisioned, no manual setup). Shows the dead-zone voltage collapse to 0 and recovery.

Qualitative-comparison note (SPEC-10, PATTERNS lines 422-427 — use this wording):
  "Replicates Meteab/Tousi/Omran (2025) Section-8 fault -> isolate -> tie-restore qualitatively.
  No numerical match is expected: our DG placement (buses 18/22/25/33, +-5% OLTC) differs from the
  paper's PSO-optimised DGs at buses 6 & 32. Restoration succeeds and losses/voltages move in the
  directions the paper reports (the de-energised zone collapses on isolation and is restored to a
  near-nominal in-band operating point when the tie closes)."

ANALOG: the existing README sections (read README.md) — match the heading level (##) and the
fenced ```bash command-block style used by "The Rhythm" steps. Append the new section AFTER the
existing content (a natural spot: after "The Rhythm" / before or after "What You Should See", or as
a new top-level section near the end). Do NOT alter the existing Rhythm steps 1-6, Data Model table,
Determinism, or Security Note sections.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the fault-scenario runbook + qualitative comparison section to README.md</name>
  <read_first>
    - system1-measurement-source/README.md (THE file being modified — note the "## The Rhythm" steps, the "## Data Model" table, "## Determinism", "## Security Note" sections; match heading + command-block style)
    - .planning/phases/08.1-.../08.1-SPEC.md (SPEC-10 acceptance: run steps + qualitative note; following it reproduces the dataset + dashboard)
    - .planning/phases/08.1-.../08.1-CONTEXT.md (D-18 command, naming, the qualitative-comparison framing)
    - .planning/phases/08.1-.../08.1-PATTERNS.md (README section lines 415-427: required content)
  </read_first>
  <action>
    Append a NEW top-level section to README.md (do NOT modify any existing section). Suggested
    heading: `## Fault & Reconfiguration Scenario (Phase 8.1)`. Content, matching the existing
    README's prose + fenced-bash style:

    1. One-line summary: a separate, re-runnable quasi-steady-state fault/reconfiguration dataset on
       the same System 1 model, replicating the article's Section-8 fault -> isolate -> tie-restore
       sequence as a 40-step (2-min @ 3-s), three-block window at a frozen evening-peak operating point.

    2. "Run it" subsection with the exact ordered commands in a ```bash block:
       ```bash
       docker compose up -d     # if not already running
       uv run ingest            # one-time; populates the profiles bucket the runner freezes on
       uv run fault-sim         # writes 40 fault-event snapshots
       ```
       State that `fault-sim` is deterministic and zero-arg; re-running overwrites the same 40
       snapshots in place (identical dataset every run). Note it prints a per-step console table
       (step, phase, vmin, served load, tap, dead count).

    3. "What it does" subsection (3-5 bullets): permanent 3-phase fault on line idx 7 (article 8->9),
       isolation of the downstream zone (buses 8-17 out of service), closure of tie idx 34 (article
       12<->22) to back-feed the zone radially; the in-zone DG (bus 17) and OLTC behaviour during
       isolation; served-load drop ~3.29 -> ~2.69 MW and recovery to ~3.29 MW; restored min voltage
       ~0.997 pu.

    4. "View it" subsection: Grafana at http://localhost:3000 -> the
       "IEEE 33-Bus — Fault & Reconfiguration" dashboard (auto-provisioned alongside the 96-step-day
       dashboard, no manual setup). List the headline panels: dead-zone voltage collapse->recovery,
       OLTC tap, served-load/slack feed-in, dead-bus count, restored min-voltage stat, topology/event
       table. Mention the new `fault_event` InfluxDB bucket holds the 40-snapshot series (the
       `profiles`/`state` buckets are untouched).

    5. "Qualitative comparison to the article" subsection — paste the SPEC-10 wording from the
       interfaces block verbatim (or near-verbatim): qualitative replication only, no numerical match
       expected, our DG placement (18/22/25/33, +-5% OLTC) vs the paper's PSO DGs at 6 & 32,
       restoration succeeds and losses/voltages move in the reported directions.

    Keep the existing "## Determinism" and "## Security Note — LOCAL DEV ONLY" sections intact and
    after your section (or leave them where they are and insert your section before them) — either
    way, do NOT edit their text.
  </action>
  <verify>
    <automated>cd system1-measurement-source && grep -q 'uv run fault-sim' README.md && grep -q 'Fault & Reconfiguration' README.md && grep -q 'fault_event' README.md && grep -qi 'qualitative' README.md && grep -q 'PSO' README.md && grep -q '6 & 32' README.md && grep -q 'uv run ingest' README.md && grep -q 'uv run sim' README.md && grep -q '## The Rhythm' README.md && grep -q 'Security Note' README.md && python -c "t=open('README.md').read(); assert 'uv run fault-sim' in t and 'IEEE 33-Bus — Fault & Reconfiguration' in t and 'qualitative' in t.lower() and 'buses 6 & 32' in t.replace('  ',' '); print('README fault section OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'uv run fault-sim' README.md` returns at least 1
    - README contains the dashboard title `Fault & Reconfiguration`, the `fault_event` bucket name, and the qualitative-comparison note (grep `qualitative`, `PSO`, `6 & 32`)
    - the run-order is documented (docker compose up -> `uv run ingest` -> `uv run fault-sim`)
    - existing sections still present: `## The Rhythm`, `uv run sim`, `## Determinism` / `Security Note` (grep each)
    - existing content UNCHANGED: `git diff system1-measurement-source/README.md` shows ONLY added lines (no `-` deletions of prior section text)
    - the python assertion prints `README fault section OK`
  </acceptance_criteria>
  <done>README.md documents the fault-scenario run (ingest -> fault-sim), the new dashboard + bucket, and the qualitative article-comparison note; all prior 96-step-day sections are intact (additive only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A documentation-only Markdown edit. No code, no input parsing, no auth boundary, no network exposure, no PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-081-08 | Information Disclosure | README documents the local-dev InfluxDB token / anonymous Grafana | accept | The token and anonymous-access posture are pre-existing System 1 (Phase 8) choices already documented in the README's "Security Note — LOCAL DEV ONLY"; this section adds no new secret and reiterates local-only scope. |

No HIGH threats. A README addition introduces no new attack surface, no untrusted input, and no new network exposure.
</threat_model>

<verification>
- README contains the fault-scenario run steps (ingest -> fault-sim), the new dashboard title, the fault_event bucket, and the qualitative-comparison note.
- Following the documented steps from a clean state reproduces the 40-snapshot dataset and renders the dashboard.
- `git diff README.md` shows additions only; the 96-step-day sections are unchanged.
</verification>

<success_criteria>
- README documents how to run the fault scenario and open its dashboard (SPEC-10).
- The qualitative article-comparison note is present with the correct framing (no numerical match; DG/OLTC differences) (SPEC-10).
- All prior README sections are intact (SPEC-1, verifiable by git).
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-system-1-fault-and-reconfiguration-scenario/08.1-04-SUMMARY.md`
</output>
