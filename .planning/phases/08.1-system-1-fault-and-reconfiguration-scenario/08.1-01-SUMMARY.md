---
phase: "08.1-system-1-fault-and-reconfiguration-scenario"
plan: "01"
subsystem: "system1-measurement-source"
tags: ["fault-scenario", "influxdb", "data-contract", "additive-only"]
dependency_graph:
  requires: ["Phase 8 system1-measurement-source baseline (config.py, influx.py)"]
  provides: ["FAULT_EVENT_BUCKET constant", "write_fault_step() helper", "fault-sim entry point"]
  affects: ["Plan 02 (fault_sim.py runner)", "Plan 03 (Grafana dashboard)", "Phase 9 measurement system"]
tech_stack:
  added: []
  patterns: ["Point().tag().field().time() InfluxDB write pattern", "zero-fill for dead entities", "event measurement for topology state"]
key_files:
  created: []
  modified:
    - "system1-measurement-source/src/ieee33/config.py"
    - "system1-measurement-source/src/ieee33/influx.py"
    - "system1-measurement-source/pyproject.toml"
decisions:
  - "D-12 landmine documented inline in config.py: OLTC_REF_BUS=17 is inside FAULT_DEAD_BUS_IDX; runner must pin tap during faulted_isolated block"
  - "dead_sgen_ids added as explicit parameter to write_fault_step() rather than deriving from dead_bus_ids — keeps function self-contained and caller-driven"
metrics:
  duration: "3 min"
  completed_date: "2026-06-24"
  tasks: 3
  files: 3
---

# Phase 08.1 Plan 01: Fault-Scenario Data-Contract Layer Summary

**One-liner:** Additive data-contract layer — FAULT_EVENT_BUCKET + 14 fault constants in config.py, write_fault_step() helper mirroring write_state_step byte-identically (energised tag, zero-fill, event measurement), and fault-sim console-script entry in pyproject.toml.

## What Was Built

Three purely additive changes to the system1-measurement-source package:

### Task 1 — config.py: Fault constants + bucket name (commit d8f036d)

Added two blocks to `config.py` (additive — 20 lines, zero deletions):

1. `FAULT_EVENT_BUCKET = "fault_event"` immediately after the existing `STATE_BUCKET` line (InfluxDB bucket section).
2. A new `# Fault / reconfiguration scenario constants (Phase 8.1)` section after `TIE_LINE_IDX`:
   - `FAULT_LINE_IDX = 7` — pandapower line idx 7 = article branch 8→9 (Section-8 fault)
   - `FAULT_DEAD_BUS_IDX = list(range(8, 18))` — buses 9–18 dead zone (10 buses)
   - `FAULT_TIE_IDX = 34` — pandapower line idx 34, article tie 12↔22
   - `FAULT_N_STEPS = 40`, `FAULT_STEP_S = 3`, block split `FAULT_PRE_STEPS=13 / FAULT_ISO_STEPS=7 / FAULT_RST_STEPS=20` (sums to 40)
   - `FAULT_EVENING_PEAK_STEP = 72` — anchor step for the frozen operating point
   - `FAULT_PHASE_PRE = "pre_fault"`, `FAULT_PHASE_ISO = "faulted_isolated"`, `FAULT_PHASE_RST = "restored"`
   - D-12 landmine comment: OLTC_REF_BUS=17 is inside the dead zone; runner must pin tap during isolation.

### Task 2 — influx.py: write_fault_step() (commit 1e021d8)

Added `write_fault_step()` as a new function (183 lines, zero deletions, `write_state_step` body untouched):

Signature:
```python
def write_fault_step(
    write_api, timestamp,
    res_bus, res_line, res_sgen, res_ext_grid, res_trafo,
    tap_pos, shift_degree, system_dict,
    phase_label, dead_bus_ids, dead_line_ids, dead_sgen_ids, tie_is_closed,
) -> None:
```

Schema extension over write_state_step:
- bus/line points gain `.tag("energised", "1")` for live entities
- Dead buses (dead_bus_ids) zero-filled: vm_pu=0.0, va_degree=0.0, energised="0"
- Dead lines (dead_line_ids) zero-filled: all 7 fields = 0.0, energised="0"
- Dead sgens (dead_sgen_ids) zero-filled: p_mw=0.0, q_mvar=0.0
- system measurement: byte-identical copy of write_state_step's 9-field system point
- NEW event measurement: `.tag("phase", phase_label)` + fields faulted_line_id, tie_closed, tie_id (int, -1 when open), n_dead_buses, dead_buses (comma-joined string)
- Writes to `config.FAULT_EVENT_BUCKET` (not STATE_BUCKET)

### Task 3 — pyproject.toml: fault-sim entry (commit b8a4ba9)

Added one line to `[project.scripts]`:
```toml
fault-sim = "ieee33.fault_sim:main"
```
Existing `ingest`/`sim`/`validate` entries unchanged.

## Verification Results

All automated checks passed:

```
config OK
write_fault_step OK; write_state_step intact
Clean import OK
Block split 13+7+20=40 OK
FAULT_DEAD_BUS_IDX==range(8,18) OK
```

`git diff HEAD~3 -- config.py influx.py pyproject.toml | grep '^-' | grep -v '^---'` returned empty — zero deleted lines across all three files.

InfluxDB not required for these checks (no write executed; config import + signature checks run offline).

## Deviations from Plan

None — plan executed exactly as written.

The `dead_sgen_ids` parameter was already part of the plan's specified final signature (the task description self-corrected mid-action). No additional design choices were needed.

## Known Stubs

None. This plan adds a data-contract layer (constants + helper signature). No UI rendering, no data wiring, no placeholder text.

## Self-Check: PASSED

All 3 files found on disk. All 3 task commits (d8f036d, 1e021d8, b8a4ba9) confirmed in git log.

## Threat Flags

No new threat surface introduced. All additions are static constants and a local write helper. No new network endpoints, no new auth paths, no new file access patterns. T-081-01 and T-081-02 from the plan's threat model remain the only applicable items (local InfluxDB dev token reuse accepted; static constants accepted).
