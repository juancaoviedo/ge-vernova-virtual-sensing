---
status: passed
phase: 08.1-system-1-fault-and-reconfiguration-scenario
source: [08.1-VERIFICATION.md]
started: 2026-06-24T00:00:00Z
updated: 2026-06-25T00:00:00Z
---

## Current Test

[complete — all tests passed]

## Tests

### 1. Grafana renders the fault & reconfiguration dashboard
expected: At http://localhost:3000, the dashboard "IEEE 33-Bus — Fault & Reconfiguration" appears in the dashboard list (alongside the original 96-step-day dashboard) and renders all panels over the `fault_event` bucket with no manual setup — the bus-voltage envelope shows the dead zone collapsing to 0 during the faulted-isolated block and recovering after restoration; the dead-bus count panel steps 0 → 10 → 0; the restored min-voltage stat shows green (~0.997 pu); the topology/event table lists the faulted line, tie state, and phase per snapshot.
result: pass — user confirmed the dashboard renders correctly. Refined during review (commit `feat(08.1): dashboard polish + per-bus power from UAT review`): removed the redundant Dead-Bus Count panel (duplicated the phase/dead-bus pulse), converted both bus-voltage panels from area to line, and added a "Bus Active Power (MW)" panel backed by new per-bus `p_mw`/`q_mvar` fields showing the per-bus load shed → restore.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
