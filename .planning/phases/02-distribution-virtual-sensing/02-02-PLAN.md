---
phase: 02-distribution-virtual-sensing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-distribution-virtual-sensing/notes/TVS-03-observability-bad-data.md
  - .planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md
autonomous: true
requirements: [TVS-03, TVS-04]

must_haves:
  truths:
    - "Juan can explain the chi-squared bad-data test, normalized residuals, leverage measurements as the critical limitation, and Jacobian rank as the observability check"
    - "Juan can describe transformer hot-spot estimation (IEEE C57.91 ODE), DGA indicator gases, Dynamic Line Rating as a virtual-sensing product, and RUL framing"
    - "TVS-03's bridge box uses the 'billions of data points across multiple substations' baseline-error analog (the strongest bridge in the phase)"
    - "Each note ends with a boxed '→ Bridge to your work' pivot and carries a '<3-min say-aloud' track"
    - "Honors CONTEXT.md decisions D-04 (per-note '→ Bridge to your work' box), D-05 (no aggregate bridge table — deferred to Phase 6), D-06 ('<3-min say-aloud' track per note), D-07 (full mock rehearsal / Q&A deferred to Phase 6), D-08 (deep-where-named / aware-elsewhere depth), D-09 (one note file per requirement), D-10 (LaTeX-in-markdown oral-rehearsal note style)"
  artifacts:
    - path: ".planning/phases/02-distribution-virtual-sensing/notes/TVS-03-observability-bad-data.md"
      provides: "Deep observability/bad-data note: H-rank observability, chi-squared detection, normalized-residual identification, leverage-measurement blind spot, substation-scale bridge"
      contains: "\\chi^2"
    - path: ".planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md"
      provides: "Asset-health note: IEEE C57.91 hot-spot ODE (deep, term tables), Arrhenius aging, DGA/DLR/RUL (awareness), virtual-sensing parallel to Phase 1 IEEE 738, OSED bridge"
      contains: "C57.91"
  key_links:
    - from: "TVS-03-observability-bad-data.md"
      to: "billions of data points across multiple substations (CV)"
      via: "boxed bridge pivot — bad-data detection at substation scale"
      pattern: "substation"
    - from: "TVS-04-asset-health.md"
      to: "Phase 1 IEEE 738 conductor EKF (DLR)"
      via: "explicit virtual-sensing parallel and DLR product framing"
      pattern: "738|DLR"
---

<objective>
Produce the remaining two Phase 2 study-notes files: TVS-03 (observability analysis + bad-data
detection — H-rank observability, chi-squared test, normalized residuals, leverage measurements)
and TVS-04 (asset-health estimation — IEEE C57.91 transformer hot-spot ODE, DGA, Dynamic Line
Rating, RUL). These cover Phase 2 success criteria 3 and 4.

Purpose: TVS-03 gives Juan the observability/bad-data vocabulary plus the single strongest bridge
in the phase (his "billions of data points across multiple substations" baseline-error analysis
IS bad-data detection at scale). TVS-04 covers the criteria-named C57.91 hot-spot ODE deeply and
keeps DGA/DLR/RUL at awareness per the locked depth strategy, with each item connected to an OSED
analog.

Output: Two markdown notes under notes/ Juan can study and deliver aloud, each ending with a
boxed OSED/HEMS/SI-MAPPER bridge pivot and carrying a "<3-min say-aloud" track.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-distribution-virtual-sensing/02-RESEARCH.md
@.planning/phases/02-distribution-virtual-sensing/02-PATTERNS.md

This is an INTERVIEW-PREP project. "Executing" means writing equation-dense study material
Juan reads and rehearses aloud — NOT building software. Draw all equations verbatim from
02-RESEARCH.md (do not improvise). Mirror Phase 1's note STRUCTURE (KAL-01 header/sections,
KAL-03 worked-example term tables for the ODE) per 02-PATTERNS.md SP-1..SP-6. Use
LaTeX-in-markdown (D-10). Depth (D-08): FULL for χ²/normalized residuals/Jacobian-rank
observability and the IEEE C57.91 hot-spot ODE; AWARENESS (crisp, no derivation) for the
leverage-measurement intuition, DGA gases, DLR productization, and RUL framing.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write TVS-03 observability + bad-data note (χ², normalized residuals, leverage, rank)</name>
  <files>.planning/phases/02-distribution-virtual-sensing/notes/TVS-03-observability-bad-data.md</files>
  <read_first>
    - .planning/phases/02-distribution-virtual-sensing/02-RESEARCH.md section "TVS-03 — Observability + Bad-Data Detection" (Jacobian-rank observability, χ² detection rule, residual covariance Ω = R − H G⁻¹ Hᵀ, normalized residual rᵢᴺ = |rᵢ|/√Ωᵢᵢ threshold ≈ 3, leverage-measurement blind spot, interacting/conforming bad data, OSED substation-scale bridge) and "Common Pitfalls" Pitfall 3 (χ² detects vs rN identifies)
    - .planning/phases/02-distribution-virtual-sensing/02-PATTERNS.md SP-1..SP-6 and the "notes/TVS-03-observability-bad-data.md" pattern-assignment block (this note IS KAL-01 §4 specialized; χ²/LNR/leverage content to reuse; section skeleton; the division-of-labor pitfall to encode)
    - .planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md §4 (chi-squared / LNR / leverage-measurement content this note specializes to the linear DC case; the leverage interview sentence to lift; style template)
  </read_first>
  <action>
    Create notes/TVS-03-observability-bad-data.md mirroring KAL-01 (SP-1..SP-6), LaTeX-in-markdown,
    content verbatim from RESEARCH §TVS-03. Header: `# TVS-03: Observability & Bad-Data Detection`,
    `**For:** …`, `**Purpose:** …`, `---`. Open with a pointer that this specializes KAL-01 §4 to the
    linear DC case. Numbered sections:

    (1) **Observability via measurement Jacobian rank** — DEEP. The system is numerically observable
        iff the gain matrix $G=H^\top W H$ is nonsingular, equivalently $H$ has full column rank
        ($\mathrm{rank}(H)=n$). If $\mathrm{rank}(H)<n$ there are unobservable states/islands. Show the
        practical check `numpy.linalg.matrix_rank(H)` (inline code). Name topological vs numerical
        observability; the rank test is the operational one.
    (2) **Chi-squared detection — "is there bad data?"** — DEEP. After WLS,
        $$J(\hat x) = r^\top W r, \qquad r = z - H\hat x$$
        follows $\chi^2$ with $m-n$ degrees of freedom. Detection rule: if
        $J(\hat x) > \chi^2_{(m-n,\,1-\alpha)}$ (e.g. $\alpha=0.05$) at least one measurement is bad.
        χ² tells you THAT bad data exists, not WHICH.
    (3) **Largest-normalized-residual (rN) identification — "which one?"** — DEEP. Residual covariance
        $$\Omega = R - H\,(H^\top W H)^{-1} H^\top = R - H\,G^{-1}H^\top$$
        Normalized residual $$r_i^N = \frac{|r_i|}{\sqrt{\Omega_{ii}}}$$
        Rule: the measurement with the largest $r_i^N$ above a threshold ≈ 3 (3σ) is the prime
        suspect; remove, re-run WLS, re-test. Encode the Pitfall-3 division of labor explicitly:
        χ² answers "is there bad data?", rN answers "which one?"
    (4) **The critical limitation — leverage measurements** — AWARENESS (but interview gold). Leverage
        measurements sit at buses whose observability depends entirely on them; their residual is
        structurally near-zero, so a gross error is invisible to the rN test (wrong-but-consistent
        estimate). Note that multiple interacting+conforming bad data can also defeat single-rN. Lift
        the KAL-01 leverage interview sentence (reworded in RESEARCH §TVS-03).

    Then add (discretionary placement, D-06): a `## <3-min say-aloud version` `> blockquote` hitting
    rank-observability → χ²-detect → rN-identify → leverage-blind-spot; a `## → Bridge to your work`
    box (SP-4 shape) using the STRONGEST bridge in the phase — the CV's "Analyzed errors in baseline
    consumption estimation by processing billions of data points across multiple substations" = bad-
    data detection at substation scale. Pivot: "Bad-data detection is exactly what I did analyzing
    baseline-estimation errors across billions of points from multiple substations — finding the
    corrupted or anomalous measurements that throw off the estimate, which is the chi-squared /
    normalized-residual problem in production." Add a `## Quick-Recall Card` ending with `**My bridge:**`,
    and a closing `*Sources: …*` line (Abur & Expósito Ch.5/6, KAL-01, CV).

    Cover all four sections fully. Keep leverage at awareness depth (no full derivation). Do NOT add
    an aggregate bridge table (D-05).
  </action>
  <verify>
    <automated>F=.planning/phases/02-distribution-virtual-sensing/notes/TVS-03-observability-bad-data.md; test -f "$F" && grep -q '\\chi^2' "$F" && grep -qi 'leverage' "$F" && grep -qiE 'normalized residual|r_i\^N|rN' "$F" && grep -qiE 'rank' "$F" && grep -qi 'substation' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains the LaTeX `\chi^2`; contains "leverage"; contains a normalized-residual
    reference; contains "rank" (observability); contains "substation" (the strongest bridge);
    contains "Bridge to your work"; contains a "3-min"/"say-aloud" track. Command prints OK.
  </acceptance_criteria>
  <done>TVS-03 note exists with H-rank observability (matrix_rank check), χ² detection, normalized-residual identification (Ω = R − H G⁻¹ Hᵀ), the χ²-detects/rN-identifies division of labor, the leverage-measurement blind spot (awareness), the substation-scale bridge pivot, a "<3-min say-aloud" track, a Quick-Recall Card, and a Sources line.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write TVS-04 asset-health note (C57.91 hot-spot ODE deep; DGA/DLR/RUL aware)</name>
  <files>.planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md</files>
  <read_first>
    - .planning/phases/02-distribution-virtual-sensing/02-RESEARCH.md section "TVS-04 — Asset-Health Estimation" (top-oil + hot-spot rise ODEs with exponents m,n and time constants; θ_HS = θ_ambient + Δθ_TO + Δθ_HS; Arrhenius aging A=9.8e-18, B=15000; "why this is virtual sensing" Phase-1 IEEE 738 parallel; DGA key gases; DLR product framing; RUL framing; OSED bridge), "Common Pitfalls" Pitfall 4 (verify C57.91 constant placement), "Open Questions" #1, and "Assumptions Log" A1
    - .planning/phases/02-distribution-virtual-sensing/02-PATTERNS.md SP-1..SP-6 and the "notes/TVS-04-asset-health.md" pattern-assignment block (KAL-03 worked-example term-table layout for the ODE; section skeleton; depth split)
    - .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md (the worked-example depth bar: state the ODE in $$…$$, then a term-by-term table and a parameter table — mirror this for the C57.91 ODEs)
    - .planning/phases/01-kalman-state-estimation/demo/ekf_line_temp_demo.py docstring (the IEEE 738 / DLR work Juan already built, to reference for the DLR section instead of re-deriving)
  </read_first>
  <action>
    FIRST — do the flagged confirm pass (RESEARCH Open Question 1 / Pitfall 4 / Assumption A1): spend
    ~5 minutes confirming the exact exponent/time-constant placement of the IEEE C57.91-2011 top-oil
    and hot-spot rise equations (Clause 7 exponential vs Annex G detailed model) against a reliable
    C57.91 summary. The two-cascaded-first-order-rise STRUCTURE is already verified; only the precise
    constant/exponent placement needs confirming. If the exact placement cannot be confirmed quickly,
    present the structure confidently and tag the display constants "per IEEE C57.91-2011" rather than
    asserting a possibly-wrong placement. Note the chosen rendering in the SUMMARY.

    Then create notes/TVS-04-asset-health.md as a single well-sectioned file (D-09), mirroring KAL-01
    header/sections and KAL-03 worked-example term tables, LaTeX-in-markdown, content from RESEARCH
    §TVS-04. Header: `# TVS-04: Asset-Health Estimation`, `**For:** …`, `**Purpose:** …`, `---`.
    Numbered sections:

    (1) **IEEE C57.91 transformer thermal model** — DEEP (worked-example depth, KAL-03 style: state
        the ODE then a term-by-term table). Two coupled first-order ODEs, ambient → top-oil → hot-spot:
        $$\tau_{TO}\,\frac{d\,\Delta\theta_{TO}}{dt} = \Delta\theta_{TO,\,U}\left(\frac{K^2 R + 1}{R+1}\right)^{n} - \Delta\theta_{TO}$$
        $$\tau_{w}\,\frac{d\,\Delta\theta_{HS}}{dt} = \Delta\theta_{HS,\,U}\,K^{2m} - \Delta\theta_{HS}$$
        $$\theta_{HS} = \theta_{ambient} + \Delta\theta_{TO} + \Delta\theta_{HS}$$
        where $K=I/I_{rated}$, $R$ = load-loss/no-load-loss ratio, $\tau_{TO}$ (hours) $\gg \tau_w$
        (minutes), $n$ = oil exponent, $m$ = winding exponent. Add a term/parameter table à la KAL-03.
        Tag the exact constants "per IEEE C57.91-2011" per the confirm pass.
    (2) **Aging / loss-of-life (Arrhenius)** — DEEP. Insulation life consumed exponentially with
        hot-spot temperature: $$\text{per-unit life} = A\,\exp\!\left(\frac{B}{\theta_{HS}+273}\right), \quad A=9.8\times10^{-18},\ B=15000$$
        State: the hot-spot temperature, not the load, determines insulation aging (equal-life loading).
    (3) **"Why this is virtual sensing"** — DEEP framing. You rarely measure the winding hot-spot
        directly (buried in the windings); you infer it from load current + ambient via this ODE —
        identical in spirit to the IEEE 738 conductor-temperature EKF Juan ALREADY BUILT in Phase 1
        (KAL-03/KAL-04). State this parallel explicitly and reference the Phase-1 demo.
    (4) **DGA — Dissolved Gas Analysis** — AWARENESS (no derivation). Key gases and associations:
        H₂ (partial discharge), CH₄/C₂H₆ (low-temp thermal), C₂H₄ (high-temp/overheating), C₂H₂
        (arcing — most severe), CO/CO₂ (cellulose/paper degradation). Name interpretation methods only:
        Duval Triangle, Rogers Ratios, Key Gas method, IEC 60599. One line: "DGA is a virtual sensor
        for internal fault type."
    (5) **Dynamic Line Rating (DLR)** — AWARENESS, product framing. Real-time ampacity from actual
        weather + IEEE 738 thermal balance vs a fixed conservative static rating. Juan ALREADY built
        this in the Phase 1 demo (`ieee738_ampacity`, `ekf_line_temp_demo.py`) — frame as a virtual-
        sensing PRODUCT ("infer unmeasured conductor temperature → unlock 10–30% more headroom").
        Reference Phase 1 rather than re-deriving IEEE 738.
    (6) **Remaining Useful Life (RUL)** — AWARENESS, framing only. Projecting cumulative damage (e.g.
        Arrhenius loss-of-life integral) forward to time-to-end-of-life; the output that turns a
        virtual condition estimate into a maintenance/replacement decision. Keep conceptual, no model.

    Then add (discretionary placement, D-06): a `## <3-min say-aloud version` `> blockquote`; a
    `## → Bridge to your work` box (SP-4 shape) — infer an unmeasured physical quantity (winding temp)
    from proxy telemetry to drive a decision = OSED building thermal estimator. Pivot: "Transformer
    hot-spot and DLR are the same virtual-sensing pattern as my building thermal estimator — you can't
    measure the quantity that matters directly, so you infer it from a physics model plus available
    telemetry and act on the estimate." Add a `## Quick-Recall Card` ending with `**My bridge:**`, and a
    closing `*Sources: …*` line (IEEE C57.91-2011, IEEE 738/Phase 1, CV; mention the 2025–2026 C57.91
    revision in progress to show currency awareness).

    Cover all six sections. Keep DGA/DLR/RUL strictly at awareness — do NOT over-derive them (D-08).
    Do NOT add an aggregate bridge table (D-05).
  </action>
  <verify>
    <automated>F=.planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md; test -f "$F" && grep -q 'C57.91' "$F" && grep -qE 'theta_{HS}|\\theta_{HS}' "$F" && grep -qE 'A=9.8|9.8\\times10' "$F" && grep -qiE '738|DLR' "$F" && grep -qi 'DGA' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "C57.91"; contains the hot-spot temperature symbol `\theta_{HS}`; contains
    the Arrhenius constant `A=9.8` (aging law); references "738"/"DLR" (the Phase-1 virtual-sensing
    parallel); contains "DGA"; contains "Bridge to your work"; contains a "3-min"/"say-aloud" track.
    Command prints OK.
  </acceptance_criteria>
  <done>TVS-04 note exists with the confirm-pass-checked C57.91 top-oil + hot-spot ODEs (term tables), Arrhenius aging (A=9.8e-18, B=15000), the explicit virtual-sensing parallel to the Phase-1 IEEE 738 EKF, DGA/DLR/RUL at awareness depth, the OSED bridge pivot, a "<3-min say-aloud" track, a Quick-Recall Card, and a Sources line.</done>
</task>

</tasks>

<threat_model>
No attack surface — this plan produces study notes (plain markdown Juan reads aloud). There is
no software service, no network surface, no untrusted input, no auth, and no data persistence.
STRIDE threat modeling does not apply to static study documents.
</threat_model>

<verification>
- Both note files exist under notes/ with the required greppable LaTeX tokens and headings.
- TVS-03 includes χ² detection, normalized residuals, H-rank observability, the leverage blind spot, and the substation-scale bridge box.
- TVS-04 includes the C57.91 hot-spot ODE (confirm-pass-checked), Arrhenius aging, the Phase-1 IEEE 738 virtual-sensing parallel, DGA/DLR/RUL at awareness, and the OSED bridge box.
- Each note carries a "<3-min say-aloud" track and a "→ Bridge to your work" box (D-04, D-06).
</verification>

<success_criteria>
Juan can read each note aloud and, without the notes, (3) explain the chi-squared test, normalized
residuals, leverage measurements, and Jacobian-rank observability, and (4) describe transformer
hot-spot estimation (C57.91), DGA gases, DLR as a virtual-sensing product, and RUL framing — each
connected to an OSED analog.
</success_criteria>

<output>
After completion, create `.planning/phases/02-distribution-virtual-sensing/02-02-SUMMARY.md`
</output>
