---
phase: 07-integrated-html-study-site
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - docs/build_site.py
  - docs/demos/
  - docs/index.html
autonomous: true
requirements: [HTML-01, HTML-05]
user_setup: []

must_haves:
  truths:
    - "HTML-01: docs/index.html is a card-grid hub in three groups (Architecture / Study Notes / Demos) linking to every content page (D-09)"
    - "HTML-05: docs/demos/index.html covers all 3 demos, each with a plain-language explanation, an embedded key-function code snippet (pygments-highlighted at build time), a result artifact (inline PNG figure for demos 1-2, numeric table for demo 3), and a working link to full source (D-10, D-11, D-12)"
    - "D-09: The hub uses .card-grid/.hub-card; each content page keeps its own sticky TOC (notes/demos pages already do)"
    - "D-11: Only KEY FUNCTIONS are embedded (not whole files), highlighted with pygments offline; full .py linked"
  artifacts:
    - path: "docs/index.html"
      provides: "Card-grid hub linking architecture (3 pages + diagram), 16 notes, demos"
      contains: "card-grid"
    - path: "docs/demos/index.html"
      provides: "Three demo sections with explanation + embedded code + result + source link"
      contains: "View full source"
    - path: "docs/demos/ekf_line_temp.png"
      provides: "Copied result image for demo 1"
    - path: "docs/demos/ekf_line_temp_demo.py"
      provides: "Copied full source for demo 1 (link target)"
  key_links:
    - from: "docs/index.html"
      to: "docs/notes/*.html and docs/architecture/*.html and docs/demos/index.html"
      via: "relative hub-card hrefs generated from .build_manifest.json"
      pattern: "href=\"notes/"
    - from: "docs/demos/index.html"
      to: "docs/demos/ekf_line_temp_demo.py"
      via: "View full source link"
      pattern: "href=\"ekf_line_temp_demo.py\""
    - from: "docs/demos/index.html"
      to: "docs/demos/ekf_line_temp.png"
      via: "inline <figure><img>"
      pattern: "<img[^>]*ekf_line_temp.png"
---

<objective>
Build the two human-facing entry surfaces: the card-grid hub (HTML-01) that ties Architecture + Study Notes + Demos together, and the demos page (HTML-05) that explains each of the three hands-on demos with an embedded key-function snippet, an inline result, and a link to full source.

Purpose: HTML-01 + HTML-05. The hub is the "easier to study, one entry point" north star (D-09); the demos page makes the code+results readable without leaving the site (D-10/D-11/D-12).
Output: docs/index.html, docs/demos/index.html, and the 3 demo .py + 2 result PNGs copied into docs/demos/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integrated-html-study-site/07-SPEC.md
@.planning/phases/07-integrated-html-study-site/07-CONTEXT.md
@.planning/phases/07-integrated-html-study-site/07-RESEARCH.md
@.planning/phases/07-integrated-html-study-site/07-PATTERNS.md
@.planning/phases/07-integrated-html-study-site/07-UI-SPEC.md
@.planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md
@.planning/phases/07-integrated-html-study-site/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the demos page with sliced key functions, pygments highlight, inline results, source links</name>
  <files>docs/build_site.py, docs/demos/</files>
  <read_first>
    - .planning/phases/07-integrated-html-study-site/07-PATTERNS.md (demos section lines 176-201: per-demo block structure, key-function table with line numbers, slicing approach, pygments highlight call)
    - .planning/phases/07-integrated-html-study-site/07-RESEARCH.md (Pattern 2 function-name slicing; D-10/D-11/D-12)
    - .planning/phases/01-kalman-state-estimation/demo/README.md (narrative source: "What It Demonstrates", "Interview Talking Points", "Key Implementation Details")
    - .planning/phases/02-distribution-virtual-sensing/demo/README.md (narrative source)
    - .planning/phases/05-federated-architectures-security/demo/README.md (narrative source; this demo has NO png — render numeric output as a table/code block)
    - .planning/phases/01-kalman-state-estimation/demo/ekf_line_temp_demo.py (slice ekf_step @120)
    - .planning/phases/02-distribution-virtual-sensing/demo/dc_powerflow_baddata_demo.py (slice wls_solve @77, chi2_test @86)
    - .planning/phases/05-federated-architectures-security/demo/fedavg_fedprox_krum_demo.py (slice fedavg_aggregate @86, krum_select @92)
  </read_first>
  <action>
    Add a `build_demos()` step to docs/build_site.py (extend the same file). Steps:

    1. Copy the demo source + result assets into docs/demos/ (so links resolve offline — D-11):
       - ekf_line_temp_demo.py, ekf_line_temp.png  (from phase 01 demo/)
       - dc_powerflow_baddata_demo.py, dc_powerflow_baddata.png  (from phase 02 demo/)
       - fedavg_fedprox_krum_demo.py  (from phase 05 demo/ — no PNG)
    2. Implement a function slicer: given a .py path and a top-level function name, return its source. Use `ast` to find the FunctionDef node and slice source lines [node.lineno-1 : node.end_lineno], OR regex from `^def <name>` to the next `^def `/`^class `/`^if __name__`. The verified key functions (NOT whole files — D-11):
       - ekf_line_temp_demo.py: `ekf_step` (line 120) — the EKF predict/update step.
       - dc_powerflow_baddata_demo.py: `wls_solve` (line 77) AND `chi2_test` (line 86).
       - fedavg_fedprox_krum_demo.py: `fedavg_aggregate` (line 86) AND `krum_select` (line 92).
    3. Highlight each slice at build time with pygments (offline, no runtime JS — D-11):
       ```python
       from pygments import highlight
       from pygments.lexers import PythonLexer
       from pygments.formatters import HtmlFormatter
       snippet_html = highlight(func_source, PythonLexer(), HtmlFormatter(cssclass="highlight"))
       ```
       (cssclass="highlight" matches docs/assets/pygments.css from plan 01.)
    4. Emit docs/demos/index.html using the shared shell helper (masthead + .wrap + sticky nav.toc with one TOC entry per demo + back-to-hub footer + the standard noindex <head> with relative ../assets paths). Per-demo block (D-12 narrative sourced from each README — paraphrase the README's "what/why-gap/what-it-shows" into plain prose, do NOT re-author facts):
       For each demo, a `<section class="card">` containing, in order:
       - `<h2 class="sec" id="...">` demo title.
       - "What it is" / "Why it was built (interview gap it closes)" / "What it demonstrates" prose paragraphs from the README.
       - Demos 1 & 2: an inline `<figure><img src="ekf_line_temp.png">...<figcaption>...</figcaption></figure>` (D-10) result image with a short caption. Demo 3 (FED): a `<table>` or `<pre class="diagram">` rendering its key numeric output (FedAvg vs FedProx vs Krum vs coord-median error contrast under poisoning) — paraphrase from the README's reported numbers (D-10).
       - the pygments-highlighted key-function snippet(s).
       - a "View full source &rarr;" link to the copied .py (e.g. `<a href="ekf_line_temp_demo.py">`).
    5. Re-run the build.
  </action>
  <acceptance_criteria>
    - `test -f docs/demos/index.html` succeeds
    - `test -f docs/demos/ekf_line_temp_demo.py && test -f docs/demos/ekf_line_temp.png && test -f docs/demos/dc_powerflow_baddata_demo.py && test -f docs/demos/dc_powerflow_baddata.png && test -f docs/demos/fedavg_fedprox_krum_demo.py` succeeds
    - `grep -c 'class="highlight"' docs/demos/index.html` >= 3 (at least one highlighted snippet per demo; demos 2/3 have two each)
    - `grep -cE '<img[^>]*src="ekf_line_temp.png"' docs/demos/index.html` >= 1 AND `grep -cE '<img[^>]*src="dc_powerflow_baddata.png"' docs/demos/index.html` >= 1
    - `grep -c 'href="ekf_line_temp_demo.py"' docs/demos/index.html` >= 1 AND `grep -c 'href="dc_powerflow_baddata_demo.py"' docs/demos/index.html` >= 1 AND `grep -c 'href="fedavg_fedprox_krum_demo.py"' docs/demos/index.html` >= 1
    - `grep -c 'def ekf_step' docs/demos/index.html` >= 1 (the key function was sliced+embedded) AND `grep -c 'def run_demo' docs/demos/index.html` == 0 (whole-file NOT embedded — D-11 key-functions-only)
    - `grep -c 'noindex' docs/demos/index.html` >= 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/juan/codes/ge-vernova-virtual-sensing && .venv-site/bin/python docs/build_site.py && test -f docs/demos/index.html && [ "$(grep -c 'class="highlight"' docs/demos/index.html)" -ge 3 ] && grep -qE '<img[^>]*src="ekf_line_temp.png"' docs/demos/index.html && grep -q 'def ekf_step' docs/demos/index.html && [ "$(grep -c 'def run_demo' docs/demos/index.html)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>docs/demos/index.html presents all 3 demos with README-sourced explanation, inline result (PNG figure ×2, numeric table ×1), pygments-highlighted KEY functions only, and working full-source links; demo assets copied into docs/demos/.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the card-grid hub index.html from the build manifest</name>
  <files>docs/build_site.py, docs/index.html</files>
  <read_first>
    - .planning/phases/07-integrated-html-study-site/07-PATTERNS.md (hub section lines 159-171: masthead copy, card-grid structure, verb-led links; .card-grid/.hub-card rules already in site.css)
    - .planning/phases/07-integrated-html-study-site/07-UI-SPEC.md (Copywriting Contract: masthead h1 + .meta subtitle, verb-led card links, "Start here" optional cue; empty-group rule)
    - .planning/phases/07-integrated-html-study-site/07-RESEARCH.md (Pitfall 5 hub links to non-existent pages — generate from the SAME manifest)
    - .planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md and 07-02-SUMMARY.md (manifest format docs/.build_manifest.json; architecture page filenames + diagram.html)
  </read_first>
  <action>
    Add a `build_hub()` step to docs/build_site.py that emits docs/index.html. CRITICAL (Pitfall 5): generate every note card href FROM docs/.build_manifest.json (the slug->path map written in plan 01), so the hub never points at a slug the converter named differently — single source of truth.

    Hub document (root level — relative paths have NO `../` prefix; assets are `assets/site.css`, `assets/pygments.css`):
    - `<head>`: standard contract with `<meta name="robots" content="noindex,nofollow">`, `<link rel="stylesheet" href="assets/site.css">`, `<link rel="stylesheet" href="assets/pygments.css">`. No MathJax needed on the hub.
    - `<body>` (.wrap shell; TOC optional on the hub per PATTERNS — a flat masthead is fine, or a minimal nav.toc with the 3 group anchors):
      ```html
      <header class="masthead">
        <h1>GE Vernova Virtual Sensing &mdash; Interview Study Site</h1>
        <div class="meta">Architecture, study notes, and hands-on demos in one place.</div>
      </header>
      ```
    - Three grouped sections, each a `<section class="card">` with an `<h2 class="sec">` group title and a `<div class="card-grid">` of `<a class="hub-card">` cards (verb-led link text + a short description per UI-SPEC):
      1. **Architecture** — cards to architecture/AGMS-architecture.html ("Read the AGMS walkthrough"), architecture/grid-operations-and-role.html ("Read grid operations & role"), architecture/INDEX.html ("Browse the patent index"), architecture/diagram.html ("View the architecture diagram").
      2. **Study Notes** — one card per entry in the manifest (16), grouped/labelled by phase prefix (KAL / TVS / AGMS deck / STK / FED) using each note's masthead title as the card title and `href="notes/<slug>.html"`. Iterate the manifest in stable order; do NOT hardcode the 16 filenames — read them from the manifest.
      3. **Demos** — one card to demos/index.html ("See the hands-on demos"), or three cards anchoring into the demo sections.
    - `<footer>` with the site tagline.
    Re-run the build. (Optionally use the --start-bg/--start-line blue once on a single "Start here" cue — discretion; default leave green-only.)
  </action>
  <acceptance_criteria>
    - `test -f docs/index.html` succeeds
    - `grep -c 'class="card-grid"' docs/index.html` >= 3 (three groups — D-09)
    - The note-card count equals the build manifest note count AND is >= 16 (self-adjusting against the manifest, with an absolute floor). docs/.build_manifest.json is a flat `{slug: relpath}` map (plan 01); count its note entries with `jq '[to_entries[]|select(.value|startswith("notes/"))]|length'`:
      `[ "$(grep -oE 'href="notes/[a-z0-9-]+\.html"' docs/index.html | sort -u | wc -l)" -eq "$(jq '[to_entries[]|select(.value|startswith("notes/"))]|length' docs/.build_manifest.json)" ]` AND `[ "$(grep -oE 'href="notes/[a-z0-9-]+\.html"' docs/index.html | sort -u | wc -l)" -ge 16 ]` (all note cards generated from the manifest; floor catches a silently shrinking manifest)
    - Cross-page link authority: plan 04 `validate_links()` is the authoritative offline-navigation / cross-page link gate; the per-task on-disk href check below resolves hub links against docs/ root and is sufficient only because hub links are one level deep
    - `grep -c 'href="architecture/AGMS-architecture.html"' docs/index.html` >= 1 AND `grep -c 'href="architecture/diagram.html"' docs/index.html` >= 1 AND `grep -c 'href="architecture/INDEX.html"' docs/index.html` >= 1 AND `grep -c 'href="architecture/grid-operations-and-role.html"' docs/index.html` >= 1
    - `grep -c 'href="demos/index.html"' docs/index.html` >= 1
    - `grep -c 'noindex' docs/index.html` >= 1
    - Every hub href resolves on disk: run `for h in $(grep -oE 'href="[^"#:]+"' docs/index.html | sed 's/href="//;s/"//' | grep -v '^http'); do test -f "docs/$h" || echo "MISSING $h"; done` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>cd /home/juan/codes/ge-vernova-virtual-sensing && .venv-site/bin/python docs/build_site.py && test -f docs/index.html && [ "$(grep -c 'class="card-grid"' docs/index.html)" -ge 3 ] && [ "$(grep -oE 'href="notes/[a-z0-9-]+\.html"' docs/index.html | sort -u | wc -l)" -eq "$(jq '[to_entries[]|select(.value|startswith("notes/"))]|length' docs/.build_manifest.json)" ] && [ "$(grep -oE 'href="notes/[a-z0-9-]+\.html"' docs/index.html | sort -u | wc -l)" -ge 16 ] && [ -z "$(for h in $(grep -oE 'href="[^\"#:]+"' docs/index.html | sed 's/href=\"//;s/\"//' | grep -v '^http'); do test -f docs/$h || echo MISSING; done)" ] && echo OK</automated>
  </verify>
  <done>docs/index.html is a 3-group card-grid hub generated from the manifest; all 16 note cards + 4 architecture cards + demos card resolve to real files; carries noindex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copied demo .py served publicly | Demo source files copied into the served docs/demos/ tree |
| hub generated from manifest | Link set is machine-generated from the converter's own manifest (no hand-typed slugs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | docs/index.html, docs/demos/* served publicly when deployed | accept | noindex,nofollow on hub + demos page (HTML-07); unlisted URL. Deploy-time exposure decision deferred per CONTEXT.md |
| T-07-08 | Tampering (broken nav / dead links) | hub card hrefs | mitigate | Hub generated from the same .build_manifest.json the converter wrote (Pitfall 5 single-source-of-truth); per-task on-disk href existence check; plan 04 full link-validation pass |
| T-07-09 | Repudiation/integrity | embedded demo code drifting from source | accept | Snippets sliced at build time directly from the copied .py; re-running the build re-syncs; full source linked alongside for verification |
</threat_model>

<verification>
- HTML-01: hub navigable; every link resolves to a real page (no 404 at file://).
- HTML-05: 3 demos each have explanation + embedded key code + result + working source link.
- D-09/D-10/D-11/D-12 satisfied.
</verification>

<success_criteria>
- HTML-01: card-grid hub in 3 groups linking all content.
- HTML-05: demos page complete for all three demos.
- Build remains one idempotent docs/build_site.py.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integrated-html-study-site/07-03-SUMMARY.md`: hub group structure, demo slicing line numbers used, demo asset filenames in docs/demos/, and confirmation the hub is manifest-driven (so re-runs stay in sync).
</output>
