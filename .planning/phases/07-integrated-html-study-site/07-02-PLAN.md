---
phase: 07-integrated-html-study-site
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - docs/build_site.py
  - docs/architecture/
autonomous: true
requirements: [HTML-02]
user_setup: []

must_haves:
  truths:
    - "HTML-02: The three existing research HTML pages (AGMS-architecture, grid-operations-and-role, INDEX) are reachable copies under docs/architecture/ and render with internal cross-links intact (D-07)"
    - "HTML-02: The standalone AGMS architecture diagram displays via a viewer page docs/architecture/diagram.html (Pitfall 7)"
    - "D-07: Pages are COPIED, not rebuilt; the sources/ siblings (flisr, mapek) are copied so cross-links resolve"
    - "D-08: Patent-PDF references inside the copied INDEX.html resolve locally (verified: they are inert <code> labels, so no broken hrefs exist; the PDFs live at docs/*.pdf if linked)"
  artifacts:
    - path: "docs/architecture/AGMS-architecture.html"
      provides: "Copied AGMS walkthrough with rewritten cross-links"
      contains: "grid-operations-and-role.html"
    - path: "docs/architecture/grid-operations-and-role.html"
      provides: "Copied grid-ops companion with rewritten cross-links"
    - path: "docs/architecture/INDEX.html"
      provides: "Copied patent index"
    - path: "docs/architecture/sources/mapek-aware-2025.html"
      provides: "Copied source sibling (link-closure)"
    - path: "docs/architecture/AGMS-architecture.svg"
      provides: "Copied diagram asset"
    - path: "docs/architecture/diagram.html"
      provides: "Shared-shell viewer page embedding the SVG (HTML-02 diagram displays)"
      contains: "AGMS-architecture.svg"
  key_links:
    - from: "docs/architecture/grid-operations-and-role.html"
      to: "docs/architecture/AGMS-architecture.html"
      via: "rewritten relative href (was patents/AGMS-architecture.html)"
      pattern: "href=\"AGMS-architecture.html\""
    - from: "docs/architecture/AGMS-architecture.html"
      to: "docs/architecture/grid-operations-and-role.html"
      via: "rewritten relative href (was ../grid-operations-and-role.html)"
      pattern: "href=\"grid-operations-and-role.html\""
    - from: "docs/architecture/diagram.html"
      to: "docs/architecture/AGMS-architecture.svg"
      via: "inline <img>"
      pattern: "<img[^>]*AGMS-architecture.svg"
---

<objective>
Integrate the existing AGMS architecture/patents research HTML into the site by COPYING (not rebuilding) the three research pages plus their source siblings and the architecture diagram into docs/architecture/, rewriting the cross-links that break when the trio is flattened from two directory depths into one, and giving the standalone diagram a viewer page so it actually displays.

Purpose: HTML-02. The existing pages are excellent and locked (D-07) — this plan moves them into the published site with intact internal navigation and surfaces the orphaned diagram.
Output: docs/architecture/ containing 3 copied+relinked HTML pages, sources/ siblings, the SVG+PNG diagram assets, and a new diagram.html viewer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integrated-html-study-site/07-SPEC.md
@.planning/phases/07-integrated-html-study-site/07-CONTEXT.md
@.planning/phases/07-integrated-html-study-site/07-RESEARCH.md
@.planning/phases/07-integrated-html-study-site/07-PATTERNS.md
@.planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy research trio + sources + diagram into docs/architecture/ and rewrite cross-links</name>
  <files>docs/build_site.py, docs/architecture/</files>
  <read_first>
    - .planning/phases/07-integrated-html-study-site/07-PATTERNS.md (the link-closure / rewrite map table, lines 210-221; files-to-copy list line 221; dead-.md note line 223)
    - .planning/phases/07-integrated-html-study-site/07-RESEARCH.md (Pitfall 3 broken cross-links; Pitfall 4 dead .md quick-links; Pitfall 6 large assets)
    - .planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md (build_site.py module layout — extend the SAME file)
    - .planning/research/patents/AGMS-architecture.html (confirm only cross-page hrefs are `../grid-operations-and-role.html` and `INDEX.html`; the rest are #anchors)
    - .planning/research/grid-operations-and-role.html (confirm hrefs `patents/AGMS-architecture.html`, `patents/INDEX.html`, `sources/flisr-distributed-fsm-2014.html`, `sources/mapek-aware-2025.html`)
  </read_first>
  <action>
    Add an `build_architecture()` step to docs/build_site.py (extend the existing file from plan 01; do NOT create a second script).

    Target layout (flatten the trio directly into architecture/, keep sources/ one level down):
      docs/architecture/AGMS-architecture.html
      docs/architecture/grid-operations-and-role.html
      docs/architecture/INDEX.html
      docs/architecture/sources/flisr-distributed-fsm-2014.html
      docs/architecture/sources/mapek-aware-2025.html
      docs/architecture/AGMS-architecture.svg          (2.9 MB)
      docs/architecture/AGMS-architecture.drawio.png    (1.6 MB)

    1. Create docs/architecture/ and docs/architecture/sources/.
    2. Copy the assets (do NOT modify the SVG/PNG):
       - .planning/research/patents/AGMS-architecture.svg  -> docs/architecture/AGMS-architecture.svg
       - .planning/research/patents/AGMS-architecture.drawio.png -> docs/architecture/AGMS-architecture.drawio.png
       - .planning/research/sources/flisr-distributed-fsm-2014.html -> docs/architecture/sources/
       - .planning/research/sources/mapek-aware-2025.html -> docs/architecture/sources/
       (The sources/ pages reference `../grid-operations-and-role.html` and `../patents/AGMS-architecture.html`. After the trio is flattened into architecture/, `../grid-operations-and-role.html` from sources/ still resolves to architecture/grid-operations-and-role.html, BUT `../patents/AGMS-architecture.html` does NOT — rewrite it to `../AGMS-architecture.html`.)
    3. Copy + REWRITE the three trio pages by reading each file, applying string replacements, and writing to docs/architecture/. Apply EXACTLY this rewrite map (verified by grep — PATTERNS table):
       - grid-operations-and-role.html:  `href="patents/AGMS-architecture.html"` -> `href="AGMS-architecture.html"`;  `href="patents/INDEX.html"` -> `href="INDEX.html"`;  the two `sources/...html` hrefs stay unchanged.
       - AGMS-architecture.html:  `href="../grid-operations-and-role.html"` -> `href="grid-operations-and-role.html"`;  `href="INDEX.html"` stays unchanged.
       - INDEX.html:  `href="AGMS-architecture.html"` stays unchanged. NOTE (D-08, verified): INDEX.html contains NO real PDF hrefs — `patent-data-management.pdf` and `docs/` appear only as inert `<code>` text, not links. Do NOT invent a rewrite. If, on inspection, any real `<a href="...patent-*.pdf">` is present, rewrite it to `../<name>.pdf` (resolves to docs/*.pdf). Otherwise leave the file's body untouched aside from the cross-page hrefs above.
       - sources/flisr-distributed-fsm-2014.html and sources/mapek-aware-2025.html:  `href="../patents/AGMS-architecture.html"` -> `href="../AGMS-architecture.html"`;  `href="../grid-operations-and-role.html"` stays unchanged.
    4. Dead `.md` quick-links in AGMS-architecture.html (adaptive-power.md, logistician-module.md, etc.) are inert `<code>` labels, NOT hrefs (Pitfall 4) — leave them as-is.
    5. Do NOT re-copy the patent PDFs — they already live at docs/*.pdf (D-04). Do NOT alter the external Wikipedia/Google hrefs in grid-operations-and-role.html (out of scope; they require network but are not core content).
  </action>
  <acceptance_criteria>
    - `test -f docs/architecture/AGMS-architecture.html && test -f docs/architecture/grid-operations-and-role.html && test -f docs/architecture/INDEX.html` succeeds
    - `test -f docs/architecture/sources/flisr-distributed-fsm-2014.html && test -f docs/architecture/sources/mapek-aware-2025.html` succeeds
    - `test -f docs/architecture/AGMS-architecture.svg && test -f docs/architecture/AGMS-architecture.drawio.png` succeeds
    - `grep -c 'href="patents/' docs/architecture/grid-operations-and-role.html` == 0 (old patents/ prefix rewritten away)
    - `grep -c 'href="AGMS-architecture.html"' docs/architecture/grid-operations-and-role.html` >= 1
    - `grep -c 'href="../grid-operations-and-role.html"' docs/architecture/AGMS-architecture.html` == 0 AND `grep -c 'href="grid-operations-and-role.html"' docs/architecture/AGMS-architecture.html` >= 1
    - `grep -c 'href="../patents/' docs/architecture/sources/mapek-aware-2025.html` == 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/juan/codes/ge-vernova-virtual-sensing && .venv-site/bin/python docs/build_site.py && test -f docs/architecture/AGMS-architecture.svg && [ "$(grep -c 'href="patents/' docs/architecture/grid-operations-and-role.html)" -eq 0 ] && [ "$(grep -c 'href="grid-operations-and-role.html"' docs/architecture/AGMS-architecture.html)" -ge 1 ] && [ "$(grep -c 'href="../patents/' docs/architecture/sources/mapek-aware-2025.html)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>The research trio + 2 source siblings + SVG/PNG are copied into docs/architecture/; all inter-page cross-links rewritten to resolve within the flattened layout; PDFs not duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the AGMS diagram viewer page in the shared shell</name>
  <files>docs/build_site.py, docs/architecture/diagram.html</files>
  <read_first>
    - .planning/phases/07-integrated-html-study-site/07-RESEARCH.md (Pitfall 7 standalone diagram — recommend a viewer page)
    - .planning/phases/07-integrated-html-study-site/07-PATTERNS.md (shared shell template; figure/figcaption additive rules already in site.css from plan 01)
    - .planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md (the shared HTML shell helper in build_site.py — reuse it, do not duplicate)
  </read_first>
  <action>
    Add a `build_diagram_page()` step to docs/build_site.py that emits docs/architecture/diagram.html using the SAME shared shell helper authored in plan 01 (masthead + .wrap; the TOC may be a minimal "On this page" stub or omitted — the page is a single figure). The page:
    - `<head>` uses the standard contract with relative paths to ../assets/site.css and ../assets/pygments.css and `<meta name="robots" content="noindex,nofollow">`. No MathJax needed (no math) — but including the standard head is harmless; omit the MathJax script for this page to keep it lean.
    - `<body>` (.wrap shell): masthead h1 "AGMS Architecture Diagram"; main contains:
      ```html
      <section class="card">
        <h2 class="sec">AGMS reference architecture</h2>
        <p>The self-organizing grid-management platform: perceive (GWM) &rarr; reason (CaCSM) &rarr; orchestrate (Logistician/GWCH) &rarr; deploy autonomous scouts. SVG below; <a href="AGMS-architecture.drawio.png">PNG fallback</a>.</p>
        <figure>
          <img src="AGMS-architecture.svg" alt="AGMS reference architecture diagram">
          <figcaption>AGMS architecture (source: director's patent family walkthrough). Crisp SVG render.</figcaption>
        </figure>
      </section>
      <footer><a href="../index.html">&larr; Back to study hub</a></footer>
      ```
    - Prefer the SVG inline (crisp); offer the PNG as a download/fallback link (Pitfall 6).
    Re-run the build.
  </action>
  <acceptance_criteria>
    - `test -f docs/architecture/diagram.html` succeeds
    - `grep -cE '<img[^>]*src="AGMS-architecture.svg"' docs/architecture/diagram.html` >= 1
    - `grep -c 'AGMS-architecture.drawio.png' docs/architecture/diagram.html` >= 1 (PNG fallback link)
    - `grep -c 'noindex' docs/architecture/diagram.html` >= 1
    - `grep -c '../index.html' docs/architecture/diagram.html` >= 1 (back-to-hub footer)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/juan/codes/ge-vernova-virtual-sensing && .venv-site/bin/python docs/build_site.py && test -f docs/architecture/diagram.html && grep -qE '<img[^>]*src="AGMS-architecture.svg"' docs/architecture/diagram.html && grep -q noindex docs/architecture/diagram.html && echo OK</automated>
  </verify>
  <done>docs/architecture/diagram.html displays the AGMS SVG inline in the shared design-system shell with a PNG fallback link and a back-to-hub footer; the previously-orphaned diagram is now reachable (HTML-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copied third-party-derived HTML | Research pages contain external Wikipedia/Google hrefs (left intact, out of scope) — they need network but are not core content |
| served diagram assets | Large SVG/PNG copied into the published docs/ tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | docs/architecture/*.html served publicly when deployed | accept | noindex,nofollow on the new diagram.html (HTML-07); copied research pages retain their own heads. Deploy-time public-exposure decision deferred per CONTEXT.md |
| T-07-05 | Tampering (integrity) | flattened cross-links | mitigate | Deterministic string-replacement rewrite map (verified by grep against the known href set); plan 04 link-validation pass asserts every emitted href resolves on disk |
| T-07-06 | Denial of Service (broken nav) | external Wikipedia/Google links in grid-ops page | accept | Out of scope per SPEC (not core content); they degrade gracefully offline; core intra-site navigation is fully relative |
</threat_model>

<verification>
- The three research pages render from docs/architecture/ with intact inter-page navigation (HTML-02).
- sources/ siblings copied so grid-ops cross-links resolve (no 404).
- The AGMS diagram displays via diagram.html (Pitfall 7 resolved).
- No PDFs duplicated (D-04); D-08 satisfied (no real broken PDF hrefs exist; inert <code> left as-is).
</verification>

<success_criteria>
- HTML-02: architecture section integrated by copy-not-rebuild (D-07), cross-links intact, diagram displays.
- Build remains a single idempotent docs/build_site.py.
</success_criteria>

<output>
After completion, append to / create `.planning/phases/07-integrated-html-study-site/07-02-SUMMARY.md`: the architecture/ layout, the exact rewrite replacements applied, the diagram-viewer URL, and the manifest entries the hub (plan 03) should link (AGMS-architecture.html, grid-operations-and-role.html, INDEX.html, diagram.html).
</output>
