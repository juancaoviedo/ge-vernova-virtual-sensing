---
phase: 07-integrated-html-study-site
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - docs/build_site.py
  - docs/.nojekyll
  - docs/robots.txt
  - docs/PUBLISH.md
autonomous: false
requirements: [HTML-06, HTML-07]
user_setup: []

must_haves:
  truths:
    - "HTML-06: The whole site navigates at file:// with no server and no network — every emitted href resolves to a file inside docs/ (a build-time link-validation pass enforces this and fails loud on any miss)"
    - "HTML-06: docs/PUBLISH.md gives concrete GitHub-Pages-from-/docs deploy steps + the offline-smoke-test + the unlisted/noindex note (D-06)"
    - "HTML-07: every emitted .html carries noindex,nofollow AND docs/robots.txt disallows all crawlers; docs/.nojekyll exists so Pages does not mangle vendor/"
    - "D-06: publish target is GitHub Pages from /docs; actual deploy is deferred and documented as such in PUBLISH.md (deferred per CONTEXT.md)"
  artifacts:
    - path: "docs/.nojekyll"
      provides: "Empty marker stopping Jekyll from ignoring vendor/ + _-prefixed MathJax paths"
    - path: "docs/robots.txt"
      provides: "Crawler disallow reinforcing noindex (HTML-07)"
      contains: "Disallow: /"
    - path: "docs/PUBLISH.md"
      provides: "Concrete GitHub-Pages-from-/docs deploy guide + offline smoke test + unlisted-URL/noindex note"
      contains: "noindex"
    - path: "docs/build_site.py"
      provides: "Build script with a final link-validation pass that asserts every emitted href exists on disk and fails loud"
      contains: "def validate"
  key_links:
    - from: "docs/build_site.py"
      to: "all emitted docs/**/*.html"
      via: "link-validation pass over every written href"
      pattern: "validate"
---

<objective>
Make the assembled site self-contained and publish-ready: emit the publishing config files (.nojekyll, robots.txt), add a build-time link-validation pass that proves the whole site navigates offline with zero broken links (the HTML-01/HTML-06 acceptance gate), write the PUBLISH.md deploy guide, and finalize the noindex posture (HTML-07).

Purpose: HTML-06 + HTML-07. This closes the phase — the site must open at file:// with no network and be one command away from a GitHub-Pages deploy (D-06, deferred).
Output: docs/.nojekyll, docs/robots.txt, docs/PUBLISH.md, and a validate() pass wired into docs/build_site.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integrated-html-study-site/07-SPEC.md
@.planning/phases/07-integrated-html-study-site/07-CONTEXT.md
@.planning/phases/07-integrated-html-study-site/07-RESEARCH.md
@.planning/phases/07-integrated-html-study-site/07-PATTERNS.md
@.planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md
@.planning/phases/07-integrated-html-study-site/07-02-SUMMARY.md
@.planning/phases/07-integrated-html-study-site/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit .nojekyll + robots.txt and add the build-time link-validation pass</name>
  <files>docs/build_site.py, docs/.nojekyll, docs/robots.txt</files>
  <read_first>
    - .planning/phases/07-integrated-html-study-site/07-RESEARCH.md (robots.txt content; .nojekyll rationale "stops Pages/Jekyll mangling vendor/"; Validation Architecture / Wave 0 Gaps — the link-validation pass IS the acceptance gate; Pitfall 5)
    - .planning/phases/07-integrated-html-study-site/07-PATTERNS.md (No Analog Found: .nojekyll empty, robots.txt content)
    - .planning/phases/07-integrated-html-study-site/07-01-SUMMARY.md (build_site.py module layout — add validate() to the same file)
  </read_first>
  <action>
    Extend docs/build_site.py (same file):

    1. Write docs/.nojekyll as an EMPTY file (its presence is the signal — stops GitHub Pages' Jekyll from ignoring vendor/ and _-prefixed MathJax component paths, RESEARCH anti-pattern).
    2. Write docs/robots.txt with exactly:
       ```
       User-agent: *
       Disallow: /
       ```
    3. Add `def validate_links()` run as the FINAL build step. For every emitted HTML page under docs/ (index.html, notes/*.html, demos/index.html, architecture/*.html, architecture/sources/*.html, architecture/diagram.html), parse each `href="..."`/`src="..."` value:
       - SKIP external schemes (http://, https://, mailto:) and pure-anchor links (`#...`).
       - For the remainder, resolve the path RELATIVE TO THE PAGE'S OWN DIRECTORY (strip any trailing `#anchor`), and assert the target file exists on disk.
       - Collect ALL misses; if any exist, print each as "BROKEN: {page} -> {href}" and raise SystemExit(1) (fail loud — UI-SPEC error contract). This is the HTML-06 acceptance gate.
       Use stdlib only (re + pathlib) — keep the venv dependency surface minimal. Also assert the noindex audit: every emitted .html contains `noindex` (HTML-07); report any page lacking it as a broken-build failure too.
    4. Re-run the full build end to end: `.venv-site/bin/python docs/build_site.py` must regenerate notes + architecture + demos + hub + config + validation in one idempotent run and exit 0.
  </action>
  <acceptance_criteria>
    - `test -f docs/.nojekyll` succeeds AND the file is empty (`test ! -s docs/.nojekyll`)
    - `grep -c 'Disallow: /' docs/robots.txt` >= 1 AND `grep -c 'User-agent: \*' docs/robots.txt` >= 1
    - `grep -c 'def validate' docs/build_site.py` >= 1
    - `.venv-site/bin/python docs/build_site.py` exits 0 (validation passes — zero broken links)
    - `grep -rL 'noindex' docs/index.html docs/notes/*.html docs/demos/index.html docs/architecture/diagram.html` prints nothing (every NEW emitted page has noindex — HTML-07)
    - Negative check: temporarily breaking a manifest href makes `build_site.py` exit non-zero with a BROKEN: line (executor confirms the gate actually fails loud, then reverts)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/juan/codes/ge-vernova-virtual-sensing && .venv-site/bin/python docs/build_site.py && test -f docs/.nojekyll && [ ! -s docs/.nojekyll ] && grep -q 'Disallow: /' docs/robots.txt && grep -q 'def validate' docs/build_site.py && [ -z "$(grep -rL noindex docs/index.html docs/notes/*.html docs/demos/index.html docs/architecture/diagram.html)" ] && echo OK</automated>
  </verify>
  <done>.nojekyll + robots.txt emitted; build_site.py ends with a fail-loud link-validation + noindex-audit pass; a clean run exits 0 with zero broken links across the whole site.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/PUBLISH.md deploy guide</name>
  <files>docs/PUBLISH.md</files>
  <read_first>
    - .planning/phases/07-integrated-html-study-site/07-CONTEXT.md (D-06 publish target GitHub Pages from /docs; Deferred Ideas: actual deploy + the patent/CV public-exposure deploy-time decision)
    - .planning/phases/07-integrated-html-study-site/07-RESEARCH.md (GitHub Pages section; offline smoke test note; Security Domain disclosure threat)
    - .planning/phases/07-integrated-html-study-site/07-SPEC.md (HTML-06 / HTML-07 acceptance)
  </read_first>
  <action>
    Write docs/PUBLISH.md (location per D-06 discretion — choose /docs so it travels with the site; it is itself noindex-served, harmless). Include these concrete sections:

    1. **Rebuild** — `cd <repo> && .venv-site/bin/python docs/build_site.py` (re-runnable; regenerates notes/architecture/demos/hub + validates links).
    2. **Offline smoke test (HTML-06)** — exact steps: disable networking, open `docs/index.html` via file://, click through to a math note (e.g. notes/kal-03-ieee738-ekf-worked-example.html) and confirm typeset equations, reach the architecture diagram, open a demo and confirm the result image + code render. "Zero broken links; math typesets with no network."
    3. **Deploy to GitHub Pages from /docs (zero-config, D-06)** — concrete steps: commit docs/ to the default branch; GitHub repo Settings -> Pages -> Source: "Deploy from a branch" -> branch = main, folder = `/docs` -> Save; the site publishes at `https://<user>.github.io/<repo>/`. Note that `.nojekyll` ensures vendor/ (MathJax) is served verbatim.
    4. **Unlisted / noindex sharing (HTML-07)** — explain every page carries `<meta name="robots" content="noindex,nofollow">` plus robots.txt Disallow; the URL is shareable with interviewers but discouraged from search indexing. State plainly: this is unlisted-by-obscurity, not access-controlled.
    5. **Deferred deploy-time privacy decision (from CONTEXT.md)** — call out explicitly: publishing /docs makes the six patent PDFs and Juan's CV publicly fetchable at the site URL. Patents are public record and the site is noindex/unlisted, but BEFORE actually enabling Pages, confirm this is acceptable OR move the CV/sensitive PDFs out of the served path. This is a conscious deploy-time choice, recorded as deferred — not done in this phase.
    Keep it concise and actionable (a checklist a reader can follow without re-reading the codebase).
  </action>
  <acceptance_criteria>
    - `test -f docs/PUBLISH.md` succeeds
    - `grep -ci 'github pages' docs/PUBLISH.md` >= 1 AND `grep -c '/docs' docs/PUBLISH.md` >= 1
    - `grep -c 'noindex' docs/PUBLISH.md` >= 1 (unlisted-sharing note — HTML-07)
    - `grep -ci 'offline' docs/PUBLISH.md` >= 1 AND `grep -ci 'file://' docs/PUBLISH.md` >= 1 (offline smoke test documented — HTML-06)
    - `grep -ciE 'patent|cv|disclosure' docs/PUBLISH.md` >= 1 (deferred privacy decision recorded)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/juan/codes/ge-vernova-virtual-sensing && test -f docs/PUBLISH.md && grep -qi 'github pages' docs/PUBLISH.md && grep -q noindex docs/PUBLISH.md && grep -qi 'file://' docs/PUBLISH.md && grep -qiE 'patent|cv|disclosure' docs/PUBLISH.md && echo OK</automated>
  </verify>
  <done>docs/PUBLISH.md documents rebuild, offline smoke test, GitHub-Pages-from-/docs deploy, unlisted/noindex sharing, and the deferred privacy decision — concrete and checklist-style.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete self-contained study site: hub + 15 notes + 3 architecture pages + diagram + demos, with offline MathJax and a passing build-time link-validation pass (HTML-06 full-offline navigation — final acceptance).</what-built>
  <how-to-verify>
    1. DISABLE networking (airplane mode / `nmcli networking off`).
    2. Open `docs/index.html` via file:// in a browser.
    3. From the hub, click into each of the 3 groups: open an Architecture page (AGMS walkthrough), follow its cross-link to grid-operations and back, open the diagram page (SVG displays), open 2-3 Study Notes (math typesets), open the Demos page (result images + code + source links work).
    4. Confirm ZERO broken links / 404s while fully offline.
    5. Confirm PUBLISH.md reads as a clear deploy checklist.
  </how-to-verify>
  <resume-signal>Type "approved" if the entire site navigates offline with no broken links and PUBLISH.md is clear; otherwise list the broken links / issues so the build can be corrected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ served by future GitHub Pages | Publishing /docs exposes everything in it (incl. patent PDFs + CV) at the site URL |
| build-time validation | Link/noindex integrity is enforced at build time, not runtime (static site) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Information Disclosure | patent PDFs + CV in served docs/ | accept (deferred) | noindex,nofollow on every page + robots.txt Disallow (HTML-07); unlisted URL. PUBLISH.md explicitly records the deploy-time confirm-or-relocate decision. Patents are public record; conscious deferred choice per CONTEXT.md — NOT a build blocker |
| T-07-11 | Tampering (broken nav) | any emitted href | mitigate | validate_links() fails the build loud on the first broken href or missing noindex; negative-tested in Task 1 |
| T-07-12 | Config | .nojekyll absent -> Pages mangles vendor/ MathJax | mitigate | .nojekyll emitted as an empty marker so Pages serves vendor/ and _-prefixed component paths verbatim |
</threat_model>

<verification>
- HTML-06: build_site.py validation pass proves zero broken links; human checkpoint confirms full offline navigation; PUBLISH.md exists with concrete deploy steps.
- HTML-07: noindex on every emitted page + robots.txt Disallow + .nojekyll present.
- D-06 satisfied; deferred deploy + privacy decision documented (not executed).
</verification>

<success_criteria>
- HTML-06: self-contained, offline-navigable, publish-ready, with PUBLISH.md.
- HTML-07: unlisted-by-default (noindex meta + robots.txt).
- The single idempotent docs/build_site.py builds the entire site and self-validates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integrated-html-study-site/07-04-SUMMARY.md`: the validation-pass behavior, the publish config files emitted, the PUBLISH.md location, and the deferred deploy-time privacy decision so it surfaces at deploy time.
</output>
