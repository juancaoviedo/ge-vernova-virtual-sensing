---
phase: 09-measurement-system-observability-layer-configurable-sensor-m
plan: 05
type: execute
wave: 4
depends_on: ["04"]
files_modified:
  - system1-measurement-source/grafana/provisioning/dashboards/ieee33-meas-day.json
  - system1-measurement-source/grafana/provisioning/dashboards/ieee33-meas-fault.json
  - system1-measurement-source/README.md
  - system1-measurement-source/tests/test_measure_determinism.py
autonomous: false
requirements: [R10, R11, R12]
must_haves:
  truths:
    - "D-16: two auto-provisioned dashboards ieee33-meas-day.json and ieee33-meas-fault.json exist over the measurements bucket with observed-vs-true overlay, observed-vs-pseudo footprint, per-class counts, and (fault) phase marker + dead-bus count"
    - "D-10: determinism is proven — two same-config runs produce byte-identical measurement values (SPEC R10)"
    - "D-14: a multirate cadence test proves AMI appears at 96/4=24 day steps and μPMU at 96 while snapshot emits all classes every step"
    - "D-03: a test proves no live measurement is emitted for a dead bus during faulted_isolated (bus 17 dark)"
    - "D-09: the README documents uv run measure + the ACTIVE block + CLI overrides + how to open the two new dashboards"
  artifacts:
    - path: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-meas-day.json"
      provides: "Day observed-states dashboard (uid ieee33-meas-day)"
      contains: "ieee33-meas-day"
    - path: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-meas-fault.json"
      provides: "Fault observed-states dashboard (uid ieee33-meas-fault)"
      contains: "ieee33-meas-fault"
    - path: "system1-measurement-source/tests/test_measure_determinism.py"
      provides: "Determinism + cadence + dead-bus-gate + no-true-value tests"
      contains: "def test_"
    - path: "system1-measurement-source/README.md"
      provides: "Measurement-layer runbook section"
      contains: "uv run measure"
  key_links:
    - from: "ieee33-meas-day.json panel"
      to: "measurements bucket meas points"
      via: "Flux filter _measurement==meas"
      pattern: "measurements"
    - from: "test_measure_determinism"
      to: "measurements bucket values"
      via: "two-run byte-identical compare"
      pattern: "byte.identical\\|test_determinism\\|run1.*run2"
---

<objective>
Ship the observability + verification layer: two auto-provisioned Grafana dashboards over the
`measurements` bucket (D-16), the README runbook (D-09/R12), and an automated test module proving
determinism (R10), multirate cadence (D-14), the dead-bus gate (D-03), and oracle separation
(no true_value). Ends with a human-verify checkpoint to confirm the dashboards render.

Purpose: The SPEC acceptance gates (R10 byte-identical, R11 two dashboards, R12 README+footprint)
are closed here. The tests turn the determinism/cadence/gate invariants into runnable assertions.

Output: two dashboard JSONs, a README section, a pytest module, and a verified-rendering checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-SPEC.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-CONTEXT.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-PATTERNS.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-RESEARCH.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-04-SUMMARY.md

<interfaces>
<!-- Dashboard + test contracts. -->

Grafana provisioning (verified):
- Datasource UID: "ieee33-influxdb" (every panel + target must carry
  `"datasource": {"type":"influxdb","uid":"ieee33-influxdb"}`)
- Existing dashboard uids: "ieee33-state-v1", "ieee33-fault-event" → new uids MUST be
  "ieee33-meas-day" and "ieee33-meas-fault" (Pitfall 4: no uid collision)
- Provider auto-loads any .json in grafana/provisioning/dashboards/ (updateIntervalSeconds:30)
- Skeleton fields: "id": null, "schemaVersion": 39, "refresh": "30s", "tags": ["measurements"]
- Analogs: ieee33-state.json (day overlay structure), ieee33-fault-event.json (phase/stat panels)

Measurements schema to query (D-06): measurement "meas"; tags class/quantity/location/scenario/
experiment(+phase); fields value, assumed_sigma. Topology: measurement "event" (D-07).

Dashboard time windows: day = 2017-06-07T00:00:00Z..2017-06-07T23:59:59Z;
fault = 2017-06-07T17:59:00Z..2017-06-07T18:03:00Z (mirror ieee33-fault-event.json's 4-min window).

Test prerequisites: a running Docker stack + populated state/fault_event buckets
(`uv run sim`, `uv run fault-sim`). The determinism test runs `measure.main()` (or the
measure module's run with a fixed cfg) twice and compares queried meas values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two provisioned Grafana dashboards over the measurements bucket</name>
  <read_first>
    - system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json (full skeleton, datasource ref, two-refId overlay target pattern, fieldConfig)
    - system1-measurement-source/grafana/provisioning/dashboards/ieee33-fault-event.json (stat/phase panel pattern, 4-min fault window)
    - 09-PATTERNS.md "ieee33-meas-day.json" + "ieee33-meas-fault.json" sections (exact skeletons + panel lists)
    - 09-RESEARCH.md Q7 (provisioning mechanism, dual-source overlay, phase markers)
    - 09-CONTEXT.md D-16
  </read_first>
  <action>
Create two new dashboard JSONs in `system1-measurement-source/grafana/provisioning/dashboards/`.
Mirror the existing JSON skeleton exactly (`"id": null`, `"schemaVersion": 39`, `"refresh": "30s"`,
`"__inputs": []`, `"__requires"` block, every panel+target carrying
`"datasource": {"type":"influxdb","uid":"ieee33-influxdb"}`).

`ieee33-meas-day.json` — `"uid": "ieee33-meas-day"`, title "IEEE 33-Bus Observed Measurements — Day",
tags ["measurements"]. Panels (12-col grid):
  1. Observed bus voltages (timeseries): target A = measurements bucket, _measurement=="meas",
     _field=="value", quantity=="vm_pu", scenario=="realistic_sparse", experiment=="day"; target B
     (overlay) = state bucket, _measurement=="bus", _field=="vm_pu" (true series). Day window.
  2. Observed power injections (timeseries): meas value where quantity=="p_inj_mw", experiment=="day".
  3. Per-class measurement counts (stat or bar): group meas by class, count. (Add stat to __requires.)
  4. Observed-vs-pseudo footprint (bar/table by location): meas grouped by location with class —
     shows which buses are real vs pseudo.

`ieee33-meas-fault.json` — `"uid": "ieee33-meas-fault"`, title "IEEE 33-Bus Observed Measurements —
Fault". Use the 4-min fault window. Panels:
  1. Observed bus voltages across 40 steps: target A meas (experiment=="fault"), target B overlay
     fault_event bus vm_pu (true). Note in description: μPMU bus 17 goes dark during isolation.
  2. Dead-bus count over time (timeseries/stat): measurements bucket event measurement,
     _field=="n_dead_buses", experiment=="fault".
  3. Phase marker (stat/state-timeline): event measurement phase tag across the window.
  4. Per-class measurement counts across phases.
  5. Observed-vs-pseudo footprint (changes at isolation).

Each Flux query is a single escaped-newline string in the panel target `"query"` field (mirror
ieee33-state.json line 80-83). Do NOT modify the existing two dashboards or default.yml.
  </action>
  <verify>
    <automated>cd system1-measurement-source && python -c "import json,glob; d=json.load(open('grafana/provisioning/dashboards/ieee33-meas-day.json')); f=json.load(open('grafana/provisioning/dashboards/ieee33-meas-fault.json')); assert d['uid']=='ieee33-meas-day' and f['uid']=='ieee33-meas-fault'; assert d['id'] is None and f['id'] is None; uids={json.load(open(p))['uid'] for p in glob.glob('grafana/provisioning/dashboards/*.json')}; assert 'ieee33-state-v1' in uids and 'ieee33-fault-event' in uids and len(uids)==4; assert 'measurements' in json.dumps(d) and 'meas' in json.dumps(d); assert 'n_dead_buses' in json.dumps(f); print('dashboards OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both JSON files parse (the python -c command prints `dashboards OK`)
    - `grep -c '"uid": "ieee33-meas-day"' grafana/provisioning/dashboards/ieee33-meas-day.json` == 1 and likewise for meas-fault (no uid collision — Pitfall 4)
    - All four dashboard uids are distinct (assertion checks len==4)
    - Both new dashboards reference the `measurements` bucket and `ieee33-influxdb` datasource uid
    - The fault dashboard queries the re-published `event` measurement (`n_dead_buses` present)
    - Existing ieee33-state.json / ieee33-fault-event.json / default.yml are byte-unchanged (git diff shows no edits to them)
  </acceptance_criteria>
  <done>Two valid dashboard JSONs with distinct uids over the measurements bucket; existing dashboards/provisioning untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Determinism + cadence + dead-bus-gate + oracle-separation tests, and README runbook</name>
  <read_first>
    - system1-measurement-source/src/ieee33/measure.py (main() / run signature, the cfg merge, write path)
    - system1-measurement-source/src/ieee33/influx.py (get_client + a query helper to read meas values back)
    - system1-measurement-source/README.md (existing structure: Prerequisites, run sections to extend)
    - 09-RESEARCH.md Q8 (determinism assertion, cadence count assertion, unseeded-grep check)
    - 09-CONTEXT.md D-03, D-09, D-10, D-14, D-15
  </read_first>
  <behavior>
    - test_no_unseeded_randomness: greps src/ieee33/measure.py — asserts no `datetime.now`, `time.time`,
      `random.random`, `np.random.seed`, and that the only RNG is `np.random.default_rng` (pure static
      check; needs no InfluxDB)
    - test_no_true_value_field: greps measure.py + influx.py — asserts no `.field("true_value"` anywhere
      (SPEC R9 / D-06 oracle separation; static check)
    - test_meas_schema_vocab: imports measure_config — asserts MEAS_QUANTITIES/CLASS_SIGMA/SCENARIOS
      match the locked D-04/D-05/D-11 values (static check; always runnable)
    - test_determinism (integration, skipped if no InfluxDB): run measure twice with the same cfg,
      query meas (timestamp,location,quantity,class,value) tuples, assert run1 == run2 byte-identical
    - test_multirate_cadence (integration): after a day+multirate_async run, assert AMI distinct
      timestamps == 24 (96/4) and μPMU == 96; snapshot run → AMI == 96
    - test_dead_bus_gate (integration): after a fault run, assert zero meas points for bus 17 in
      faulted_isolated phase (D-03 dark bus)
    - Integration tests use a module-level skip when the Docker InfluxDB is unreachable (pytest.skip)
  </behavior>
  <action>
Create `system1-measurement-source/tests/test_measure_determinism.py` (create the `tests/` dir).
Implement the static tests (always run) and the integration tests (guarded by an
`_influx_available()` helper that tries `influx.get_client()` + a quick ping; `pytest.skip(...)` if
unreachable, so the static gates always pass in CI without Docker):
- `test_no_unseeded_randomness()` — read measure.py text; assert the forbidden tokens absent and
  `default_rng` present.
- `test_no_true_value_field()` — read measure.py + influx.py; assert `.field("true_value"` not in either.
- `test_meas_schema_vocab()` — assert measure_config locked values (pseudo σ 0.30, pmu va 0.0003,
  cadence day ami 4 / fault ami 10 / fault scada 2, SCENARIOS realistic_sparse pmu==[17,24,30],
  well_observed zero_inj==[2,19]).
- `test_determinism()` — run measure twice (call the measure run function with a fixed cfg, e.g.
  scenario=realistic_sparse/source=day/sampling=snapshot/noise=gaussian/seed=42), query all meas
  values both times, sort tuples, assert equal.
- `test_multirate_cadence()` — run day+multirate_async; assert distinct-_time count per class
  (ami==24, pmu==96); run day+snapshot; assert ami==96.
- `test_dead_bus_gate()` — run source=fault; assert no meas points with location=="17" and
  phase=="faulted_isolated".
If `measure.main()` is not directly callable with an injected cfg, add a thin `run(cfg)` function to
measure.py (refactor main() to parse+merge then delegate to run(cfg)) so tests can drive it without
argv — keep main() behavior identical.

Then extend `system1-measurement-source/README.md` with a "## Measurement Layer (System 1 →
System 2 inputs)" section documenting: prerequisites (sim + fault-sim must have populated their
buckets first); `uv run measure`; that the ACTIVE block in `src/ieee33/measure_config.py` is the
PRIMARY switch (scenario/source/sampling/noise/seed) and CLI flags
(`--scenario --source --sampling --noise --seed`) override it for sweeps; the per-run footprint
report meaning (per-class counts, redundancy real-only vs with-pseudo); and how to open the two new
Grafana dashboards (Observed Measurements — Day / — Fault). State explicitly the layer is additive
and does not modify the state/fault_event buckets or existing dashboards.
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run python -m pytest tests/test_measure_determinism.py -k "not determinism and not cadence and not dead_bus" -q && grep -q "uv run measure" README.md && grep -q "measure_config" README.md && grep -q "ieee33-meas-day\|Observed Measurements" README.md && echo "static+readme OK"</automated>
  </verify>
  <acceptance_criteria>
    - The static tests (test_no_unseeded_randomness, test_no_true_value_field, test_meas_schema_vocab) PASS without Docker
    - `grep -c "def test_" tests/test_measure_determinism.py` >= 6
    - `grep -q "pytest.skip\|_influx_available" tests/test_measure_determinism.py` (integration tests guarded)
    - README contains `uv run measure`, `measure_config`, the ACTIVE/CLI override note, and the two dashboard names
    - the command prints `static+readme OK`
  </acceptance_criteria>
  <done>Static determinism/oracle/schema tests pass without Docker; integration tests (determinism, cadence, dead-bus gate) present and Docker-guarded; README documents the runbook + dashboards.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Two auto-provisioned Grafana dashboards over the `measurements` bucket (Observed Measurements — Day
and — Fault) plus a full `uv run measure` pipeline. Claude has already created the JSONs and
verified they parse with distinct uids; the integration tests are Docker-guarded. This checkpoint
confirms the dashboards actually render against live data.
  </what-built>
  <how-to-verify>
1. Ensure the stack is up and source buckets populated:
   `cd system1-measurement-source && docker compose up -d`
   `uv run sim` (populates state) and `uv run fault-sim` (populates fault_event) if not already done.
2. Generate measurements for both regimes, e.g.:
   `uv run measure --scenario realistic_sparse --source day --sampling snapshot --noise gaussian`
   `uv run measure --scenario well_observed --source day --sampling snapshot --noise gaussian`
   `uv run measure --scenario realistic_sparse --source fault --sampling snapshot --noise gaussian`
   Confirm each run prints the footprint report and that well_observed real-only redundancy > 1.0
   and realistic_sparse real-only < 1.0 (≥ 1.0 with pseudo).
3. Open Grafana at http://localhost:3000 and confirm FOUR dashboards are listed: the original two
   (state day, fault-event) PLUS "IEEE 33-Bus Observed Measurements — Day" and "— Fault".
4. Open "— Day": confirm observed voltages render with the true `state` series overlaid, plus the
   observed-vs-pseudo footprint and per-class counts.
5. Open "— Fault": confirm the 40-step observed voltages, the dead-bus-count panel rising during
   isolation, the phase marker, and that μPMU bus 17 drops out during faulted_isolated.
6. Optionally run the full integration tests now that Docker is up:
   `uv run python -m pytest tests/test_measure_determinism.py -q` (all should pass).
  </how-to-verify>
  <resume-signal>Type "approved" if all four dashboards render correctly and the redundancy footprint is right, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana → local InfluxDB | Dashboards query the local datasource via the provisioned literal token; local-only. |
| Browser → local Grafana | Operator views dashboards at localhost:3000; no remote exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Information disclosure | Grafana datasource token | accept | Literal `ieee33-dev-token` in influxdb.yml (Pitfall 8), unchanged from System 1; local Docker only. No new exposure. |
| T-09-13 | Tampering | dashboard JSON uid collision | mitigate | New uids ieee33-meas-day / ieee33-meas-fault are distinct from the existing two (verified by the parse assertion) — no silent overwrite of existing dashboards. |
| T-09-14 | Spoofing | local Grafana login | accept | Local dev stack, default anonymous/admin per existing docker-compose; out of scope for an offline study repo (matches System 1). |
</threat_model>

<verification>
- Both new dashboard JSONs parse with distinct uids; existing dashboards/provisioning unchanged
- Static tests pass without Docker (determinism grep, no-true-value, schema vocab)
- README documents the runbook + ACTIVE/CLI + dashboards
- Human-verify checkpoint confirms four dashboards render and redundancy footprint is correct
</verification>

<success_criteria>
- Two provisioned dashboards over measurements (D-16); existing dashboards untouched
- Determinism (R10), cadence (D-14), dead-bus gate (D-03), oracle separation (R9) proven by tests
- README runbook complete (R12); dashboards render (human-verified)
</success_criteria>

<output>
After completion, create `.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-05-SUMMARY.md`
</output>
