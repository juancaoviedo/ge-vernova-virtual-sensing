---
phase: 09-measurement-system-observability-layer-configurable-sensor-m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - system1-measurement-source/src/ieee33/measure_config.py
  - system1-measurement-source/pyproject.toml
autonomous: true
requirements: [R1, R2, R3, R4, R5, R8]
must_haves:
  truths:
    - "D-04: realistic_sparse SCADA bus 0 / μPMU {17,24,30} / DER {17,21,24,32} / AMI {3,6,9,12,15,18,21,24,28,31} / pseudo=rest are all declared in measure_config.SCENARIOS"
    - "D-05: well_observed SCADA bus 0 / μPMU {0,4,8,13,17,21,24,28,32} / DER {17,21,24,32} / AMI ~80% load buses / zero_inj {2,19} are all declared in measure_config.SCENARIOS"
    - "D-06: the meas-point tag/field contract (class/quantity/location/scenario/experiment/phase tags; value+assumed_sigma fields; no true_value) is documented in measure_config and the quantity vocabulary is enumerated"
    - "D-08: measure_config.py exists in src/ieee33/ with named scenario dicts, noise sigma table, cadence table, and an ACTIVE selection block"
    - "D-09: ACTIVE block is the primary switch with keys scenario/source/sampling/noise/seed/assumed_sigma_scale"
    - "D-11: per-class sigma table matches SCADA |V|0.005 P,Q0.02 / μPMU |V|0.001 angle0.0003 / AMI 0.03 / DER 0.015 / pseudo 0.30 / zero_inj 1e-4"
    - "D-14: CADENCE table declares day {ami:4, rest:1} and fault {scada:2, ami:10, rest:1}"
  artifacts:
    - path: "system1-measurement-source/src/ieee33/measure_config.py"
      provides: "All measurement-layer knobs as pure constants (CLASS_SIGMA, CADENCE, SCENARIOS, ACTIVE)"
      contains: "ACTIVE"
      min_lines: 90
    - path: "system1-measurement-source/pyproject.toml"
      provides: "measure script entry point"
      contains: "measure   = \"ieee33.measure:main\""
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "ieee33.measure:main"
      via: "uv run measure"
      pattern: "measure\\s*=\\s*\"ieee33.measure:main\""
---

<objective>
Create the measurement-layer configuration module (`measure_config.py`) — the single
source of all switchable knobs (scenario / source / sampling / noise / seed / σ) — and
register the `measure` uv script entry point. This is the **contracts plan**: every
downstream plan reads its scenario bus sets, σ values, and cadences from here.

Purpose: D-09 mandates the config file is the PRIMARY switch. Locking the exact bus
assignments (D-04/D-05), σ table (D-11), cadences (D-14), and the meas schema vocabulary
(D-06) here means the runner (Plan 03/04) and tests (Plan 05) consume fixed contracts
rather than re-deriving them.

Output: `system1-measurement-source/src/ieee33/measure_config.py` + the `measure` script
line in `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-SPEC.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-CONTEXT.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-PATTERNS.md
@.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-RESEARCH.md

<interfaces>
<!-- The analog module (config.py) — mirror its pure-constants style exactly. -->
From system1-measurement-source/src/ieee33/config.py (the style analog — DO NOT edit it):
- Module docstring "pure constants — no I/O side effects" pattern (lines 1-15)
- `# ---` section-header comment blocks per logical group
- Inline rationale comments citing D-NN on each constant
- Topology facts to reuse: SOLAR_BUSES=[17,21], WIND_BUSES=[24,32], RPC_SHUNTS={17:-0.4,32:-0.6}
  (DG/sgen buses = 17,21,24,32). Bus 0 = feeder-head/slack (no load). Loads on buses 1..32.

From system1-measurement-source/pyproject.toml [project.scripts] (the addition target):
```toml
[project.scripts]
ingest   = "ieee33.ingest:main"
sim      = "ieee33.sim:main"
validate = "ieee33.validate:main"
fault-sim = "ieee33.fault_sim:main"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create measure_config.py with σ table, cadence table, and scenario bus assignments</name>
  <read_first>
    - system1-measurement-source/src/ieee33/config.py (style analog: pure-constants module, `# ---` section headers, inline D-NN comments, type annotations on dicts — Python 3.12, no `from __future__`)
    - 09-PATTERNS.md "src/ieee33/measure_config.py" section (exact layout to mirror)
    - 09-RESEARCH.md Q6 (the full measure_config.py skeleton)
    - 09-CONTEXT.md D-04, D-05, D-06, D-11, D-14 (the locked values)
  </read_first>
  <action>
Create `system1-measurement-source/src/ieee33/measure_config.py` as a pure-constants module
(docstring stating "pure constants — no I/O side effects"; NO load_dotenv, NO imports beyond
stdlib typing if needed). Mirror config.py's `# ---` section-header style and inline-comment
discipline. Define these blocks exactly:

(1) `# --- per-class sigma table (D-11) ---` — `CLASS_SIGMA: dict[str, dict[str, float]]`:
```python
CLASS_SIGMA = {
    "scada":    {"vm_pu": 0.005, "p_inj_mw": 0.02, "q_inj_mvar": 0.02},
    "pmu":      {"vm_pu": 0.001, "va_degree": 0.0003},   # angle σ is ABSOLUTE radians, not a fraction
    "ami":      {"p_inj_mw": 0.03, "q_inj_mvar": 0.03},
    "der":      {"p_mw": 0.015, "q_mvar": 0.015},
    "pseudo":   {"p_inj_mw": 0.30, "q_inj_mvar": 0.30},
    "zero_inj": {"p_inj_mw": 1e-4, "q_inj_mvar": 1e-4},
}
```
Comment: all values are FRACTION of |measured value| EXCEPT pmu va_degree (absolute rad).

(2) `# --- multirate cadence (per source, per class) (D-14) ---` — `CADENCE: dict[str, dict[str, int]]`:
```python
CADENCE = {
    "day":   {"scada": 1, "pmu": 1, "der": 1, "ami": 4,  "pseudo": 1, "zero_inj": 1},
    "fault": {"scada": 2, "pmu": 1, "der": 1, "ami": 10, "pseudo": 1, "zero_inj": 1},
}
```
Comment: cadence k = "emit only at step indices where step_idx % k == 0"; snapshot mode ignores this.

(3) `# --- sensor-bus assignments per scenario (D-04, D-05) ---` — `SCENARIOS: dict[str, dict]`:
- `realistic_sparse` (D-04): scada=[0]; pmu=[17,24,30]; der=[17,21,24,32];
  ami=[3,6,9,12,15,18,21,24,28,31]; zero_inj=[]. (pseudo derived at runtime = every load bus 1..32
  not covered by scada/pmu/ami/der/zero_inj.)
- `well_observed` (D-05): scada=[0]; pmu=[0,4,8,13,17,21,24,28,32]; der=[17,21,24,32];
  zero_inj=[2,19]; ami = all load buses 1..32 EXCEPT a held-out set {5,10,20,25,29} AND except
  buses already carrying μPMU (Claude's Discretion within "~80% of loads → real redundancy > 1.0").
  Write the explicit AMI list (do not compute it implicitly) so it is greppable.
Add a comment per scenario stating its redundancy target (D-04: real-only <1.0, ≥1.0 after pseudo;
D-05: real >1.0 without pseudo).

(4) `# --- measurements schema vocabulary (D-06 forward contract) ---` — document as constants/comments:
`MEAS_MEASUREMENT = "meas"`; `MEAS_CLASSES = ["scada","pmu","ami","der","pseudo","zero_inj"]`;
`MEAS_QUANTITIES = ["vm_pu","va_degree","p_inj_mw","q_inj_mvar","p_mw","q_mvar"]`;
`MEASUREMENTS_BUCKET = "measurements"`. Comment block: tags = class/quantity/location/scenario/
experiment(+phase for fault); fields = value, assumed_sigma; NO true_value field (SPEC R9 / D-06
scoring-oracle separation).

(5) `# --- noise model defaults (D-12, D-13) ---`: `OUTLIER_FRACTION = 0.03`;
`OUTLIER_SPIKE_MULT = 15.0` (D-12); `INSTRUMENT_BIAS_SCALE = 0.005`; `INSTRUMENT_AR1_RHO = 0.7`
(D-13); and a `QUANT_LSB: dict[str, dict[str, float]]` table (Claude's Discretion per RESEARCH Q3:
scada vm_pu 0.001 / p_inj_mw 0.01 / q_inj_mvar 0.01; pmu vm_pu 0.0001 / va_degree 0.001;
der p_mw 0.001 / q_mvar 0.001; ami p_inj_mw 0.001 / q_inj_mvar 0.001; pseudo & zero_inj 0.001).

(6) `# --- ACTIVE BLOCK — edit to switch experiments (D-09) ---` — `ACTIVE: dict`:
```python
ACTIVE = {
    "scenario": "realistic_sparse",   # "well_observed" | "realistic_sparse"
    "source":   "day",                # "day" | "fault"
    "sampling": "snapshot",           # "snapshot" | "multirate_async"
    "noise":    "gaussian",           # "gaussian" | "gaussian_outliers" | "instrument"
    "seed":     42,
    "assumed_sigma_scale": 1.0,       # assumed_sigma = CLASS_SIGMA * this (1.0 = equal to true σ)
}
```
Every ACTIVE key gets an inline comment listing its valid options (mirrors config.py discipline).
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run python -c "from ieee33 import measure_config as mc; assert mc.ACTIVE['scenario'] in ('well_observed','realistic_sparse'); assert mc.CLASS_SIGMA['pseudo']['p_inj_mw']==0.30; assert mc.CLASS_SIGMA['pmu']['va_degree']==0.0003; assert mc.CADENCE['day']['ami']==4 and mc.CADENCE['fault']['ami']==10 and mc.CADENCE['fault']['scada']==2; assert mc.SCENARIOS['realistic_sparse']['pmu']==[17,24,30]; assert mc.SCENARIOS['well_observed']['zero_inj']==[2,19]; assert 0 in mc.SCENARIOS['well_observed']['pmu']; assert mc.OUTLIER_SPIKE_MULT==15.0; print('measure_config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-04\|D-05\|D-06\|D-11\|D-14" src/ieee33/measure_config.py` ≥ 5 (decisions cited in comments)
    - `grep -q "no.*true_value\|true_value.*field\|scoring" src/ieee33/measure_config.py` (D-06 oracle separation documented)
    - The import-time assertion command above prints `measure_config OK`
    - `grep -cv '^#' src/ieee33/measure_config.py | head` — module is ≥90 non-blank lines
    - No `import os`, no `load_dotenv`, no `np.random`, no `datetime.now` in the file
  </acceptance_criteria>
  <done>
`measure_config.py` imports cleanly; CLASS_SIGMA/CADENCE/SCENARIOS/ACTIVE/QUANT_LSB defined with
exact D-04/D-05/D-11/D-14 values; meas schema vocabulary + oracle-separation documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register the measure script entry point in pyproject.toml</name>
  <read_first>
    - system1-measurement-source/pyproject.toml (the [project.scripts] block, lines 16-20)
    - 09-PATTERNS.md "pyproject.toml — [project.scripts] addition" section
  </read_first>
  <action>
Append ONE line to the existing `[project.scripts]` table in
`system1-measurement-source/pyproject.toml`, preserving column alignment:
`measure   = "ieee33.measure:main"`
Do NOT add dependencies (none needed per D-08/RESEARCH — numpy/pandas/influxdb-client already pinned).
Do NOT touch any other table. The `measure.py` module itself is created in Plan 03; this only
registers the entry point so `uv run measure` resolves once Plan 03 lands.
  </action>
  <verify>
    <automated>cd system1-measurement-source && grep -q 'measure   = "ieee33.measure:main"' pyproject.toml && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['scripts']['measure']=='ieee33.measure:main'; assert set(d['project']['scripts'])=={'ingest','sim','validate','fault-sim','measure'}; print('scripts OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ieee33.measure:main" pyproject.toml` == 1
    - The four existing script entries (ingest/sim/validate/fault-sim) are unchanged (still present)
    - `tomllib.load` parses the file without error and the assertion prints `scripts OK`
  </acceptance_criteria>
  <done>`uv run measure` is registered to `ieee33.measure:main`; no dependency or other-table changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only a pure-constants module + a toml script line. No I/O, no network, no input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | measure_config.py constants | accept | Local-only study repo; constants are source-controlled in git; no external write path. No mitigation needed. |
| T-09-02 | Information disclosure | (none) | accept | No secrets in this file (InfluxDB dev token stays in config.py, local-only, matches existing System 1). |
</threat_model>

<verification>
- `uv run python -c "from ieee33 import measure_config"` succeeds (module valid)
- `pyproject.toml` parses; `measure` script registered alongside the four existing entries
- All D-04/D-05/D-11/D-14 values present and exact (assertion command in Task 1 verify)
</verification>

<success_criteria>
- measure_config.py is a pure-constants module exposing CLASS_SIGMA, CADENCE, SCENARIOS, ACTIVE,
  QUANT_LSB, and the meas schema vocabulary, all with the exact locked values
- `measure` entry point registered; existing scripts untouched; no new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/09-measurement-system-observability-layer-configurable-sensor-m/09-01-SUMMARY.md`
</output>
