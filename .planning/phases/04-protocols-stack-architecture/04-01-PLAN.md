---
phase: 04-protocols-stack-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-protocols-stack-architecture/notes/STK-01-protocol-stack.md
  - .planning/phases/04-protocols-stack-architecture/notes/STK-02-iec-61850.md
autonomous: true
requirements: [STK-01, STK-02]

must_haves:
  truths:
    - "Juan can place SCADA, DNP3, PMU/C37.118, IEC 61850, and LoRa (and MQTT) in the correct field-to-cloud tier and state one key property of each"
    - "Juan can describe IEC 61850 GOOSE vs SV vs MMS, the three-tier station/bay/process hierarchy, and at least three named logical nodes (XCBR/MMXU/CSWI/PTOC/PDIS) without notes"
    - "Each note opens with the For:/Purpose: header, uses numbered sections, carries a '<3-min say-aloud' track, and ends with a boxed '→ Bridge to your work' callout (Modbus→DNP3, Zigbee→LoRa for STK-01)"
    - "Honors CONTEXT.md D-01 (lift+tighten from CLAUDE.md Category 3), D-02 (logical-node gap fill), D-03 (one note file per requirement in notes/), D-07 (oral-rehearsal style), D-08 (per-note bridge), D-10 (markdown + selective LaTeX)"
  artifacts:
    - path: ".planning/phases/04-protocols-stack-architecture/notes/STK-01-protocol-stack.md"
      provides: "Protocol tier map: SCADA/OPC-UA, DNP3, PMU/C37.118, IEC 61850, LoRa, MQTT each placed in tier with one key property; Modbus→DNP3 and Zigbee→LoRa bridge"
      contains: "C37.118"
    - path: ".planning/phases/04-protocols-stack-architecture/notes/STK-02-iec-61850.md"
      provides: "IEC 61850 deep note: GOOSE(<4ms L2)/SV(process bus)/MMS(TCP client-server), process/bay/station three-tier hierarchy, ≥3 named logical nodes with one-liners, command flow"
      contains: "XCBR"
  key_links:
    - from: "STK-01-protocol-stack.md"
      to: "Modbus (Juan has) → DNP3 (gap)"
      via: "boxed bridge pivot"
      pattern: "Bridge to your work"
    - from: "STK-02-iec-61850.md"
      to: "GOOSE/SV/MMS transport split"
      via: "Layer-2 vs TCP/IP distinction"
      pattern: "GOOSE"
---

<objective>
Produce the two grid-protocol study notes for Phase 4: STK-01 (the field-to-cloud protocol
tier map — SCADA, DNP3, PMU/C37.118, IEC 61850, LoRa, MQTT, one property each) and STK-02
(IEC 61850 internals — GOOSE vs SV vs MMS, three-tier hierarchy, ≥3 named logical nodes).
STK-02 is the single highest-yield gap-fill of the phase (logical-node names are the
criterion-2 specific Juan cannot fake).

Purpose: Give Juan the grid-protocol vocabulary to place every named protocol in the correct
tier with one property each (STK-01, criterion 1) and to discuss IEC 61850 fluently —
naming GOOSE/SV/MMS roles, the station/bay/process hierarchy, and at least three logical
nodes without notes (STK-02, criterion 2).

Output: Two markdown notes under notes/ Juan reads and rehearses aloud, each with the
For:/Purpose: header, a "<3-min say-aloud" track, and a boxed "→ Bridge to your work" pivot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocols-stack-architecture/04-CONTEXT.md
@.planning/phases/04-protocols-stack-architecture/04-RESEARCH.md
@.planning/phases/02-transmission-virtual-sensing/notes/TVS-04-asset-health.md

This is an INTERVIEW-PREP project. "Executing" means authoring accurate, interview-ready
study notes Juan reads and rehearses aloud — NOT building software. Acceptance is "the note
exists, covers the required points accurately, and follows the rehearsal style." Draw all
facts VERBATIM from 04-RESEARCH.md (do not improvise protocol facts). Mirror the note STYLE
of the Phase 2 exemplar TVS-04-asset-health.md exactly: For:/Purpose: header, numbered
mental-model-first sections, a "<3-min say-aloud" track, a boxed "→ Bridge to your work"
callout (blockquote + comparison table + "How to say this in the interview" pivot), a
Quick-Recall Card, and a closing italic Sources line. Markdown prose + tables carry these
notes (D-10) — use LaTeX-in-markdown only where a formula genuinely helps (e.g. a C37.118
reporting-rate note). Prefer "current stable" framing over hard version numbers per the
RESEARCH.md version corrections (do NOT propagate CLAUDE.md's stale numbers).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write STK-01 protocol-stack tier-map note</name>
  <files>.planning/phases/04-protocols-stack-architecture/notes/STK-01-protocol-stack.md</files>
  <read_first>
    - .planning/phases/04-protocols-stack-architecture/04-RESEARCH.md section "Protocol Tier Map (STK-01)" (the six-row protocol table: SCADA+OPC-UA, DNP3, PMU/IEEE C37.118, IEC 61850, LoRa/LoRaWAN, MQTT — each with field-to-cloud tier, one key property, transport; the IEEE C37.118 reporting-rate detail; the DNP3-vs-Modbus distinction)
    - .planning/phases/04-protocols-stack-architecture/04-CONTEXT.md sections D-01, D-03, D-07, D-08, D-10 and the <specifics> block
    - CLAUDE.md "Category 3: Grid Protocols" (SCADA, DNP3-vs-Modbus table, PMU/C37.118, IEC 61850, LoRa/Zigbee table) and the "Summary Reference Table" rows for DNP3, PMU, LoRa, IEC 61850 (raw material for the bridge callout)
    - .planning/phases/02-transmission-virtual-sensing/notes/TVS-04-asset-health.md (the literal style template: For:/Purpose: header, numbered sections, "<3-min say-aloud" track, boxed "→ Bridge to your work" callout, Quick-Recall Card, Sources line)
  </read_first>
  <action>
    Create notes/STK-01-protocol-stack.md mirroring the TVS-04 style. Header block:
    `# STK-01: Grid Protocol Stack — Field-to-Cloud Tier Map`, then
    `**For:** Oral rehearsal — speak each section aloud before the interview.`,
    `**Purpose:** …` (place each named protocol in the correct field-to-cloud tier with one
    key property so you can navigate the protocol stack in conversation), then `---`.

    Numbered mental-model-first sections, drawing facts VERBATIM from RESEARCH §"Protocol Tier Map":

    (1) **The field-to-cloud mental model** — one paragraph: protocols live in tiers from the
        switchyard up to the control center; the question an interviewer is testing is "where does
        each protocol live and what is its one distinguishing property."
    (2) **The tier map** — reproduce the RESEARCH six-row table with columns Protocol |
        Field-to-Cloud Tier | One Key Property | Transport, covering exactly:
        - **SCADA + OPC-UA** — Station level → cloud/control center; RTUs/IEDs aggregate field data,
          OPC-UA is the integration API bridging SCADA to historians/analytics; TCP/IP.
        - **DNP3** — Field → station (RTU/IED to SCADA master); KEY PROPERTY: millisecond timestamps
          on every data point + unsolicited reporting (RTU pushes on change, no polling); Serial or TCP/IP.
        - **PMU / IEEE C37.118** — Field → regional/cloud via PDC; KEY PROPERTY: GPS-synchronized
          phasors at 10–120 frames/sec (30/60 common for 60 Hz), enables wide-area state estimation; TCP/IP streaming.
        - **IEC 61850** — process bus (GOOSE/SV) + station bus (MMS); KEY PROPERTY: GOOSE <4 ms L2
          protection trips, SV digitized I/V process bus, MMS client-server SCADA supervision; L2 Ethernet (GOOSE/SV) + TCP/IP (MMS).
        - **LoRa / LoRaWAN** — Field → wide-area (cloud via network server); KEY PROPERTY: 2–15 km
          range on battery power (5–10 years), star-of-stars topology, 0.3–50 kbps; LoRa RF → gateway → TCP.
        - **MQTT** — Field → edge broker (device-to-broker); KEY PROPERTY: lightweight pub/sub, QoS
          0/1/2, star topology through broker, no native timestamping; TCP/IP.
    (3) **IEEE C37.118 reporting-rate detail** — DEEP one-liner: baseline 10/30/60 fps for 60 Hz
        (user-selectable), 120 fps common in high-resolution WAMS; GPS timestamp on every frame
        enables cross-system phase comparison. (LaTeX only if a rate formula genuinely helps; prose is fine.)
    (4) **DNP3 vs Modbus — the distinction that powers the bridge** — Modbus has no native
        timestamps and is poll-only; DNP3 adds millisecond timestamps on every object + unsolicited
        reporting, three-layer architecture (app/transport/data-link) vs Modbus two-layer, and DNP3
        SAv5 for authenticated comms.

    Then add (placement top or bottom is Claude's discretion, D-07):
    - A `## <3-min say-aloud version` `> blockquote` walking the tiers bottom-to-top and naming the
      one key property of each protocol (hit DNP3 timestamps, C37.118 GPS phasors, IEC 61850 GOOSE
      <4 ms, LoRa km-range battery, MQTT lightweight device pub/sub).
    - A `## → Bridge to your work` box (TVS-04 shape: `> blockquote` + comparison table mapping grid
      protocol ↔ Juan's experience + a `**How to say this in the interview:**` `> blockquote` pivot).
      Use TWO bridges from CLAUDE.md's Summary Reference Table: **Modbus (Juan has) → DNP3** ("Modbus
      without timestamps; DNP3 adds timestamps + unsolicited push") and **Zigbee (Juan has) → LoRa**
      ("Zigbee indoors; LoRa for km-range field sensors").
    - A `## Quick-Recall Card (Recite Before the Interview)` numbered list ending with `**My bridge:**`.
    - A closing `*Sources: …*` italic line (CLAUDE.md Category 3, IEEE C37.118, DNP3, IEC 61850, the CV).

    Cover all four numbered sections fully. Do NOT produce a "v1"/abbreviated note. Do NOT deep-dive
    LoRa RF engineering or IEC 61850 SCL/CID (awareness only per CLAUDE.md "What NOT to Over-Invest In").
    Do NOT add an aggregate vocabulary-bridge table (that is Phase 6, D-09) — the per-note box only.
  </action>
  <verify>
    <automated>F=.planning/phases/04-protocols-stack-architecture/notes/STK-01-protocol-stack.md; test -f "$F" && grep -q 'C37.118' "$F" && grep -q 'DNP3' "$F" && grep -qi 'LoRa' "$F" && grep -q 'GOOSE' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && grep -q 'For:' "$F" && grep -q 'Purpose:' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "C37.118" (PMU tier), "DNP3", "LoRa", and "GOOSE" (the named protocols);
    contains the "Bridge to your work" heading; contains a "3-min"/"say-aloud" track; contains the
    "For:" and "Purpose:" header tokens. Command prints OK.
  </acceptance_criteria>
  <done>STK-01 note exists with the For:/Purpose: header, the six-protocol tier map (SCADA/OPC-UA, DNP3, PMU/C37.118, IEC 61850, LoRa, MQTT each with tier + one key property + transport), the C37.118 reporting-rate detail, the DNP3-vs-Modbus distinction, a "<3-min say-aloud" track, the boxed Modbus→DNP3 and Zigbee→LoRa bridge, a Quick-Recall Card, and a Sources line.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write STK-02 IEC 61850 note (GOOSE/SV/MMS, three-tier hierarchy, logical nodes)</name>
  <files>.planning/phases/04-protocols-stack-architecture/notes/STK-02-iec-61850.md</files>
  <read_first>
    - .planning/phases/04-protocols-stack-architecture/04-RESEARCH.md section "IEC 61850 Deep-Dive (STK-02 Gap Fill — Highest Priority)" (the three-tier process/bay/station table; the GOOSE/SV/MMS roles table with latency + transport; the five logical-node table XCBR/MMXU/CSWI/PTOC/PDIS with group letters + one-liners; the bonus nodes LLN0/LPHD/TCTR/TVTR; the 13 LN functional groups; the command-flow-to-say-aloud) AND "Common Pitfalls" Pitfalls 1, 2, 6
    - .planning/phases/04-protocols-stack-architecture/04-CONTEXT.md sections D-02 (logical-node gap is criterion-2), D-03, D-07, D-08, D-10 and the <specifics> "Lead with the gap-fills" block
    - CLAUDE.md "Category 3: Grid Protocols" → "IEC 61850 (MEDIUM AWARENESS)" sub-block (GOOSE, SV, MMS, CIM one-liners — the base CLAUDE.md covers; note it has NO logical-node names, which this note fills)
    - .planning/phases/02-transmission-virtual-sensing/notes/TVS-04-asset-health.md (the literal style template: For:/Purpose: header, numbered sections, "<3-min say-aloud" track, boxed "→ Bridge to your work" callout, Quick-Recall Card, Sources line)
  </read_first>
  <action>
    Create notes/STK-02-iec-61850.md mirroring the TVS-04 style. Header block:
    `# STK-02: IEC 61850 — GOOSE / SV / MMS, Hierarchy & Logical Nodes`, then
    `**For:** Oral rehearsal — speak each section aloud before the interview.`,
    `**Purpose:** …` (be able to describe GOOSE vs SV vs MMS, the three-tier hierarchy, and at
    least three named logical nodes without notes — the criterion-2 specifics you cannot fake),
    then `---`.

    Add a `>` depth-strategy callout near the top (à la TVS-04): DEEP for the logical-node names,
    GOOSE/SV/MMS roles, and the three-tier hierarchy (criterion-named); AWARENESS only — do NOT
    cover SCL/CID config files (per CLAUDE.md "What NOT to Over-Invest In").

    Numbered mental-model-first sections, drawing facts VERBATIM from RESEARCH §"IEC 61850 Deep-Dive":

    (1) **Three-tier hierarchy (process / bay / station)** — DEEP. Reproduce the RESEARCH table:
        - **Process level** (bottom): circuit breakers, CTs/VTs, merging units, actuators →
          GOOSE (trips, interlocking) + SV (digitized I/V samples).
        - **Bay level** (middle): IEDs — protection relays, bay controllers; receives GOOSE+SV from
          process, sends MMS up to station.
        - **Station level** (top): SCADA HMI, engineering workstation, gateway/RTU to control center
          → MMS (client-server supervision, setpoints, reports).
        Include the interview one-liner verbatim: "Process level is the switchyard — GOOSE and SV
        never leave the LAN. Bay level is the protection relay and bay controller that act on them.
        Station level is the SCADA HMI that talks MMS over TCP to the control center."
    (2) **GOOSE vs SV vs MMS roles** — DEEP. Reproduce the RESEARCH services table with columns
        Service | Full Name | Layer | Latency | Direction | Use Case:
        - **GOOSE** — Generic Object Oriented Substation Event; Ethernet Layer 2 (no IP); **< 4 ms**;
          publisher → multicast subscribers; protection trips, interlocks, breaker status.
        - **SV** — Sampled Values; Ethernet Layer 2 (no IP); sub-ms high rate; merging unit → IED;
          digitized current+voltage on process bus.
        - **MMS** — Manufacturing Message Specification (ISO 9506); TCP/IP; seconds (supervisory);
          client (SCADA) ↔ server (IED); read measurements, write setpoints, retrieve logs.
        State the key structural fact verbatim (Pitfall 2): "GOOSE and SV are Layer 2 — they never
        leave the substation LAN and are never routed. MMS is TCP/IP and is routable, so that's the
        path from substation to control center." Add the GOOSE retransmission mechanism (~2 ms
        intervals on state change, backing off to slow heartbeat; reliability via repetition, not
        acknowledgment → deterministic latency) and the <4 ms protection-class fact (Pitfall 1).
    (3) **Logical nodes — the criterion-2 gap** — DEEP. State: all LN class names are exactly four
        letters; the first letter is the functional group. Reproduce the five-node table (LN Name |
        Group | Full Name/Function | Interview One-Liner) for **XCBR** (X-Switchgear, circuit breaker,
        carries Pos/OpCnt), **MMXU** (M-Measurement, three-phase V/I/power/PF/freq — most-used),
        **CSWI** (C-Control, switch controller — open/close from SCADA, verifies interlock then
        commands XCBR), **PTOC** (P-Protection, time overcurrent / IEEE device 50/51, Op=trip Str=start),
        **PDIS** (P-Protection, distance protection / IEEE device 21, fault-impedance zone trigger).
        Add a short "bonus nodes" line (LLN0 = root LN hosting GOOSE control blocks/datasets;
        LPHD = physical device health; TCTR/TVTR = CT/VT models on process bus) and name the 13 LN
        functional groups at AWARENESS level (L/P/X/M/C/R/A/S/T/Y/Z/I/G).
    (4) **The command flow (ties it together)** — DEEP, say-aloud. Reproduce verbatim: "Dispatcher
        issues 'open breaker' → hits CSWI (controller) → CSWI verifies interlocking via CILO → sends
        operate command to XCBR (the breaker LN) → XCBR changes Pos and emits a GOOSE message → other
        IEDs receive the GOOSE in <4 ms."
    (5) **Pitfall guard (criterion clarity)** — AWARENESS. Note Pitfall 6: the IEC 61850 three-tier
        hierarchy (process/bay/station) describes the SUBSTATION; it is NOT the STK-05 four-tier
        field/edge/fog/cloud architecture — say which hierarchy you mean when you speak.

    Then add (placement Claude's discretion, D-07):
    - A `## <3-min say-aloud version` `> blockquote` hitting: three tiers (process/bay/station),
      GOOSE(<4 ms L2)/SV(process bus)/MMS(TCP routable), and at least three logical-node names with
      a one-liner each, ending on the CSWI→XCBR→GOOSE command flow.
    - A `## → Bridge to your work` box (TVS-04 shape). Content: Juan's MQTT/pub-sub instinct maps to
      GOOSE's publish/multicast model, but GOOSE is hard-real-time Layer-2 deterministic protection
      where his pub/sub was application-tier — frame it as "I know publish/subscribe; IEC 61850's
      GOOSE is that pattern pushed to <4 ms Layer-2 determinism for protection, and MMS is the
      routable SCADA channel I'd integrate at the station level." Include the comparison table + the
      "How to say this in the interview" pivot.
    - A `## Quick-Recall Card (Recite Before the Interview)` numbered list — MUST include the five LN
      names (XCBR/MMXU/CSWI/PTOC/PDIS) and the GOOSE/SV/MMS layer+latency triplet — ending with `**My bridge:**`.
    - A closing `*Sources: …*` italic line (IEC TR 61850-10-3:2022 PDF in docs/, scadaprotocols.com,
      CLAUDE.md Category 3, the CV).

    Cover all five numbered sections fully. Do NOT cover SCL/CID config files (awareness exclusion).
    Do NOT add an aggregate vocabulary-bridge table (Phase 6, D-09).
  </action>
  <verify>
    <automated>F=.planning/phases/04-protocols-stack-architecture/notes/STK-02-iec-61850.md; test -f "$F" && grep -q 'XCBR' "$F" && grep -q 'MMXU' "$F" && grep -q 'CSWI' "$F" && grep -q 'GOOSE' "$F" && grep -q 'MMS' "$F" && grep -qE 'Sampled Value|[^A-Za-z]SV[^A-Za-z]' "$F" && grep -qiE '<\s*4\s*ms|< 4 ms|<4 ms' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains at least three logical-node names ("XCBR", "MMXU", "CSWI"); contains the
    "GOOSE", "MMS", and Sampled-Values/"SV" service names; contains the "<4 ms" GOOSE latency;
    contains the "Bridge to your work" heading; contains a "3-min"/"say-aloud" track. Command prints OK.
  </acceptance_criteria>
  <done>STK-02 note exists with the For:/Purpose: header, the process/bay/station three-tier hierarchy, the GOOSE(<4 ms L2)/SV(process bus)/MMS(TCP client-server) roles table with the Layer-2-vs-routable distinction, ≥3 named logical nodes (XCBR/MMXU/CSWI plus PTOC/PDIS) with one-liners, the CSWI→XCBR→GOOSE command flow, the three-tier-vs-four-tier pitfall guard, a "<3-min say-aloud" track, the boxed bridge, a Quick-Recall Card naming the five LNs, and a Sources line.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only phase, no runtime/attack surface. This plan produces static markdown
study notes Juan reads aloud: no executable, deployable, or network-facing artifact, no user
input, no secrets, no data handling. STRIDE threat modeling does not apply.
</threat_model>

<verification>
- Both note files exist under notes/ with the required greppable tokens and headings.
- STK-01 places all six protocols (SCADA/OPC-UA, DNP3, PMU/C37.118, IEC 61850, LoRa, MQTT) in tiers with one key property each, plus the Modbus→DNP3 and Zigbee→LoRa bridge.
- STK-02 names ≥3 logical nodes (XCBR/MMXU/CSWI...), the GOOSE/SV/MMS roles with the <4 ms GOOSE latency and Layer-2-vs-routable split, and the three-tier hierarchy.
- Each note carries the For:/Purpose: header, a "<3-min say-aloud" track, and a "→ Bridge to your work" box (D-07, D-08).
</verification>

<success_criteria>
Juan can read each note aloud and, without notes, (1) place SCADA/DNP3/PMU-C37.118/IEC 61850/LoRa
in the correct tier and state one key property of each (STK-01 / criterion 1), and (2) describe
GOOSE vs SV vs MMS, the three-tier hierarchy, and at least three logical-node names (STK-02 /
criterion 2) — each with its Modbus→DNP3 / Zigbee→LoRa / pub-sub→GOOSE bridge ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocols-stack-architecture/04-01-SUMMARY.md`
</output>
</content>
</invoke>
