---
phase: 04-protocols-stack-architecture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-protocols-stack-architecture/notes/STK-05-reference-architecture.md
autonomous: true
requirements: [STK-05]

must_haves:
  truths:
    - "Juan can draw and narrate the four-tier reference architecture (field → edge → fog/federated → cloud) on a whiteboard from memory, including control-latency bands and key components per tier"
    - "The doc carries BOTH an ASCII diagram (the whiteboard-rehearsal target) AND a Mermaid diagram, plus a numbered narration script"
    - "The doc includes a short AGMS overlay mapping the director's patent components (Field Agent Devices/Scout Command → edge; Operation Loop simulate-before-commit/GWCH → fog; GWM → cloud) onto the tiers, cross-linked to Phase 3 (not re-derived)"
    - "Honors CONTEXT.md D-03 (standalone STK-05 doc in notes/), D-05 (both ASCII + Mermaid + narration script), D-06 (dual-layer: Juan's stack annotated + short AGMS overlay), D-07 (oral-rehearsal style), D-08 (bridge callout), D-10 (markdown)"
  artifacts:
    - path: ".planning/phases/04-protocols-stack-architecture/notes/STK-05-reference-architecture.md"
      provides: "Standalone whiteboard four-tier reference architecture: ASCII diagram, Mermaid diagram, four tiers with latency bands + key components + Juan's stack, numbered narration script, AGMS overlay table"
      contains: "fog"
  key_links:
    - from: "STK-05-reference-architecture.md (edge tier)"
      to: "K3s + NATS JetStream + EKF/virtual-sensing"
      via: "tier component annotation"
      pattern: "K3s"
    - from: "STK-05-reference-architecture.md (AGMS overlay)"
      to: "Phase 3 patents INDEX.md"
      via: "patent-component-to-tier mapping cross-link"
      pattern: "Operation Loop"
---

<objective>
Produce STK-05, the SIGNATURE deliverable of Phase 4: a standalone, whiteboard-able four-tier
reference-architecture doc (field → edge → fog/federated → cloud) Juan can draw and narrate
from memory. It carries BOTH an ASCII diagram (the physical-whiteboard rehearsal target) AND a
Mermaid diagram (polished reference), a numbered narration script (latency tiers + key
components per layer), and a short AGMS overlay mapping the director's patent components onto
the tiers — making STK-05 do double duty as a Phase-3 patent-connection power move.

Purpose: Equip Juan to satisfy success criterion 5 — draw AND narrate the four-tier
architecture, including control-latency tiers and key components at each level — and to land
the AGMS overlay as a differentiation move that ties his architecture fluency to the director's
own patented work.

Output: One standalone markdown doc under notes/ that is both a study artifact and the literal
rehearsal target (ASCII = what he physically draws), with a narration script and AGMS overlay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocols-stack-architecture/04-CONTEXT.md
@.planning/phases/04-protocols-stack-architecture/04-RESEARCH.md
@.planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md
@.planning/research/patents/INDEX.md

This is an INTERVIEW-PREP project. "Executing" means authoring an accurate, interview-ready
architecture doc Juan studies and rehearses aloud (and draws on a whiteboard) — NOT building
software. Acceptance is "the doc exists, contains both diagrams + narration + AGMS overlay
accurately, and follows the rehearsal style." Draw the tier content, latency bands, component
lists, the ready-made ASCII diagram, the AGMS overlay table, and the narration script elements
VERBATIM from 04-RESEARCH.md §"Four-Tier Architecture Design". Mirror the note STYLE of the
Phase 2 exemplar TVS-04-asset-health.md (For:/Purpose: header, numbered sections, "<3-min
say-aloud" track, boxed "→ Bridge to your work" callout, Quick-Recall Card, Sources line),
adapted to an architecture doc. Markdown (D-10). Keep the AGMS overlay SHORT (6–8 lines + small
table) — reference Phase 3 patents/INDEX.md for depth, do not re-derive it.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scan intelligrid.pdf for tier terminology, then write STK-05 four-tier reference-architecture doc</name>
  <files>.planning/phases/04-protocols-stack-architecture/notes/STK-05-reference-architecture.md</files>
  <read_first>
    - .planning/phases/04-protocols-stack-architecture/04-RESEARCH.md section "Four-Tier Architecture Design (STK-05)" IN FULL — the four-tier spine with latency bands + protocols + hardware + control per tier (FIELD/EDGE/FOG-FEDERATED/CLOUD), the AGMS overlay table (six patent components → tier + rationale), the READY-MADE ASCII box diagram (copy it), and the five numbered narration-script elements — AND "Common Pitfalls" Pitfall 6 (three-tier IEC 61850 ≠ four-tier architecture) AND "Open Questions" #1 (intelligrid.pdf not yet read — scan pages 1–20 for tier terminology)
    - docs/intelligrid.pdf — scan the FIRST ~20 pages (use the Read tool with pages:"1-20") to confirm IntelliGrid's tier naming and check whether it names the tiers differently or adds a fifth tier; if it surfaces an impressive IntelliGrid-specific term for a tier, fold it in as an aside (do not restructure the four-tier model — it is locked by D-05/D-06)
    - .planning/phases/04-protocols-stack-architecture/04-CONTEXT.md sections D-03, D-05, D-06, D-07, D-08, D-10 and the <specifics> "STK-05 is the signature deliverable" block
    - .planning/research/patents/INDEX.md — the AGMS component map (Field Agent Devices/scouts, Scout Command, ORACS operating cells, Operation Loop Formation simulate-before-commit, GridWideCommandHub/GWCH, GridWideMind/GWM) for the overlay table + cross-link target
    - .planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md (the literal style template)
  </read_first>
  <action>
    STEP A — Scan docs/intelligrid.pdf pages 1–20 (Read tool, pages:"1-20"). Note any IntelliGrid
    tier terminology. If IntelliGrid names a tier with an impressive standard term, add it as a
    one-line aside in the relevant tier; if it materially contradicts the four-tier model, surface
    that in the SUMMARY rather than silently changing the locked structure (D-05/D-06). If the scan
    yields nothing useful, proceed with the RESEARCH four-tier model unchanged and note "IntelliGrid
    scan: no tier-naming change needed" in the SUMMARY.

    STEP B — Create notes/STK-05-reference-architecture.md mirroring the TVS-04 style, adapted to an
    architecture doc. Header block:
    `# STK-05: Four-Tier Reference Architecture (Field → Edge → Fog/Federated → Cloud)`, then
    `**For:** Whiteboard rehearsal — practice drawing the ASCII diagram from memory, then narrate it
    aloud.`, `**Purpose:** …` (draw and narrate the four-tier architecture with control-latency tiers
    and key components per layer — criterion 5), then `---`.

    Numbered sections, content VERBATIM from RESEARCH §"Four-Tier Architecture Design":

    (1) **The ASCII diagram (whiteboard target)** — DEEP. Reproduce the ready-made four-layer ASCII
        box diagram from RESEARCH (CLOUD on top → FOG/FEDERATED → EDGE → FIELD on bottom, with the
        NATS JetStream hub/leaf-node connectors between tiers and the IEC 61850 GOOSE/SV·DNP3·C37.118·
        LoRa connector into the field tier, and the `← AGMS` annotations). This is the artifact Juan
        physically draws — keep it clean and copyable in a fenced ``` block.
    (2) **The Mermaid diagram** — DEEP. Add an equivalent Mermaid `flowchart TB` (or graph TD) in a
        ```mermaid fenced block: four nodes Field → Edge → Fog/Federated → Cloud with the inter-tier
        protocol/transport labels on the edges (IEC 61850/DNP3/C37.118/LoRa into field; NATS
        JetStream leaf node field-side→edge→fog; NATS hub fog→cloud). Polished on-screen reference.
    (3) **The four tiers in detail** — DEEP. One subsection per tier, each stating the LATENCY BAND,
        KEY COMPONENTS, and the CONTROL characteristic, VERBATIM from RESEARCH:
        - **TIER 1 — FIELD** (<4 ms GOOSE protection to ~100 ms DNP3 unsolicited): IEC 61850 GOOSE+SV,
          DNP3, IEEE C37.118, LoRa, Modbus; XCBR/merging units/PMUs/RTUs/LoRa sensors; hardwired
          protection + GOOSE, no software round-trip.
        - **TIER 2 — EDGE / K3s substation node** (100 ms – 1 s inference): K3s, NATS JetStream leaf
          node, EKF/virtual-sensing FastAPI service, Prometheus node-exporter, local InfluxDB buffer
          for island-mode; EKF state estimation, virtual temperature sensing, local reactive-power
          compensation; operates autonomously without WAN (island mode).
        - **TIER 3 — FOG / FEDERATED** (seconds–minutes): NATS JetStream hub, federated learning
          coordinator (Flower — Phase 5, named only), cross-substation state aggregation; aggregate
          EKF outputs, federated weight exchange (Phase 5 depth), cross-substation voltage profile.
          EXPLICITLY note this tier is NAMED here; depth is Phase 5 (FED-01/FED-02).
        - **TIER 4 — CLOUD** (minutes–hours): Kafka ingestion, OSIsoft PI / InfluxDB historian, ML
          training (Azure ML), GitOps fleet config push to K3s, Grafana/PromQL dashboards,
          Alertmanager; long-term retention, model training/versioning, fleet config; no protective
          control from here.
        Add the Pitfall 6 guard: this four-tier architecture is NOT the IEC 61850 three-tier
        (process/bay/station) hierarchy — field tier ≈ IEC 61850 process+bay; say which you mean.
    (4) **Numbered narration script** — DEEP, the "narrate it" half. Reproduce the five numbered
        narration elements from RESEARCH (1: field tier — XCBR, PMUs at 30–120 fps, LoRa, GOOSE <4 ms
        no round-trip; 2: edge tier — K3s node, EKF sub-second, NATS leaf node + island mode; 3:
        fog/federated — cross-substation aggregation, Phase 5 owns the algorithm depth; 4: cloud —
        Kafka/PI/Azure ML/GitOps/Grafana, no protective control; 5: AGMS overlay narration). This is
        the say-aloud spine Juan rehearses.
    (5) **AGMS overlay** — DEEP but SHORT (6–8 lines + the small table). Reproduce the overlay table
        (AGMS Patent Component | Maps to Tier | Rationale): Field Agent Devices running scouts → Edge;
        Scout Command (1441→1447) → Edge (deployment); ORACS operating cells (island mode) → Edge;
        Operation Loop Formation simulate-before-commit → Fog; GridWideCommandHub (GWCH) federation →
        Fog; GridWideMind (GWM) alert correlation + learning → Cloud. Add ONE cross-link line pointing
        to `.planning/research/patents/INDEX.md` for depth ("reference, don't re-derive" per D-06).

    Then add:
    - A `## <3-min say-aloud version` `> blockquote` — the compressed bottom-to-top tier walk
      (field <4 ms GOOSE → edge K3s/NATS/EKF sub-second island mode → fog cross-substation
      seconds–minutes → cloud Kafka/historian/GitOps minutes+), closing with the one-line AGMS
      overlay sentence.
    - A `## → Bridge to your work` box (TVS-04 shape): the whole edge tier IS Juan's production stack
      — "I have run this edge tier in production: full Kubernetes (→ K3s here), MQTT (→ NATS JetStream),
      InfluxDB+Grafana (→ Prometheus). The architecture isn't theoretical for me — I'd swap each
      component for its edge-optimized peer and the EKF engine is the Phase-1 virtual-sensing demo I
      built." Include a comparison table (each tier component ↔ Juan's analog) + a "How to say this in
      the interview" pivot.
    - A `## Quick-Recall Card (Recite Before the Interview)` numbered list — MUST include the four
      tier names with their latency bands and the AGMS-tier mapping — ending with `**My bridge:**`.
    - A closing `*Sources: …*` italic line (RESEARCH four-tier design, IntelliGrid reference
      docs/intelligrid.pdf, IEC 61850 process/bay/station model, Phase 3 patents INDEX.md, the CV).

    Cover all five numbered sections fully. Keep the AGMS overlay short (reference, not re-derive).
    Do NOT deep-dive federated algorithms or edge security (Phase 5 — the fog tier NAMES the concept
    only). Do NOT add an aggregate vocabulary-bridge table (Phase 6, D-09).
  </action>
  <verify>
    <automated>F=.planning/phases/04-protocols-stack-architecture/notes/STK-05-reference-architecture.md; test -f "$F" && grep -qi 'field' "$F" && grep -qi 'edge' "$F" && grep -qi 'fog' "$F" && grep -qi 'cloud' "$F" && grep -q 'K3s' "$F" && grep -q 'JetStream' "$F" && grep -q '```mermaid' "$F" && grep -qE '<\s*4\s*ms|< 4 ms|<4 ms' "$F" && grep -qi 'Operation Loop' "$F" && grep -qi 'GWM\|GridWideMind' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains all four tier names ("field", "edge", "fog", "cloud"); contains the edge
    stack ("K3s", "JetStream"); contains a ```mermaid fenced block (the Mermaid diagram); contains
    the "<4 ms" field-tier latency; contains the AGMS overlay markers ("Operation Loop" and
    "GWM"/"GridWideMind"); contains the "Bridge to your work" heading and a "3-min"/"say-aloud"
    track. Command prints OK. (The ASCII diagram is verified by manual review during execution.)
  </acceptance_criteria>
  <done>STK-05 doc exists with the For:/Purpose: header, the ASCII four-layer box diagram (whiteboard target), an equivalent Mermaid diagram, the four tiers each with latency band + key components + Juan's stack + control characteristic, the Pitfall-6 three-tier-vs-four-tier guard, a numbered narration script, a SHORT AGMS overlay table cross-linked to Phase 3 INDEX.md, a "<3-min say-aloud" track, the boxed bridge, a Quick-Recall Card, and a Sources line. The intelligrid.pdf pages 1–20 were scanned and any tier-terminology finding folded in or noted in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only phase, no runtime/attack surface. This plan produces a static markdown
architecture/study doc Juan reads aloud and draws on a whiteboard: no executable, deployable, or
network-facing artifact, no user input, no secrets, no data handling. STRIDE threat modeling does
not apply.
</threat_model>

<verification>
- The STK-05 doc exists under notes/ with the required greppable tokens and headings.
- It contains BOTH an ASCII four-layer box diagram AND a ```mermaid diagram (D-05).
- It states all four tiers (field/edge/fog/cloud) with latency bands and key components, and annotates Juan's own stack at the edge tier (K3s, NATS JetStream, EKF, Prometheus) (D-06a).
- It contains the short AGMS overlay (Operation Loop / GWM / Scout Command tier mapping) cross-linked to Phase 3 (D-06b).
- It carries the For:/Purpose: header, a numbered narration script, a "<3-min say-aloud" track, and a "→ Bridge to your work" box (D-05, D-07, D-08).
- docs/intelligrid.pdf pages 1–20 were scanned before finalizing (RESEARCH Open Question 1).
</verification>

<success_criteria>
Juan can draw the ASCII four-tier diagram from memory on a whiteboard and narrate it aloud —
naming each tier's control-latency band and key components (field <4 ms GOOSE / edge 100 ms–1 s
K3s+NATS+EKF / fog seconds–minutes / cloud minutes+) and the AGMS overlay (Field Agent Devices
→ edge, Operation Loop simulate-before-commit → fog, GWM → cloud) — satisfying STK-05 / criterion 5.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocols-stack-architecture/04-03-SUMMARY.md`
</output>
</content>
</invoke>
