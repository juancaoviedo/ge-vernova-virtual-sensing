---
phase: 04-protocols-stack-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-protocols-stack-architecture/notes/STK-03-messaging-orchestration.md
  - .planning/phases/04-protocols-stack-architecture/notes/STK-04-observability.md
autonomous: true
requirements: [STK-03, STK-04]

must_haves:
  truths:
    - "Juan can justify NATS JetStream vs MQTT (and vs Kafka) for the substation edge, and state the three K3s-vs-K8s distinctions (air-gap, memory, etcd/SQLite)"
    - "Juan can explain the Prometheus pull model vs InfluxDB push, recite a concrete PromQL query, and name what kube-prometheus-stack adds (Prometheus Operator, node-exporter, kube-state-metrics, Grafana, Alertmanager)"
    - "Each note opens with the For:/Purpose: header, uses numbered sections, carries a '<3-min say-aloud' track, and ends with a boxed '→ Bridge to your work' callout (MQTT→NATS / full K8s→K3s for STK-03; InfluxDB+Grafana→Prometheus/PromQL for STK-04)"
    - "Honors CONTEXT.md D-01 (lift+tighten from CLAUDE.md Categories 1/2/4), D-02 (NATS-over-Kafka justification + kube-prometheus-stack name + recitable PromQL gap fills), D-03, D-07, D-08, D-10"
  artifacts:
    - path: ".planning/phases/04-protocols-stack-architecture/notes/STK-03-messaging-orchestration.md"
      provides: "Edge stack positioning: NATS JetStream vs MQTT vs Kafka (vs Pulsar awareness) substation-edge justification with the Kafka JVM/64–128 GB quote; three K3s-vs-K8s distinctions; MQTT→NATS and K8s→K3s bridge"
      contains: "JetStream"
    - path: ".planning/phases/04-protocols-stack-architecture/notes/STK-04-observability.md"
      provides: "Observability: Prometheus pull vs InfluxDB push; a recitable PromQL query; the five kube-prometheus-stack components; InfluxDB+Grafana→Prometheus bridge"
      contains: "kube-prometheus-stack"
  key_links:
    - from: "STK-03-messaging-orchestration.md"
      to: "Kafka edge unsuitability (JVM + 64–128 GB RAM)"
      via: "verbatim NATS-docs hardware quote"
      pattern: "64"
    - from: "STK-04-observability.md"
      to: "recitable PromQL query"
      via: "rate() over a counter in a namespace"
      pattern: "rate\\(container_cpu_usage_seconds_total"
---

<objective>
Produce the two software-stack study notes for Phase 4: STK-03 (edge/cloud messaging +
orchestration — NATS JetStream vs MQTT vs Kafka vs Pulsar, and the three K3s-vs-K8s
distinctions) and STK-04 (observability — Prometheus pull vs InfluxDB push, a recitable
PromQL query, what kube-prometheus-stack adds). These fill the two software-stack gap-fills
CLAUDE.md does not cover crisply: the spoken "NATS over Kafka at the edge" justification and
the kube-prometheus-stack name + a recitable PromQL query.

Purpose: Give Juan interview-ready justifications for the edge software stack — why NATS
JetStream over MQTT and over Kafka for the substation edge, the three K3s-vs-K8s distinctions
(air-gap, memory, etcd/SQLite) (STK-03, criterion 3) — and for observability — the
Prometheus pull model vs InfluxDB push, a PromQL query he can recite, and what
kube-prometheus-stack bundles (STK-04, criterion 4).

Output: Two markdown notes under notes/ Juan reads and rehearses aloud, each with the
For:/Purpose: header, a "<3-min say-aloud" track, and a boxed "→ Bridge to your work" pivot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocols-stack-architecture/04-CONTEXT.md
@.planning/phases/04-protocols-stack-architecture/04-RESEARCH.md
@.planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md

This is an INTERVIEW-PREP project. "Executing" means authoring accurate, interview-ready
study notes Juan reads and rehearses aloud — NOT building software. Acceptance is "the note
exists, covers the required points accurately, and follows the rehearsal style." Draw all
facts VERBATIM from 04-RESEARCH.md (do not improvise the hardware quote, version facts, or the
PromQL query). Mirror the note STYLE of the Phase 2 exemplar TVS-04-asset-health.md exactly:
For:/Purpose: header, numbered mental-model-first sections, a "<3-min say-aloud" track, a
boxed "→ Bridge to your work" callout, a Quick-Recall Card, and a closing italic Sources line.
Markdown prose + tables carry these notes (D-10). Prefer "current stable" framing over hard
version numbers in the note body (per RESEARCH.md: NATS is now v2.14.x not 2.11, K3s is
v1.33/1.34 not 1.31, kube-prometheus-stack chart 86.2.3) — do NOT propagate CLAUDE.md's stale
numbers; you MAY mention "current stable" with the verified number in parentheses once.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write STK-03 messaging + orchestration note (NATS vs MQTT/Kafka, K3s vs K8s)</name>
  <files>.planning/phases/04-protocols-stack-architecture/notes/STK-03-messaging-orchestration.md</files>
  <read_first>
    - .planning/phases/04-protocols-stack-architecture/04-RESEARCH.md section "Edge/Cloud Stack Positioning (STK-03 Gap Fill)" — sub-sections "Why NATS JetStream Over MQTT at the Substation Edge" (durable replay / request-reply / JWT accounts / leaf-node topology + the bridge pivot sentence), "Why NATS JetStream Over Kafka at the Substation Edge" (the VERBATIM "eight cores, 64 GB to 128 GB of RAM, two or more 8-TB SAS/SSD disks, 10-Gig NIC" quote + GC-pause point + the bicycle-lane one-liner), and "The Three K3s-vs-K8s Distinctions" (memory, etcd/SQLite, air-gap table) — AND "Common Pitfalls" Pitfall 3 (footprint not throughput) and Pitfall 4 (K3s SQLite-default-but-etcd-available)
    - .planning/phases/04-protocols-stack-architecture/04-CONTEXT.md sections D-01, D-02, D-03, D-07, D-08, D-10 and the <specifics> "NATS over Kafka because Kafka needs a JVM + 64–128 GB" block
    - CLAUDE.md "Category 1: Streaming & Messaging" (NATS/JetStream vs MQTT vs Kafka vs Pulsar tables) and "Category 2: Edge Orchestration" (K3s-vs-K8s table) — the base content to lift+tighten; plus the Summary Reference Table rows for NATS, Kafka, Pulsar, K3s
    - .planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md (the literal style template)
  </read_first>
  <action>
    Create notes/STK-03-messaging-orchestration.md mirroring the TVS-04 style. Header block:
    `# STK-03: Edge Messaging & Orchestration — NATS/MQTT/Kafka, K3s/K8s`, then
    `**For:** Oral rehearsal — speak each section aloud before the interview.`,
    `**Purpose:** …` (justify NATS JetStream vs MQTT and vs Kafka for the substation edge, and
    state the three K3s-vs-K8s distinctions), then `---`.

    Numbered mental-model-first sections, drawing facts VERBATIM from RESEARCH §"Edge/Cloud Stack Positioning":

    (1) **The edge constraint mental model** — one paragraph: a substation edge node is a Pi-class /
        small industrial PC, often air-gapped or island-capable; this constraint, not raw throughput,
        decides the stack. (Pre-empt Pitfall 3: it's footprint, not speed.)
    (2) **NATS JetStream vs MQTT** — DEEP. Four reasons NATS for the federated edge: durable replay
        (island mode — JetStream persists for replay when WAN returns; MQTT only transient QoS),
        built-in request-reply (MQTT needs manual correlation IDs), decentralized JWT security
        accounts (multi-tenant substation isolation, no central auth server), leaf-node topology
        (bridges substation LAN to cloud without VPN; MQTT star needs broker reachable).
    (3) **NATS JetStream vs Kafka — the quote** — DEEP, the highest-yield specific. Quote VERBATIM
        from NATS docs: "Kafka servers require a JVM, eight cores, 64 GB to 128 GB of RAM, two or
        more 8-TB SAS/SSD disks, and a 10-Gig NIC." Contrast: NATS JetStream = single static binary
        <20 MB on ~512 MB RAM. Add: Kafka isn't slow — it's enormous (frame as footprint per Pitfall
        3); JVM GC pauses inject latency spikes (bad for protection monitoring); Kafka needs
        ZooKeeper/KRaft coordination, NATS is self-contained. Include the one-liner verbatim: "Kafka
        at the substation edge is like bringing a semi-truck to park in a bicycle lane … NATS
        JetStream is a 20 MB binary on a Pi-class node." Add a ONE-LINE Pulsar awareness note only
        (also heavy / storage-separated, edge-unsuitable — do NOT deep-dive Pulsar internals per
        CLAUDE.md "What NOT to Over-Invest In").
    (4) **The three K3s-vs-K8s distinctions** — DEEP, the criterion-3 named trio. Reproduce the table:
        - **Memory footprint:** K3s ~512 MB RAM server node vs K8s 2–4 GB minimum/node.
        - **Database (etcd/SQLite):** K3s defaults to embedded SQLite (single-node), embedded etcd
          for HA; K8s requires an external etcd cluster. (Pitfall 4: say "defaults to SQLite, supports
          embedded etcd for HA" — NOT "no etcd".)
        - **Air-gap design:** K3s single binary <100 MB, ships Traefik ingress + local-path
          provisioner, `curl | sh`; K8s is complex (kubeadm, external etcd, choose-your-own ingress).
        Add the awareness context: K3s is a Rancher/SUSE, CNCF-certified project, same K8s API surface
        with components stripped — production-grade at telco/substation edge. Frame versions as
        "current stable" (K3s tracks upstream Kubernetes minor; current ~v1.33/1.34).

    Then add (placement Claude's discretion, D-07):
    - A `## <3-min say-aloud version` `> blockquote` hitting: edge = Pi-class node → NATS over MQTT
      (durable replay, request-reply, JWT, leaf nodes) → NATS over Kafka (the JVM + 64–128 GB quote,
      footprint not throughput) → K3s over K8s (memory, SQLite-default, air-gap).
    - A `## → Bridge to your work` box (TVS-04 shape). TWO bridges from CLAUDE.md Summary Reference
      Table: **MQTT (Juan has) → NATS JetStream** (use the verbatim pivot: "I ran MQTT-to-Mosquitto
      in OSED; NATS JetStream is the same publish-subscribe instinct but adds durable replay and
      decentralized JWT accounts for the federated edge — that's the upgrade path I'd take here") and
      **full Kubernetes (Juan has) → K3s** ("Same K8s API, 4x lighter, designed for air-gap field
      deployment"). Include the comparison table + "How to say this in the interview" pivot.
    - A `## Quick-Recall Card (Recite Before the Interview)` numbered list — MUST include the Kafka
      "JVM + 64–128 GB RAM" fact and the three K3s distinctions — ending with `**My bridge:**`.
    - A closing `*Sources: …*` italic line (docs.nats.io compare-nats, docs.k3s.io, CLAUDE.md
      Categories 1–2, the CV).

    Cover all four numbered sections fully. Do NOT deep-dive Pulsar internals or Kafka ops
    (MirrorMaker/Schema Registry) — awareness only per CLAUDE.md "What NOT to Over-Invest In". Do NOT
    add an aggregate vocabulary-bridge table (Phase 6, D-09).
  </action>
  <verify>
    <automated>F=.planning/phases/04-protocols-stack-architecture/notes/STK-03-messaging-orchestration.md; test -f "$F" && grep -q 'JetStream' "$F" && grep -qi 'MQTT' "$F" && grep -qi 'Kafka' "$F" && grep -q '64' "$F" && grep -q 'K3s' "$F" && grep -qiE 'SQLite|etcd' "$F" && grep -qi 'air-gap' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "JetStream", "MQTT", "Kafka", "K3s"; contains "64" (the 64–128 GB Kafka
    hardware fact); contains "SQLite"/"etcd" and "air-gap" (two of the three K3s distinctions);
    contains the "Bridge to your work" heading and a "3-min"/"say-aloud" track. Command prints OK.
  </acceptance_criteria>
  <done>STK-03 note exists with the For:/Purpose: header, the NATS-vs-MQTT four-reason justification, the NATS-vs-Kafka verbatim "JVM + 64–128 GB RAM" footprint argument (with the bicycle-lane one-liner and a one-line Pulsar awareness note), the three K3s-vs-K8s distinctions (memory, SQLite-default/etcd-for-HA, air-gap), a "<3-min say-aloud" track, the boxed MQTT→NATS and K8s→K3s bridge, a Quick-Recall Card, and a Sources line.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write STK-04 observability note (Prometheus pull vs InfluxDB push, PromQL, kube-prometheus-stack)</name>
  <files>.planning/phases/04-protocols-stack-architecture/notes/STK-04-observability.md</files>
  <read_first>
    - .planning/phases/04-protocols-stack-architecture/04-RESEARCH.md section "Observability Stack (STK-04 Gap Fill)" — sub-sections "Prometheus Pull Model vs InfluxDB Push" (the comparison table + the pull-model key implication), "kube-prometheus-stack — What It Adds" (the FIVE bundled components: Prometheus Operator, node-exporter, kube-state-metrics, Grafana, Alertmanager + the interview sentence), and "Concrete Recitable PromQL Query" (the rate() CPU query + the additional queries + the PromQL building blocks) — AND "Common Pitfalls" Pitfall 5 (name all five components) and the "Code Examples" PromQL block
    - .planning/phases/04-protocols-stack-architecture/04-CONTEXT.md sections D-01, D-02, D-03, D-07, D-08, D-10 and the <specifics> "the kube-prometheus-stack name" block
    - CLAUDE.md "Category 4: Observability" (Prometheus vs InfluxDB table, the PromQL snippets, the exporter list) — the base to lift+tighten; note CLAUDE.md NEVER names kube-prometheus-stack, which this note fills; plus the Summary Reference Table row for Prometheus
    - .planning/phases/02-distribution-virtual-sensing/notes/TVS-04-asset-health.md (the literal style template)
  </read_first>
  <action>
    Create notes/STK-04-observability.md mirroring the TVS-04 style. Header block:
    `# STK-04: Observability — Prometheus, PromQL & kube-prometheus-stack`, then
    `**For:** Oral rehearsal — speak each section aloud before the interview.`,
    `**Purpose:** …` (explain Prometheus pull vs InfluxDB push, recite a PromQL query, and name
    what kube-prometheus-stack adds), then `---`.

    Numbered mental-model-first sections, drawing facts VERBATIM from RESEARCH §"Observability Stack":

    (1) **Pull vs push mental model** — DEEP. Reproduce the comparison table (Dimension | Prometheus |
        InfluxDB): ingest model (Prometheus PULLS — scrapes /metrics on a schedule; InfluxDB services
        PUSH), query language (PromQL vs Flux/InfluxQL), K8s integration (native ServiceMonitor CRD +
        auto pod discovery vs manual), alert rules (native Prometheus + Alertmanager vs external
        Kapacitor/Grafana), default retention (15 days vs configurable long-term), cardinality
        (Prometheus sensitive to high-cardinality labels vs InfluxDB higher tolerance). State the
        pull-model key implication: services don't need to know where to send metrics — they expose
        /metrics, Prometheus finds them via K8s service discovery (ServiceMonitor) and scrapes; this
        is why it fits K8s where pod IPs change constantly.
    (2) **kube-prometheus-stack — the named gap** — DEEP, criterion-4 specific. State it is a single
        Helm chart (current chart ~86.2.3) bundling FIVE components — list all five with their roles
        (Pitfall 5: never say just "Prometheus + Grafana"):
        1. **Prometheus Operator** — K8s controller watching ServiceMonitor/PodMonitor CRDs,
           auto-configures scrape targets (you don't hand-edit prometheus.yml).
        2. **node-exporter** — DaemonSet on every node; Linux host metrics (CPU/mem/disk/net).
        3. **kube-state-metrics** — K8s object states (pod running/pending/failed, replicas).
        4. **Grafana** — visualization, pre-loaded K8s dashboards.
        5. **Alertmanager** — routes firing alerts to PagerDuty/Slack/email/webhook.
        Include the interview sentence verbatim: "Instead of installing Prometheus, Grafana, and the
        exporters separately and wiring them together, kube-prometheus-stack is a single Helm chart
        that delivers Prometheus Operator, node-exporter, kube-state-metrics, Grafana, and
        Alertmanager pre-wired. The Operator pattern means you define what to scrape with a
        ServiceMonitor CRD and the Operator manages Prometheus config automatically."
    (3) **A recitable PromQL query** — DEEP. Render the primary query in a fenced ```promql block
        VERBATIM:
        ```promql
        rate(container_cpu_usage_seconds_total{namespace="virtual-sensing"}[5m])
        ```
        Explain it: per-second CPU usage rate averaged over the last 5 minutes for all containers in
        the virtual-sensing namespace; rate() is required because the metric is a cumulative counter.
        Add the additional awareness queries in fenced blocks (memory working set by pod with
        sum(...) by (pod); kube_pod_status_ready; up{job="node-exporter"} == 0 down-alert) and name
        the PromQL building blocks: rate(counter[window]), sum(...) by (label), label matchers
        (=, !=, =~, !~), and the `up` heartbeat metric (0 = scrape target down).
    (4) **Where observability runs in the architecture** — AWARENESS. Prometheus runs at BOTH edge
        (per-K3s-node scraping) AND cloud; Grafana is centralized (forward reference to STK-05's
        tiers — keep one line).

    Then add (placement Claude's discretion, D-07):
    - A `## <3-min say-aloud version` `> blockquote` hitting: pull vs push (Prometheus scrapes
      /metrics, InfluxDB is pushed-to) → PromQL is the query language → recite the rate() CPU query →
      kube-prometheus-stack is five components in one Helm chart (name all five).
    - A `## → Bridge to your work` box (TVS-04 shape). Bridge: **InfluxDB + Grafana (Juan has) →
      Prometheus + PromQL** (use the Summary Reference Table one-liner: "Prometheus pull-scrapes K8s
      pods; Grafana visualizes via PromQL"). Frame: "I ran InfluxDB + Grafana in OSED on a push
      model; Prometheus inverts that to a pull/scrape model with native K8s service discovery, and
      kube-prometheus-stack gives me the whole stack — Operator, exporters, Grafana, Alertmanager —
      in one Helm install." Include the comparison table + "How to say this in the interview" pivot.
    - A `## Quick-Recall Card (Recite Before the Interview)` numbered list — MUST include the five
      kube-prometheus-stack component names and the rate() PromQL query — ending with `**My bridge:**`.
    - A closing `*Sources: …*` italic line (Prometheus docs, ArtifactHub kube-prometheus-stack,
      CLAUDE.md Category 4, the CV).

    Cover all four numbered sections fully. Do NOT add an aggregate vocabulary-bridge table (Phase 6,
    D-09). Use the verified PromQL query verbatim — do not invent metric names.
  </action>
  <verify>
    <automated>F=.planning/phases/04-protocols-stack-architecture/notes/STK-04-observability.md; test -f "$F" && grep -q 'kube-prometheus-stack' "$F" && grep -q 'rate(container_cpu_usage_seconds_total' "$F" && grep -q 'node-exporter' "$F" && grep -q 'kube-state-metrics' "$F" && grep -q 'Alertmanager' "$F" && grep -q 'Operator' "$F" && grep -qi 'InfluxDB' "$F" && grep -q 'Bridge to your work' "$F" && grep -qiE '3-min|say-aloud|say aloud' "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "kube-prometheus-stack"; contains the verbatim PromQL query
    "rate(container_cpu_usage_seconds_total"; names all five components ("Operator", "node-exporter",
    "kube-state-metrics", "Grafana" implicit, "Alertmanager"); contains "InfluxDB" (the pull-vs-push
    contrast); contains the "Bridge to your work" heading and a "3-min"/"say-aloud" track. Command prints OK.
  </acceptance_criteria>
  <done>STK-04 note exists with the For:/Purpose: header, the Prometheus-pull-vs-InfluxDB-push comparison table + pull-model implication, the five kube-prometheus-stack components named with roles, the recitable rate(container_cpu_usage_seconds_total{namespace="virtual-sensing"}[5m]) query plus awareness queries and PromQL building blocks, a "<3-min say-aloud" track, the boxed InfluxDB+Grafana→Prometheus/PromQL bridge, a Quick-Recall Card, and a Sources line.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only phase, no runtime/attack surface. This plan produces static markdown
study notes Juan reads aloud: no executable, deployable, or network-facing artifact, no user
input, no secrets, no data handling. STRIDE threat modeling does not apply.
</threat_model>

<verification>
- Both note files exist under notes/ with the required greppable tokens and headings.
- STK-03 contains the NATS-vs-MQTT justification, the verbatim Kafka "64–128 GB" footprint quote, and the three K3s-vs-K8s distinctions (memory, SQLite/etcd, air-gap), plus the MQTT→NATS and K8s→K3s bridge.
- STK-04 contains the Prometheus-pull-vs-InfluxDB-push contrast, the five named kube-prometheus-stack components, and the recitable rate() PromQL query, plus the InfluxDB+Grafana→Prometheus bridge.
- Each note carries the For:/Purpose: header, a "<3-min say-aloud" track, and a "→ Bridge to your work" box (D-07, D-08).
</verification>

<success_criteria>
Juan can read each note aloud and, without notes, (3) justify NATS JetStream vs MQTT and vs Kafka
for the substation edge and state the three K3s-vs-K8s distinctions (STK-03 / criterion 3), and
(4) explain the Prometheus pull model vs InfluxDB push, recite a PromQL query, and name what
kube-prometheus-stack adds (STK-04 / criterion 4) — each with its MQTT→NATS / K8s→K3s /
InfluxDB+Grafana→Prometheus bridge ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocols-stack-architecture/04-02-SUMMARY.md`
</output>
</content>
</invoke>
