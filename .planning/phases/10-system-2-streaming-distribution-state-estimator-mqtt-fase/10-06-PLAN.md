---
phase: 10-system-2-streaming-distribution-state-estimator-mqtt-fase
plan: 06
type: execute
wave: 4
depends_on: [10-04]
files_modified:
  - system1-measurement-source/grafana/provisioning/dashboards/ieee33-est-day.json
  - system1-measurement-source/grafana/provisioning/dashboards/ieee33-est-fault.json
  - system1-measurement-source/README.md
autonomous: true
requirements: [R13]
user_setup: []

must_haves:
  truths:
    - "Grafana lists the four existing dashboards PLUS two new estimator dashboards (ieee33-est-day, ieee33-est-fault) rendering from a clean provisioned start"
    - "The day dashboard shows true-vs-estimated voltage overlay, per-bus error, trace_P (ORACS observability index), the per-step NIS calibration time series (from estimate_system.nis_k), and dark-node recovery"
    - "The fault dashboard adds island-mode P-inflation (sigma_V / trace_P rising in faulted_isolated) and pre_fault/faulted_isolated/restored phase-region markers"
    - "Two consecutive same-config estimate runs produce reproducible estimates (identical or within 1e-9); no unseeded random/np.random/wall-clock in System 2 paths (grep-verified)"
    - "Following the README from a clean checkout reproduces an estimates bucket and the dashboards"
  artifacts:
    - path: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-est-day.json"
      provides: "Estimator day dashboard (true-vs-est, error, trace_P, NIS calibration from nis_k, dark-node)"
      contains: "ieee33-est-day"
    - path: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-est-fault.json"
      provides: "Estimator fault dashboard (P-inflation + phase regions)"
      contains: "ieee33-est-fault"
    - path: "system1-measurement-source/README.md"
      provides: "Runbook: broker, publish/estimate/score, dashboards"
      contains: "publish"
  key_links:
    - from: "ieee33-est-day.json panels"
      to: "estimates bucket"
      via: "Flux queries on estimate / estimate_system measurements (incl. nis_k)"
      pattern: "estimates"
    - from: "README.md"
      to: "publish/estimate/score runners"
      via: "documented run sequence"
      pattern: "uv run"
---

<objective>
Make System 2 visible and reproducible. Add two auto-provisioned Grafana dashboards (additive to the four existing) — `ieee33-est-day.json` (true-vs-estimated voltage overlay, per-bus error heatmap, `trace_P` ORACS observability index timeseries, per-step NIS calibration time series fed from the `estimate_system.nis_k` field, dark-node recovery) and `ieee33-est-fault.json` (island-mode P-inflation + phase-region markers). Add a README runbook documenting the broker, the `publish`/`estimate`/`score` runners, and how to open the dashboards. Verify determinism: two same-config estimate runs reproduce estimates within 1e-9, and no unseeded randomness/wall-clock exists in System 2 paths.

Purpose: The `trace_P` / P-inflation panels are the ORACS-observability and island-mode-resilience showpieces for the interview; the determinism + README close out SPEC R13's reproducible/visible/documented mandate.
Output: Two new dashboard JSONs, a README runbook, and a determinism verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-SPEC.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-CONTEXT.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-PATTERNS.md

<interfaces>
<!-- Existing dashboards to copy structure from (do NOT modify them) -->
# grafana/provisioning/dashboards/ieee33-meas-day.json   — day dashboard skeleton, datasource uid, Flux panel style
# grafana/provisioning/dashboards/ieee33-meas-fault.json — fault dashboard with phase-region annotation panels
# datasource: { "type": "influxdb", "uid": "ieee33-influxdb" }   (every panel)

<!-- estimates schema (written by Plan 04) -->
# estimate: tags bus_id/scenario/experiment/estimator; fields vm_pu_est, va_degree_est, sigma_vm, sigma_va
# estimate_system: tags scenario/experiment/estimator; field trace_P; AND (recursive filters) nis_k (float), m_k (int)
#   -> nis_k is a REAL per-step time series the NIS calibration panel queries directly (no static annotation).
# oracle overlay (true vm_pu) comes from the state bucket — read by the DASHBOARD (Grafana), which is allowed;
#   the oracle-separation rule binds estimator CODE, not visualization.

<!-- D-08: two dashboards (not one templated) -->
</interfaces>
</context>

<threat_model>
No new external attack surface — Grafana is already localhost-bound (existing stack); dashboards are static provisioned JSON read by Grafana over the existing InfluxDB datasource. README is documentation. No broker/network surface added by this plan. Broker-boundary threats handled in Plan 01 (T-10-01/02).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ieee33-est-day.json + ieee33-est-fault.json (auto-provisioned, additive)</name>
  <files>system1-measurement-source/grafana/provisioning/dashboards/ieee33-est-day.json, system1-measurement-source/grafana/provisioning/dashboards/ieee33-est-fault.json</files>
  <read_first>
    - system1-measurement-source/grafana/provisioning/dashboards/ieee33-meas-day.json (skeleton: __inputs/__requires, datasource uid, Flux panel queries, gridPos)
    - system1-measurement-source/grafana/provisioning/dashboards/ieee33-meas-fault.json (phase-region annotation panel pattern)
    - 10-PATTERNS.md §ieee33-est-day.json (lines 769-814 — JSON skeleton, datasource ref, Flux query, gridPos, fault variant)
    - 10-CONTEXT.md D-08 (the exact panel list per dashboard)
  </read_first>
  <action>
    Create the two dashboards by copying the structure of the corresponding `ieee33-meas-*.json` (do NOT modify the existing four dashboards). Use `"datasource": { "type": "influxdb", "uid": "ieee33-influxdb" }` on every panel.
    **ieee33-est-day.json** (uid `ieee33-est-day`, title "IEEE33 Estimator Day", description per PATTERNS line 784): panels —
      1. True-vs-estimated voltage overlay (per selectable bus via a `bus_id` template variable): Flux on `estimates`/`estimate` field `vm_pu_est` filtered by `estimator`/`scenario`, overlaid with the true `vm_pu` from the `state` bucket (Grafana may read the oracle — visualization is exempt).
      2. Per-bus error heatmap (|vm_pu_est - vm_pu|).
      3. `trace_P` timeseries (ORACS observability index) from `estimate_system`.
      4. NIS calibration panel (timeseries): query the `estimates` bucket `estimate_system` measurement field `nis_k` (the real per-step NIS series persisted by the recursive filters in Plan 04), filtered by `estimator`/`scenario`. This is a concrete time series — NOT a static annotation. Where convenient, overlay or note the per-step χ²(m_k) 95% band that score.py uses (band itself is computed in score.py; the panel renders the live nis_k series and the operator reads the score.py PASS/FAIL alongside). For WLS (no nis_k persisted) the series is simply empty — that is expected (NIS is a recursive-filter metric).
      5. Dark-node recovery panel (pseudo-only buses' estimated |V| vs true).
      Use a 2×2+ grid of 12×9 panels (gridPos pattern PATTERNS line 812). Add an `estimator` and `scenario` template variable.
    **ieee33-est-fault.json** (uid `ieee33-est-fault`, title "IEEE33 Estimator Fault"): mirror the day dashboard PLUS — island-mode P-inflation panel (mean σ_V or `trace_P` over the run, expected to rise in `faulted_isolated`) and `pre_fault`/`faulted_isolated`/`restored` phase-region markers (copy the annotation/region pattern from `ieee33-meas-fault.json`).
    Ensure both UIDs are UNIQUE (no collision with the four existing dashboard uids — prior phase mitigated uid collisions; keep that invariant).
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run python -c "
import json, glob
existing={json.load(open(f)).get('uid') for f in glob.glob('grafana/provisioning/dashboards/ieee33-*.json') if 'est' not in f}
for f in ('grafana/provisioning/dashboards/ieee33-est-day.json','grafana/provisioning/dashboards/ieee33-est-fault.json'):
    d=json.load(open(f))   # valid JSON
    assert d['uid'] in ('ieee33-est-day','ieee33-est-fault'), d['uid']
    assert d['uid'] not in existing, f'uid collision {d[\"uid\"]}'
    assert d.get('panels'), 'no panels'
    assert any('estimates' in json.dumps(p) for p in d['panels']), 'no estimates query'
day=json.load(open('grafana/provisioning/dashboards/ieee33-est-day.json'))
assert any('nis_k' in json.dumps(p) for p in day['panels']), 'NIS panel must query estimate_system.nis_k'
print('both estimator dashboards valid + unique uid + query estimates; day dashboard NIS panel queries nis_k')"</automated>
    <automated>cd system1-measurement-source && uv run python -c "import json,glob; n=len(glob.glob('grafana/provisioning/dashboards/ieee33-*.json')); assert n==6, n; print(f'{n} dashboards (4 existing + 2 new)')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files are valid JSON with unique uids `ieee33-est-day` / `ieee33-est-fault` (no collision with the four existing dashboards).
    - Day dashboard has panels querying the `estimates` bucket for `vm_pu_est` overlay, error, `trace_P`, AND a NIS calibration panel that queries the `estimate_system` `nis_k` field (a real time series, not a static annotation); fault dashboard adds a P-inflation panel + phase-region markers.
    - The four existing dashboards remain byte-unchanged (`git diff` shows only the two new files).
    - Total dashboard count under `grafana/provisioning/dashboards/` is 6.
  </acceptance_criteria>
  <done>Two auto-provisioned estimator dashboards added (additive; day dashboard NIS panel reads the live nis_k series); existing four untouched.</done>
</task>

<task type="auto">
  <name>Task 2: README runbook + determinism verification</name>
  <files>system1-measurement-source/README.md</files>
  <read_first>
    - system1-measurement-source/README.md (existing — append the System 2 section; do not rewrite System 1/8.1/P9 sections)
    - 10-SPEC.md R13 (lines 180-192 — determinism, dashboards, README requirements)
    - 10-PATTERNS.md Shared Patterns: Seeded Deterministic RNG (lines 840-846), Oracle Separation Boundary (lines 931-939)
  </read_first>
  <action>
    1. **README** — ADD a "System 2 — Streaming State Estimator" section documenting the runbook from a clean checkout: (a) `docker compose up -d` starts InfluxDB + Grafana + the new Mosquitto broker; (b) `uv run publish --scenario <s> --source <day|fault> [--acceleration N]` streams measurements + retained `netmodel/current`; (c) `uv run estimate --scenario <s> --source <...> --estimator <wls|ekf|ukf>` consumes the stream and writes the `estimates` bucket (recursive filters also persist per-step `nis_k`/`m_k` innovation statistics); (d) `uv run score --estimator all` prints the PASS/FAIL report (incl. the NIS in-band fraction from the persisted nis_k/m_k); (e) open Grafana (existing localhost URL) and select the new "IEEE33 Estimator Day" / "IEEE33 Estimator Fault" dashboards. Note the oracle-separation property (estimator never reads state/fault_event; profiles is forecast side-information, not the oracle — D-06) and the `trace_P` = ORACS observability index framing. Keep existing README sections intact.
    2. **Determinism verification** — add a small repeatable check (a `tests/` script or a documented command sequence) that runs `estimate` twice with the same config and asserts the resulting estimates match within 1e-9 (query the estimates bucket for both runs, or run estimate into two distinguishable experiment tags and diff). Document it in the README. Also confirm via grep that no unseeded randomness/wall-clock exists in System 2 paths.
  </action>
  <verify>
    <automated>cd system1-measurement-source && grep -q "uv run publish" README.md && grep -q "uv run estimate" README.md && grep -q "uv run score" README.md && grep -qi "Estimator" README.md && echo "README runbook section present"</automated>
    <automated>cd system1-measurement-source && grep -rnE "np\.random\.seed|random\.seed\(|np\.random\.randn|np\.random\.rand\(" src/ieee33/publish.py src/ieee33/estimate.py src/ieee33/estimators.py src/ieee33/fase_predict.py src/ieee33/ac_model.py src/ieee33/score.py; test $? -ne 0 && echo "no legacy/unseeded RNG in System 2 paths OK" || (echo "UNSEEDED RNG found in System 2 path" && exit 1)</automated>
    <automated>cd system1-measurement-source && grep -rnE "datetime\.now|time\.time\(\)" src/ieee33/estimate.py src/ieee33/estimators.py src/ieee33/fase_predict.py src/ieee33/ac_model.py src/ieee33/score.py; test $? -ne 0 && echo "no wall-clock in deterministic System 2 paths OK"</automated>
  </verify>
  <acceptance_criteria>
    - README has a System 2 section with the `docker compose up -d` -> `uv run publish` -> `uv run estimate` -> `uv run score` -> open-dashboard sequence; existing README sections unchanged.
    - README documents oracle separation (D-06) and the `trace_P` ORACS framing.
    - `grep -rnE "np\.random\.seed|random\.seed\(|np\.random\.randn"` across the six System 2 source files returns ZERO matches (project RNG norm — `np.random.default_rng` only).
    - `grep -rnE "datetime\.now|time\.time\(\)"` across estimate/estimators/fase_predict/ac_model/score returns ZERO matches (publish.py's `time.sleep` throttle is the only permitted wall-clock and lives outside this grep set).
    - A documented determinism check asserts two same-config estimate runs agree within 1e-9.
  </acceptance_criteria>
  <done>README runbook documents the broker + three runners + dashboards; determinism (1e-9) and no-unseeded-randomness/wall-clock are grep-verified.</done>
</task>

</tasks>

<verification>
- `docker compose up -d` then Grafana shows the four existing dashboards PLUS "IEEE33 Estimator Day" and "IEEE33 Estimator Fault" rendering from a clean provisioned start.
- Following the README from a clean checkout reproduces an `estimates` bucket and the dashboards.
- Two same-config `estimate` runs agree within 1e-9; grep finds no unseeded randomness/wall-clock in System 2 paths.
</verification>

<success_criteria>
- Two additive estimator dashboards (true-vs-est, error, trace_P, live NIS series from nis_k, dark-node; fault P-inflation + phase regions) auto-provision; existing four untouched (R13).
- README runbook documents broker + publish/estimate/score + dashboards (R13).
- Determinism (1e-9) + no unseeded randomness/wall-clock grep-verified (R13).
</success_criteria>

<output>
After completion, create `.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-06-SUMMARY.md`
</output>
</content>
</invoke>
