---
phase: 10-system-2-streaming-distribution-state-estimator-mqtt-fase
plan: 10
type: execute
wave: 3
depends_on: ["10-07", "10-08", "10-09"]
files_modified:
  - system1-measurement-source/src/ieee33/estimate.py
autonomous: false
gap_closure: true
requirements: [R5, R6, R7, R10, R11, R12]
must_haves:
  truths:
    - "estimate.py subscribes to `ieee33/{experiment}/{scenario}/forecast`, buffers the retained-latest forecast, and attaches it to each snapshot by timestamp"
    - "For each EKF/UKF step, estimate.py builds Δp = [ΔP;ΔQ] (forecast(k) − forecast(k−1)) over buses 1..32 and Cov(ε) = diag(sigma_p², sigma_q²) in the order injection_sensitivity expects"
    - "estimate.py computes S = ac_model.injection_sensitivity(x_prev, Ybus) and calls fase_predictor.predict(x_prev, S, delta_p, cov_eps) — the old fase_sensitivity/measurement-Jacobian predict path is removed"
    - "S @ delta_p is dimensionally AND order-consistent: a synthetic round-trip x_prev + S @ delta_p (Δp of length 64 in injection_sensitivity's documented column order) is finite and shape (64,) — proves no column-order mismatch"
    - "EKF/UKF run predict→update across all snapshots and write (x̂,P,trace_P,nis_k) to the estimates bucket for buses 1..32 (R6/R7/R11/R12); WLS still writes on well_observed (R5/R10)"
  artifacts:
    - path: "system1-measurement-source/src/ieee33/estimate.py"
      provides: "forecast-stream consumer + injection-sensitivity predict wiring"
      contains: "injection_sensitivity"
  key_links:
    - from: "system1-measurement-source/src/ieee33/estimate.py"
      to: "ieee33/{experiment}/{scenario}/forecast (MQTT)"
      via: "subscribe + on_message buffer keyed by timestamp"
      pattern: "forecast"
    - from: "system1-measurement-source/src/ieee33/estimate.py"
      to: "ac_model.injection_sensitivity + fase_predictor.predict"
      via: "S = injection_sensitivity(x_prev, Ybus); predict(x_prev, S, delta_p, cov_eps)"
      pattern: "injection_sensitivity"
---

<objective>
Wire the pieces together: `estimate.py` consumes the external forecast stream (10-09), computes the
correct injection sensitivity (10-08) against the 64-state model (10-07), and feeds Δp/Cov(ε) into
the FASE predict. This closes the last mile so EKF/UKF run predict→update across all snapshots
(R6/R7), WLS converges on well_observed (R5/R10), NIS/NEES is populated (R11), and dead-bus masking
works in fault (R12).

This replaces the broken predict wiring in estimate.py L523-554 (which built S from the measurement
Jacobian and fed a scalar-derived delta_p — the source of bug #2).

Depends on ALL THREE prior gap plans (state def, sensitivity+predict, forecast publisher). Runs in
wave 3 alone (touches estimate.py, which 10-07 also edits — sequenced after it).

Purpose: end-to-end functional streaming estimator; the estimates bucket fills for wls/ekf/ukf.
Output: a runnable pipeline (`docker compose up` → `publish` + `forecast` → `estimate` → `score`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-CONTEXT.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-SPEC.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-PATTERNS.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-07-SUMMARY.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-08-SUMMARY.md
@.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-09-SUMMARY.md

<interfaces>
<!-- Contracts from the three upstream gap plans. Build against these — no exploration. -->

From 10-07: state 64 = 2×32 (buses 1..32); bus 0 reference; `n_state = ec.N_FREE_STATES` (64),
`n_bus_est = ec.N_FREE_STATES // 2` (32). write_estimate_step bus_id = state_index+1.

From 10-08:
- `ac_model.injection_sensitivity(x, Ybus, ref_vm=1.0) -> S` shape (64, 2·n_nonslack)=(64,64);
  columns are bus injections [ΔP;ΔQ] over buses 1..32 in the ORDER documented in its docstring
  (READ that docstring to build Δp/Cov(ε) in the matching order).
- `FASEPredictor.__init__(cfg, rng, n_bus)` (no prof_df) and
  `FASEPredictor.predict(x_prev, S, delta_p, cov_eps) -> (x_minus, Q)`; asserts
  `delta_p.shape[0]==S.shape[1]` and `cov_eps` square. Persistence foil = Δp:=0.
- `estimators.EKFEstimator.predict(delta_p, S, Cov_eps)` / `UKFEstimator.predict(...)` unchanged.

From 10-09: forecast topic `ieee33/{experiment}/{scenario}/forecast`, retained-latest, QoS1,
payload `{timestamp, step_k, p_fcst{bus}, q_fcst{bus}, sigma_p{bus}, sigma_q{bus}}` with string bus
keys "1".."32". One message per timestep.

Current estimate.py (to be rewired):
- L419-430 MQTTSnapshotAssembler subscribes `ieee33/netmodel/current` + `ieee33/#` — the `#` wildcard
  ALREADY receives forecast messages; add a topic-branch in on_message to buffer them (do NOT let
  forecast payloads fall into the meas buffer — they lack class/location, will be dropped by the
  meas key-check, so add an explicit `/forecast` branch BEFORE the `/meas/` branch).
- L416-417 `fase_predictor = FASEPredictor(prof_df, cfg, rng, n_bus_est)` — drop prof_df arg
  (10-08 changed the signature). NOTE: `prof_df = influx.read_profiles(client)` at L402 is no longer
  needed for predict; remove if unused.
- L523-554 the broken predict block (inj_ml / fase_sensitivity / W_inj) — REPLACE.
- `_apply_prior_to_estimator` L644-668 — keep (still the injection mechanism for x_minus/Q).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subscribe to + buffer the forecast stream; attach forecast per snapshot by timestamp</name>
  <files>system1-measurement-source/src/ieee33/estimate.py</files>
  <read_first>
    - system1-measurement-source/src/ieee33/estimate.py (file under edit; L109-285 MQTTSnapshotAssembler, L154-171 on_message dispatch, L204-249 buffer/drain)
    - .planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-09-SUMMARY.md (forecast topic + payload contract)
    - .planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-PATTERNS.md (Landmine 4 retained-message race; paho subscribe pattern)
  </read_first>
  <action>
    Extend MQTTSnapshotAssembler to consume the forecast stream alongside meas + netmodel.

    1. `on_message` dispatch (L166-171): add an explicit branch — if `msg.topic` ends with
       `/forecast`, call a new `_handle_forecast(payload)`; keep the existing netmodel and `/meas/`
       branches. Place the `/forecast` check BEFORE the `/meas/` check so forecast payloads are never
       misrouted into the meas buffer.
    2. `_handle_forecast(payload)`: defensively validate keys `{timestamp, p_fcst, q_fcst, sigma_p,
       sigma_q}` (skip+log malformed, mirroring `_handle_meas`). Store under the lock into a new
       `self._forecast_buffer: dict[ts_str -> payload]`. Because the publisher retains the latest,
       also keep `self._latest_forecast = payload` for the retained-message-race path.
    3. Add `get_forecast_for(ts_str)` returning the forecast payload whose `timestamp` matches the
       snapshot ts (exact string match; fall back to `_latest_forecast` if the exact ts is not yet
       buffered, and log a one-line note). Thread-safe via the lock.
    4. Do NOT gate meas processing on the forecast (WLS needs no forecast; EKF/UKF fall back to the
       persistence prior — Δp:=0 — if no forecast has arrived yet, so the pipeline never deadlocks).

    Determinism/oracle norms unchanged; forecast is legal side-information (not oracle).
  </action>
  <verify>
    <automated>cd system1-measurement-source && python -c "import ast; ast.parse(open('src/ieee33/estimate.py').read()); print('parse OK')" && grep -qE 'def _handle_forecast' src/ieee33/estimate.py && grep -qE 'def get_forecast_for' src/ieee33/estimate.py && grep -qE '/forecast' src/ieee33/estimate.py</automated>
  </verify>
  <done>estimate.py subscribes and buffers the forecast stream, routes `/forecast` before `/meas/`, and exposes get_forecast_for(ts) with a retained-latest fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the predict wiring — build Δp/Cov(ε) from the forecast and call injection_sensitivity + predict</name>
  <files>system1-measurement-source/src/ieee33/estimate.py</files>
  <read_first>
    - system1-measurement-source/src/ieee33/estimate.py (file under edit; L400-417 startup/predictor, L484-561 per-step predict block, L644-668 _apply_prior_to_estimator)
    - system1-measurement-source/src/ieee33/ac_model.py (injection_sensitivity docstring — the exact Δp column ORDER)
    - system1-measurement-source/src/ieee33/fase_predict.py (new predict(x_prev, S, delta_p, cov_eps) signature)
    - .planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-08-SUMMARY.md (S shape + injection column order)
  </read_first>
  <action>
    Rewire the predict path to use the forecast + injection sensitivity. This removes the bug-#2
    code entirely.

    1. Startup (L402, L416-417): update `FASEPredictor(prof_df, cfg, rng, n_bus_est)` →
       `FASEPredictor(cfg, rng, n_bus_est)` (10-08 dropped prof_df). Remove the now-unused
       `prof_df = influx.read_profiles(client)` at L402 and its print (predict no longer needs it);
       keep imports tidy.
    2. Per-step forecast → Δp/Cov(ε) (replace the L523-554 `estimator_name in ("ekf","ukf")` block):
       - Fetch `fc = assembler.get_forecast_for(ts_str)`.
       - Maintain a loop-level `prev_fcst` (previous step's forecast p/q arrays, init None). Build
         `p_now`, `q_now` as length-32 arrays over buses 1..32 from `fc["p_fcst"]["1".."32"]` /
         `fc["q_fcst"][...]` (missing bus → 0.0). Same for `sigma_p`, `sigma_q`.
       - `delta_p = concat(p_now - p_prev, q_now - q_prev)` assembled in EXACTLY the injection column
         order documented by `injection_sensitivity` (read that docstring; if [P₁..P₃₂,Q₁..Q₃₂] use
         that; if interleaved, interleave). On the FIRST step (prev None, or no forecast) →
         `delta_p := np.zeros(64)` (persistence-equivalent prior; do NOT crash).
       - `cov_eps = np.diag(concat(sigma_p², sigma_q²))` in the SAME order as delta_p → shape (64,64).
       - `S = ac_model.injection_sensitivity(x_prev, _Ybus_snap)`  (64×64).
       - `x_minus, Q = fase_predictor.predict(x_prev=x_prev, S=S, delta_p=delta_p, cov_eps=cov_eps)`.
       - `_apply_prior_to_estimator(estimator, estimator_name, x_minus, Q)` (unchanged).
       - Update `prev_fcst = (p_now, q_now)` AFTER computing delta_p.
       - DELETE the `inj_ml`, `W_inj`, `fase_sensitivity` code (the bug-#2 path).
    3. Dead-bus handling in fault (R12): if a forecast bus is de-energised (in
       `assembler.dead_buses`), zero its p/q/σ contribution so delta_p/cov_eps do not inject spurious
       energy into dead buses. Reuse the `energised_mask` from 10-07.
    4. WLS branch unchanged (no predict). Update/NIS/write blocks unchanged — they already produce
       nis_k/m_k (R11) and mask dead buses (R12) via 10-07's mask.

    Verify the assertion in FASEPredictor.predict (`delta_p.shape[0]==S.shape[1]`) passes — if the
    order is wrong the assert fails loudly at step 0 (good). The synthetic `S @ delta_p` round-trip
    in <verify> additionally proves S's COLUMN ORDER matches the Δp assembly order (a mismatch is
    dimensionally invisible — both size 64 — but numerically wrong).

    Determinism/oracle norms unchanged. Grep must stay clean.
  </action>
  <verify>
    <automated>cd system1-measurement-source && python -c "import ast; ast.parse(open('src/ieee33/estimate.py').read()); print('parse OK')" && grep -qE 'injection_sensitivity' src/ieee33/estimate.py && grep -vE '^\s*#|"""' src/ieee33/estimate.py | grep -cE 'fase_sensitivity|inj_ml|W_inj' | grep -qx 0 && grep -qE 'FASEPredictor\(\s*cfg' src/ieee33/estimate.py && grep -vE '^\s*#|"""' src/ieee33/estimate.py | grep -cE '"state"|"fault_event"|read_state|read_fault' | grep -qx 0 && uv run python -c "
import numpy as np
from ieee33 import ac_model as m
from ieee33.network import build_enhanced_33bus
# Build net + Ybus at flat start, then exercise the S @ delta_p round-trip in the DOCUMENTED order.
net,_ = build_enhanced_33bus()
p,z,ppci = m.extract_static_line_params(net)
Ytr = m.compute_trafo_fixed(p,z,ppci)
Y = m.build_ybus_from_topology(p, set(net.line.index[net.line['in_service']]), 34, z, Ytr)
x_prev = np.zeros(64); x_prev[0::2] = 1.0
S = m.injection_sensitivity(x_prev, Y)
assert S.shape == (64,64), S.shape
# Build a synthetic Δp of length 64 in injection_sensitivity's documented column order.
delta_p = np.full(S.shape[1], 0.01)   # small uniform injection perturbation [ΔP;ΔQ] over buses 1..32
assert delta_p.shape[0] == S.shape[1], (delta_p.shape, S.shape)
x_minus = x_prev + S @ delta_p
assert x_minus.shape == (64,), x_minus.shape
assert np.all(np.isfinite(x_minus)), 'non-finite x_prev + S@delta_p — column-order/dimension mismatch'
print('S @ delta_p round-trip finite, shape', x_minus.shape)
" && echo "GATES PASS"</automated>
  </verify>
  <acceptance_criteria>
    - estimate.py builds Δp/Cov(ε) from the forecast, computes S via injection_sensitivity, and drives predict.
    - The fase_sensitivity/inj_ml/W_inj bug-#2 path is gone; FASEPredictor is constructed without prof_df.
    - **Synthetic S @ delta_p round-trip:** `S = injection_sensitivity(x_prev, Ybus)` is (64,64); a
      Δp of length 64 built in injection_sensitivity's documented column order gives
      `x_prev + S @ delta_p` finite and shape (64,) — proves column order and dimension agree (a
      mismatch would be size-invisible but numerically wrong).
    - Oracle grep on estimate.py is clean.
  </acceptance_criteria>
  <done>estimate.py builds Δp/Cov(ε) from the forecast, computes S via injection_sensitivity, and drives predict; a synthetic `x_prev + S @ delta_p` round-trip in the documented column order is finite and shape (64,); the fase_sensitivity/inj_ml/W_inj bug-#2 path is gone; FASEPredictor is constructed without prof_df; oracle grep clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live end-to-end run — confirm all three estimators write and meet acceptance bars</name>
  <files>system1-measurement-source/src/ieee33/estimate.py</files>
  <action>
    Human-verify checkpoint (no code change). Claude has already automated the full rewire in Tasks
    1–2; this step confirms the live pipeline produces estimates and meets the R5/R6/R7/R10/R11/R12
    acceptance bars, re-running the exact live-UAT flow that surfaced the three defects.
  </action>
  <what-built>
    The full streaming pipeline is rewired: 64-state model (10-07), injection sensitivity + Δp/Cov(ε)
    predict (10-08), external forecast publisher (10-09), and estimate.py consumer (this plan). WLS
    should converge on well_observed; EKF/UKF should run predict→update across all snapshots.
  </what-built>
  <how-to-verify>
    From `system1-measurement-source/`:
    1. `docker compose up -d` (InfluxDB + Grafana + Mosquitto; broker localhost-bound per 19c0422).
    2. Ensure profiles + measurements buckets are populated (Phase 9 ingest/measure already ran).
    3. In one shell: `uv run forecast --source day --scenario well_observed --acceleration 50`.
    4. In another: `uv run publish --source day --scenario well_observed --acceleration 50`.
    5. In a third: `uv run estimate --source day --scenario well_observed --estimator wls`
       — expect NO `RankDeficientError`; console shows trace_P + RMSE-proxy per step; run completes
       with "N snapshots written".
    6. Repeat step 5 with `--estimator ekf` then `--estimator ukf` — expect NO `S @ delta_p`
       ValueError; both run to completion.
    7. `uv run score --source day --scenario well_observed --estimator all` — expect voltage RMSE
       < 0.005 pu (R10), NEES in [61.76, 66.28] (R11) for the recursive filters.
    8. (Optional) repeat for `--source fault` to exercise dead-bus masking (R12) and P-inflation.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; live-pipeline acceptance (RMSE/NEES/no-crash) is validated interactively per <how-to-verify>. The automated structural + numerical gates for the rewire live in Task 1 and Task 2 (parse, injection_sensitivity wiring, S@delta_p round-trip, oracle grep).</automated>
  </verify>
  <done>All three estimators (wls/ekf/ukf) write to the estimates bucket with no RankDeficientError and no S@delta_p ValueError; score reports RMSE < 0.005 pu and NEES in [61.76, 66.28]; human types "approved".</done>
  <resume-signal>Type "approved" if all three estimators write and RMSE/NEES pass, or describe the failing bar (paste the console line).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT broker → estimate.py | Untrusted MQTT payloads (meas/netmodel/forecast) cross into the estimator; already validated by defensive key-checks. Broker localhost-bound (19c0422). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Tampering | forecast payload → Δp/Cov(ε) | mitigate | _handle_forecast validates keys; missing bus → 0.0; FASEPredictor asserts delta_p/S dimension match |
| T-10-10-02 | Denial of Service | missing forecast stalls estimator | mitigate | predict falls back to Δp:=0 (persistence prior) when no forecast — pipeline never deadlocks |
| T-10-10-03 | Tampering | S/Δp column-order mismatch (size-invisible) | mitigate | synthetic `x_prev + S @ delta_p` round-trip in Task 2 verify asserts finite (64,) in the documented column order |
</threat_model>

<verification>
- estimate.py parses; references `injection_sensitivity`; no `fase_sensitivity`/`inj_ml`/`W_inj`.
- Synthetic `S @ delta_p` round-trip is finite, shape (64,) in injection_sensitivity's documented order.
- Live run: WLS on well_observed writes with no RankDeficientError (bug #3 closed, R5/R10);
  EKF/UKF run predict→update to completion with no matmul error (bug #2 closed, R6/R7).
- score reports RMSE < 0.005 pu and NEES in band (R10/R11); fault run masks dead buses (R12).
- Oracle grep clean on estimate.py.
</verification>

<success_criteria>
- The three defects (#2 predict dimension, #3 WLS rank/state-dim) are closed end-to-end.
- S @ delta_p is order- and dimension-consistent (synthetic round-trip finite, shape (64,)).
- All three estimators write to the estimates bucket for buses 1..32.
- Acceptance bars R5/R6/R7/R10/R11/R12 re-satisfied on the live pipeline (human-verified).
</success_criteria>

<output>
Create `.planning/phases/10-system-2-streaming-distribution-state-estimator-mqtt-fase/10-10-SUMMARY.md` when done.
</output>
