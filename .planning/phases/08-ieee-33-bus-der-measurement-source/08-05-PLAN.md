---
phase: 08-ieee-33-bus-der-measurement-source
plan: 05
type: execute
wave: 4
depends_on: [01, 04]
files_modified:
  - system1-measurement-source/grafana/provisioning/datasources/influxdb.yml
  - system1-measurement-source/grafana/provisioning/dashboards/default.yml
  - system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json
  - system1-measurement-source/Makefile
  - system1-measurement-source/README.md
autonomous: false
requirements: [SPEC-7]

must_haves:
  truths:
    - "Grafana auto-provisions an InfluxDB Flux datasource (hard-coded ieee33-dev-token, not ${VAR}) pointing at the state bucket — no manual setup (SPEC-7, D-13)"
    - "A prebuilt dashboard renders the 96-step evolution of the SPEC panel set: bus-voltage profile/envelope, line loadings, total losses, DG solar/wind output, slack feed-in, OLTC tap (SPEC-7, D-13)"
    - "A Makefile wraps the runbook (up, ingest, sim, validate, all, down, clean) so the README rhythm maps to a few targets"
    - "The README documents the full ordered rhythm (uv sync → docker compose up → ingest → sim → open Grafana/InfluxDB) reproducing the 96-snapshot dataset from a clean checkout (SPEC-7, D-12)"
  artifacts:
    - path: "system1-measurement-source/grafana/provisioning/datasources/influxdb.yml"
      provides: "Provisioned InfluxDB 2.x Flux datasource"
      contains: "ieee33-dev-token"
    - path: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json"
      provides: "Prebuilt dashboard with the SPEC panel set over the state bucket"
      contains: "vm_pu"
    - path: "system1-measurement-source/Makefile"
      provides: "Runbook targets wrapping uv + docker compose"
      contains: "ingest"
    - path: "system1-measurement-source/README.md"
      provides: "Step-by-step reproducible runbook (the rhythm)"
      contains: "uv sync"
  key_links:
    - from: "system1-measurement-source/grafana/provisioning/datasources/influxdb.yml"
      to: "InfluxDB container (http://influxdb:8086, state bucket)"
      via: "provisioned Flux datasource with hard-coded token"
      pattern: "http://influxdb:8086"
    - from: "system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json"
      to: "InfluxDB state bucket measurements (bus/line/sgen/system)"
      via: "Flux panel queries"
      pattern: "from\\(bucket"
---

<objective>
Auto-provision Grafana (Flux datasource + a prebuilt dashboard covering the SPEC panel set over the `state` bucket), wrap the runbook in a Makefile, and write the README "rhythm" so the whole stack comes up and reproduces the 96-snapshot dataset from a clean checkout — with a human checkpoint to visually confirm the dashboard renders.

Purpose: SPEC-7 (provisioned Grafana dashboard rendering the 96-step evolution without manual setup; README runbook; pinned deps) and D-12/D-13. This makes the ground-truth dataset visible and the whole pipeline reproducible — the deliverable Juan demos in the interview.
Output: grafana/provisioning/** (datasource + dashboard provider + dashboard JSON), Makefile, README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-SPEC.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md
@system1-measurement-source/docker-compose.yml
@system1-measurement-source/src/ieee33/config.py
@system1-measurement-source/src/ieee33/influx.py

<interfaces>
<!-- State-bucket schema (from Plan 03 influx.write_state_step / D-07) the dashboard queries -->
bucket "state":
  measurement "bus":    tag bus_id;   fields vm_pu, va_degree
  measurement "line":   tag line_id;  fields p_from_mw, q_from_mvar, p_to_mw, q_to_mvar, loading_percent, pl_mw, ql_mvar
  measurement "sgen":   tag sgen_id;  fields p_mw, q_mvar
  measurement "system": fields total_load_mw, total_gen_mw, total_loss_mw, vmin_pu, vmax_pu, slack_p_mw, slack_q_mvar, tap_pos, shift_degree
Timestamps = real OPSD datetimes of config.TARGET_DATE (00:00-23:45 UTC).

<!-- Grafana/InfluxDB provisioning contract (RESEARCH §Pattern 6, verified docs 2026-06-23) -->
datasource: type influxdb, jsonData.version Flux, organization "ieee33", defaultBucket "state",
  url http://influxdb:8086 (container service name), secureJsonData.token "ieee33-dev-token" (LITERAL, not ${VAR} — Pitfall 8 / issue #89519).
dashboards provider: apiVersion 1, type file, options.path /etc/grafana/provisioning/dashboards.
Container provisioning path: /etc/grafana/provisioning (mounted in Plan 01 compose).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grafana provisioning — Flux datasource + dashboard provider + prebuilt dashboard JSON (SPEC panel set)</name>
  <files>system1-measurement-source/grafana/provisioning/datasources/influxdb.yml, system1-measurement-source/grafana/provisioning/dashboards/default.yml, system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 6 lines 495-532 datasource+provider YAML; Pitfall 8 hard-code token; §Code Examples Flux bus-18 query lines 671-679; §Architecture panel list line 186-187)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-CONTEXT.md (D-13 SPEC panel set)
    - system1-measurement-source/src/ieee33/config.py (TARGET_DATE for dashboard time range; org/bucket names)
    - system1-measurement-source/src/ieee33/influx.py (state schema = panel query source of truth)
  </read_first>
  <action>
    PART A — system1-measurement-source/grafana/provisioning/datasources/influxdb.yml (RESEARCH §Pattern 6 lines 497-515):
      apiVersion: 1
      datasources:
        - name: InfluxDB-IEEE33
          type: influxdb
          uid: ieee33-influxdb
          access: proxy
          url: http://influxdb:8086        # container-to-container service name
          jsonData:
            version: Flux
            organization: ieee33
            defaultBucket: state
            tlsSkipVerify: true
          secureJsonData:
            token: ieee33-dev-token          # LITERAL — do NOT use ${VAR} (Pitfall 8 / issue #89519)
          isDefault: true

    PART B — system1-measurement-source/grafana/provisioning/dashboards/default.yml (RESEARCH §Pattern 6 lines 522-531):
      apiVersion: 1
      providers:
        - name: IEEE33
          type: file
          updateIntervalSeconds: 30
          options:
            path: /etc/grafana/provisioning/dashboards

    PART C — system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json — a valid Grafana dashboard model (schemaVersion compatible with Grafana 11.x) with `uid` set, a `time` range covering config.TARGET_DATE (from `<TARGET_DATE>T00:00:00Z` to `<TARGET_DATE>T23:59:59Z`), and every panel's `datasource` referencing the datasource by uid "ieee33-influxdb" (type influxdb). Include the FULL SPEC panel set (D-13), each a timeseries panel with a Flux query over the state bucket (range start/stop on TARGET_DATE; pivot _time/_field where needed; sort by _time):
      1. "Bus Voltage Profile / Envelope" — measurement=="bus", field=="vm_pu" for the key DER buses (bus_id "17","21","24","32") plus a slack-side bus; y unit pu; threshold lines at 0.95 and 1.05.
      2. "Line Loadings (%)" — measurement=="line", field=="loading_percent" (a few representative line_ids, e.g. "0","1","2", or all).
      3. "Total System Losses (MW)" — measurement=="system", field=="total_loss_mw".
      4. "DG Output — Solar & Wind (MW)" — measurement=="sgen", field=="p_mw" grouped by sgen_id (so solar vs wind are distinguishable).
      5. "Slack / Substation Feed-in (MW)" — measurement=="system", field=="slack_p_mw" (negative => midday reverse power flow toward the substation).
      6. "OLTC Tap Position" — measurement=="system", field=="tap_pos" (stepped line; integer y).
    Validate the JSON parses. Keep all queries in Flux (the datasource is Flux mode).
  </action>
  <verify>
    <automated>cd system1-measurement-source && python3 -c "import json; json.load(open('grafana/provisioning/dashboards/ieee33-state.json')); print('dashboard JSON valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json'))"` exits 0 (valid JSON)
    - `grep -c 'ieee33-dev-token' system1-measurement-source/grafana/provisioning/datasources/influxdb.yml` returns 1 (literal token) AND `grep -c '\${' system1-measurement-source/grafana/provisioning/datasources/influxdb.yml` returns 0 (no env-var substitution — Pitfall 8)
    - `grep -c 'version: Flux' system1-measurement-source/grafana/provisioning/datasources/influxdb.yml` returns 1
    - `grep -c 'http://influxdb:8086' system1-measurement-source/grafana/provisioning/datasources/influxdb.yml` returns 1 (container service name, not localhost)
    - dashboard JSON references all six SPEC fields: `grep -cE 'vm_pu|loading_percent|total_loss_mw|slack_p_mw|tap_pos' system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json` >= 5 (each SPEC panel field present)
    - dashboard JSON references the sgen DG output panel: `grep -c '"sgen"' system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json` >= 1
    - every panel uses the provisioned datasource uid: `grep -c 'ieee33-influxdb' system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json` >= 1
  </acceptance_criteria>
  <done>Grafana provisioning is complete: a Flux datasource (literal token, container URL, state default bucket) and a valid dashboard JSON with all six SPEC panels (voltage profile, line loadings, losses, DG solar/wind, slack feed-in, OLTC tap), each bound to the provisioned datasource uid.</done>
</task>

<task type="auto">
  <name>Task 2: Makefile (runbook targets) + README (the ordered rhythm) + end-to-end clean-checkout reproducibility</name>
  <files>system1-measurement-source/Makefile, system1-measurement-source/README.md</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 7 lines 534-578 Makefile targets + uv entry points; §Environment Availability lines 752-769)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-CONTEXT.md (D-12 README rhythm, D-11 infra-only, D-13 Grafana no manual setup)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (README greenfield rationale lines 298-304; module-docstring "rhythm" spirit)
    - system1-measurement-source/docker-compose.yml (service/port names referenced in README)
    - system1-measurement-source/src/ieee33/config.py (TARGET_DATE, ports, bucket names referenced in README)
  </read_first>
  <action>
    PART A — system1-measurement-source/Makefile (RESEARCH §Pattern 7 lines 544-567). .PHONY targets:
      up:        docker compose up -d
      ingest:    depends on up; `uv run ingest`
      sim:       depends on up; `uv run sim`
      validate:  `uv run validate`
      all:       up ingest sim  (the full pipeline in order)
      down:      docker compose down
      clean:     down; remove the named docker volumes (influxdb-data, influxdb-config, grafana-data) using the actual compose project prefix (system1-measurement-source_*), with `2>/dev/null || true` so it is safe to re-run.
    Use TAB indentation (Makefile requirement). Each recipe is one or two shell lines.

    PART B — system1-measurement-source/README.md — the human runbook (D-12 "rhythm"), expanded from the PATTERNS module-docstring spirit. Sections:
      1. Title + one-paragraph what-this-is: System 1, the re-runnable IEEE 33-bus DER ground-truth measurement source; produces 96 full power-flow snapshots of a high-DER DE day in InfluxDB, visualized in Grafana. Note System 2 (virtual-sensing estimator) is a later phase.
      2. Prerequisites: Docker + Docker Compose v2, uv (Astral), Python 3.12 (verified versions from RESEARCH §Environment Availability). One-time network needed for the ingest fetch only.
      3. The Rhythm (ordered, copy-paste runnable), each step with the equivalent `make` target:
         a. `uv sync`                      (install pinned deps)
         b. `docker compose up -d`  (= `make up`)  — starts InfluxDB (127.0.0.1:8086) + Grafana (127.0.0.1:3000)
         c. `uv run validate`       (= `make validate`)  — base-case Baran & Wu anchor (≈0.913 pu at bus 18)
         d. `uv run ingest`         (= `make ingest`)    — one-time OPSD fetch → 96 profile points (HALTS with a clear message if OPSD is unreachable — no fallback; download the CSV manually and retry)
         e. `uv run sim`            (= `make sim`)        — 96 OLTC-regulated power flows → 96 full-state snapshots in the `state` bucket
         f. Open Grafana http://localhost:3000 (anonymous; dashboard "IEEE33 …" auto-provisioned — NO manual setup) and InfluxDB Data Explorer http://localhost:8086 (org ieee33, token ieee33-dev-token).
         g. `make all` runs b→e in one shot; `make clean` tears down + wipes volumes.
      4. What you should see (the interview narrative, D-01): on the high-DER day (TARGET_DATE), midday PV/wind pushes bus voltages up at the DER buses, slack feed-in goes negative (reverse power flow toward the substation), the OLTC taps to hold the 0.95–1.05 band, and total losses dip at noon.
      5. Data model: two buckets — `profiles` (one-time 96 load/solar/wind points) and `state` (per-run snapshots; per-entity measurements bus/line/sgen/system with bus_id/line_id/sgen_id tags). This is the forward contract for System 2.
      6. SECURITY note (threat model): the stack is LOCAL DEV ONLY. `ieee33-dev-token` and admin passwords are fixed development credentials, NOT production secrets; ports are bound to 127.0.0.1; Grafana anonymous access is for the local demo only. Do not deploy this as-is.
      7. Determinism: re-running `uv run sim` overwrites the same 96 snapshots in place (real OPSD timestamps as keys) → identical dataset every run.
  </action>
  <verify>
    <automated>cd system1-measurement-source && make -n all >/dev/null && grep -qE '^\t.*uv run ingest' Makefile && grep -qE '^\t.*uv run sim' Makefile && grep -q 'uv sync' README.md && grep -q 'docker compose up' README.md && grep -qi 'localhost:3000' README.md && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `make -n all` (dry-run) exits 0 and shows up → ingest → sim ordering
    - Makefile has TAB-indented recipes invoking `uv run ingest` and `uv run sim`: `grep -cE '^\t.*uv run (ingest|sim)' system1-measurement-source/Makefile` >= 2
    - `grep -cE '^(up|ingest|sim|validate|all|down|clean):' system1-measurement-source/Makefile` returns 7 (all targets present)
    - README documents the full rhythm: `grep -c 'uv sync' README.md`, `grep -c 'docker compose up' README.md`, `grep -ci 'uv run ingest' README.md`, `grep -ci 'uv run sim' README.md` each >= 1
    - README has the no-fallback note: `grep -ci 'unreachable\|no fallback\|download.*manually' system1-measurement-source/README.md` >= 1
    - README has the local-dev security note: `grep -ci 'local dev only\|not.*production\|127.0.0.1' system1-measurement-source/README.md` >= 1
  </acceptance_criteria>
  <done>Makefile wraps the ordered runbook (up/ingest/sim/validate/all/down/clean); README documents the full reproducible rhythm with the interview narrative, data model, determinism, and the local-dev security caveat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Human-verify the Grafana dashboard auto-renders all six SPEC panels with data</name>
  <what-built>
    The full stack is now provisioned and runnable end-to-end. Claude has (Plans 01-04) built the network, ingested 96 OPSD profile points, run the 96-step sim writing full state to InfluxDB, and (this plan) auto-provisioned the Grafana datasource + the SPEC-panel dashboard, plus the Makefile + README runbook. To bring everything up from clean: `cd system1-measurement-source && make clean && uv sync && make all`.
  </what-built>
  <how-to-verify>
    1. From a clean state run: `cd system1-measurement-source && make clean && uv sync && make all` (this does up → ingest → sim). Confirm `uv run sim` prints "sim OK — 96 snapshots written".
    2. Open Grafana at http://localhost:3000 (anonymous access; no login). The "IEEE33" dashboard should be present WITHOUT any manual datasource/dashboard setup (SPEC-7 / D-13 acceptance).
    3. Confirm all six panels render the 96-step (15-min) day with data (not "No data"):
       - Bus Voltage Profile/Envelope (voltages move; stays roughly in 0.95–1.05)
       - Line Loadings (%)
       - Total System Losses (MW) — should dip around midday
       - DG Output Solar & Wind (MW) — solar bell-curve midday, wind nonzero
       - Slack / Substation Feed-in (MW) — should trend toward/through zero (reverse flow) midday
       - OLTC Tap Position — should show at least one tap change (not flat at 0)
    4. Optionally open InfluxDB Data Explorer at http://localhost:8086 (org ieee33, token ieee33-dev-token) and confirm the `state` bucket has bus/line/sgen/system measurements.
  </how-to-verify>
  <resume-signal>Type "approved" if the dashboard auto-renders all six panels with data, or describe what is missing/broken (e.g., a panel shows "No data", OLTC never taps, dashboard not auto-provisioned).</resume-signal>
  <files>system1-measurement-source/grafana/provisioning/dashboards/ieee33-state.json (the dashboard being visually verified)</files>
  <action>PAUSE for human visual verification. Claude has already automated everything (provisioning + full pipeline via `make all`); this task only confirms the auto-provisioned Grafana dashboard renders. Do NOT skip: open Grafana and confirm the six panels show data, then wait for the human resume-signal.</action>
  <verify>Human confirms via the how-to-verify steps; resume on "approved". (Automated pre-check: `cd system1-measurement-source && make clean && uv sync && make all` completes and `uv run sim` prints "sim OK — 96 snapshots written" before the human opens Grafana.)</verify>
  <done>Human typed "approved": the IEEE33 dashboard auto-renders all six SPEC panels with the 96-step data, no manual setup (SPEC-7 / D-13 acceptance).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → Grafana container (localhost:3000) | Anonymous read on a localhost-bound demo dashboard |
| Grafana container → InfluxDB container (http://influxdb:8086) | Provisioned datasource with literal dev token |
| repo → committed datasource token | Literal `ieee33-dev-token` in provisioning YAML (local-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Information Disclosure | Literal `ieee33-dev-token` committed in provisioning YAML | accept | Required workaround for Grafana ${VAR} unreliability (Pitfall 8 / issue #89519); same fixed local-only dev token (T-08-02); both ports localhost-bound; README explicitly flags LOCAL DEV ONLY / not for deployment |
| T-08-15 | Spoofing / Elevation | Grafana anonymous access | accept | Read-only anonymous on a localhost-bound interview demo; documented in README security note; rationale recorded |
| T-08-16 | Tampering | Dashboard provisioned with a wrong/blank datasource silently shows "No data" | mitigate | Datasource bound by literal token + uid; the human-verify checkpoint explicitly confirms all six panels render real data (not "No data") before sign-off |
</threat_model>

<verification>
- `cd system1-measurement-source && python3 -c "import json; json.load(open('grafana/provisioning/dashboards/ieee33-state.json'))"` parses.
- Datasource YAML uses a literal token (no `${`), Flux mode, container URL, state default bucket.
- `make -n all` shows up → ingest → sim; README documents the full rhythm + no-fallback + security caveats.
- Human checkpoint: from clean (`make clean && uv sync && make all`) Grafana auto-renders all six SPEC panels with the 96-step data, no manual setup.
</verification>

<success_criteria>
- SPEC-7: Grafana opens and the provisioned dashboard renders the 96-step evolution of the key variables without manual setup; README reproduces the 96-snapshot dataset from a clean state; deps pinned.
- D-12: README documents the ordered rhythm; Makefile maps it to targets.
- D-13: the SPEC panel set is present and auto-provisioned with the InfluxDB datasource.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ieee-33-bus-der-measurement-source/08-05-SUMMARY.md`
</output>
