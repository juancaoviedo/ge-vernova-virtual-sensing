---
phase: 08-ieee-33-bus-der-measurement-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - system1-measurement-source/pyproject.toml
  - system1-measurement-source/uv.lock
  - system1-measurement-source/.env.example
  - system1-measurement-source/docker-compose.yml
  - system1-measurement-source/src/ieee33/__init__.py
  - system1-measurement-source/src/ieee33/config.py
  - system1-measurement-source/scripts/inspect_opsd_day.py
  - .gitignore
autonomous: true
requirements: [SPEC-6]
user_setup:
  - service: docker
    why: "InfluxDB 2.x + Grafana run as local containers via Docker Compose (infra-only; D-11)"
    env_vars: []
    dashboard_config:
      - task: "Ensure Docker Engine + Docker Compose v2 are running locally (verified available: Docker 28.1.1, Compose v2.35.1)"
        location: "Local machine"

must_haves:
  truths:
    - "A dedicated top-level folder system1-measurement-source/ exists, separate from docs/ (D-09)"
    - "uv sync installs pinned pandapower/influxdb-client/pandas/numpy and produces uv.lock (D-10)"
    - "docker compose config validates an InfluxDB 2.9.1 + Grafana 11.6.15 infra-only stack with localhost-bound ports (D-11)"
    - "config.py pins TARGET_DATE (a real high-DER DE day with 96 non-null profile rows) and the DG scaling decision as documented constants (D-15, D-04)"
    - "config.py pins the data-shaping decisions as constants the ingest/sim consume: a high-DER day selection (D-01), DG scaled by the 0–1 DE_solar_profile/DE_wind_profile columns × nameplate with load peak-normalized (D-02), the article's 3.715 MW anchored as the daily PEAK load (D-03), and the solar→buses 18,22 / wind→buses 25,33 split (D-05)"
  artifacts:
    - path: "system1-measurement-source/pyproject.toml"
      provides: "uv project metadata + pinned deps + [project.scripts] entry points (ingest/sim/validate)"
      contains: "pandapower==3.4.0"
    - path: "system1-measurement-source/docker-compose.yml"
      provides: "InfluxDB 2.9.1 + Grafana 11.6.15 infra-only services"
      contains: "influxdb:2.9.1"
    - path: "system1-measurement-source/src/ieee33/config.py"
      provides: "All physical/data constants: OPSD_URL, TARGET_DATE, DG nameplates+scaling, bus index maps, INFLUXDB_* from dotenv"
      contains: "TARGET_DATE"
    - path: "system1-measurement-source/scripts/inspect_opsd_day.py"
      provides: "One-time data-inspection script that selects + justifies TARGET_DATE"
      contains: "DE_solar_profile"
  key_links:
    - from: "system1-measurement-source/src/ieee33/config.py"
      to: ".env (INFLUXDB_URL/TOKEN/ORG)"
      via: "python-dotenv load_dotenv()"
      pattern: "load_dotenv"
    - from: "system1-measurement-source/docker-compose.yml"
      to: "grafana/provisioning"
      via: "volume mount ./grafana/provisioning:/etc/grafana/provisioning"
      pattern: "provisioning"
---

<objective>
Stand up the greenfield System 1 project scaffold and the infra-only Docker Compose stack, and make the two locked one-time data decisions (TARGET_DATE selection per D-15; DG scaling per D-04) — pinning every constant the downstream builder/ingest/sim plans consume.

Purpose: This is the first hands-on build phase in a study-notes repo. Establish clean conventions (a dedicated top-level folder, uv tooling, Docker Compose for infra only, sim on host) and resolve the data unknowns (which DE day, what DG scaling) ONCE so Wave 2/3 plans run deterministically. config.py becomes the single source of constants.
Output: system1-measurement-source/ with pyproject.toml + uv.lock, docker-compose.yml (InfluxDB 2.9.1 + Grafana 11.6.15), .env.example, src/ieee33/ package skeleton with config.py, a one-time inspect_opsd_day.py, and .gitignore updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-SPEC.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-CONTEXT.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md
@.planning/research/articles/case33.xlsx

<interfaces>
<!-- Verified canonical data extracted from case33.xlsx (DG/shunt/OLTC tables) on 2026-06-23. -->
<!-- Downstream plans consume these from config.py — no further xlsx inspection needed. -->

DG nameplate data (case33.xlsx "generator data" rows 62-65):
  bus 18 → Pmax 0.2 MW   (Very High Renewable Generation note: Pmax 0.8)
  bus 22 → Pmax 0.2 MW
  bus 25 → Pmax 0.2 MW
  bus 33 → Pmax 0.2 MW
  Renewable rated capacity = 200 kW per row 113.
  => 4 × 0.2 MW = 0.8 MW total = ~21% of 3.715 MW peak (too small → D-04 scaling).

RPC shunt capacitors (case33.xlsx rows 40, 55):
  bus 18 → 0.4 MVAr capacitive (radial)
  bus 33 → 0.6 MVAr capacitive (radial)
  In pandapower: capacitive shunt = NEGATIVE q_mvar.

Tie-lines (case33.xlsx rows 102-104): branches 33 (21-8), 34 (12-22), 35 (25-29) →
  "linestatus: switchable 0 for radial" → OPEN in radial config.

OLTC / phase shifter (case33.xlsx rows 118-119):
  OLTC branch 1 ratio -0.05 to 0.05 (= 0.95-1.05 pu).
  Phase shifter branch 1 angle -5 to 5 degrees.

Bus index map: article bus N = pandapower index N-1.
  article 18→idx17, 22→idx21, 25→idx24, 33→idx32.

OPSD source (RESEARCH §Pattern 3, verified HTTP 200 2026-06-23):
  URL: https://data.open-power-system-data.org/time_series/2020-10-06/time_series_15min_singleindex.csv
  Columns: utc_timestamp, DE_load_actual_entsoe_transparency, DE_solar_profile (0-1), DE_wind_profile (0-1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create uv project scaffold, package skeleton, and .gitignore updates</name>
  <files>system1-measurement-source/pyproject.toml, system1-measurement-source/uv.lock, system1-measurement-source/.env.example, system1-measurement-source/src/ieee33/__init__.py, .gitignore</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Standard Stack lines 116-136 for exact pyproject deps; §Pattern 7 lines 536-542 for [project.scripts])
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (pyproject.toml + .env.example greenfield rationale)
    - .gitignore (current state — has __pycache__/ but NOT .venv or .env)
  </read_first>
  <action>
    Create the dedicated top-level folder system1-measurement-source/ (D-09: separate from docs/).

    1. Write system1-measurement-source/pyproject.toml with EXACTLY (RESEARCH §Standard Stack):
       [project]
       name = "ieee33-measurement-source"
       version = "0.1.0"
       requires-python = ">=3.12"
       dependencies = [
         "pandapower==3.4.0",
         "influxdb-client==1.50.0",
         "pandas>=2.3",
         "numpy>=1.26,<2.4",
         "requests>=2.32",
         "python-dotenv>=1.0",
       ]
       [project.scripts]
       ingest   = "ieee33.ingest:main"
       sim      = "ieee33.sim:main"
       validate = "ieee33.validate:main"
       Add a [build-system] using hatchling and [tool.hatch.build.targets.wheel] packages = ["src/ieee33"] so the src/ layout installs the ieee33 package. Add openpyxl>=3.1 to dependencies (needed by the Wave-0 inspect script that reads case33.xlsx). Do NOT add the pandapower[control] extra — DiscreteTapControl is in base install (RESEARCH note line 136).
    2. Create system1-measurement-source/src/ieee33/__init__.py (empty or a single-line package docstring marker).
    3. Run `uv sync` (cwd = system1-measurement-source) to install deps and generate uv.lock. This satisfies the RESEARCH Wave-0 gap "uv sync run to generate uv.lock".
    4. Write system1-measurement-source/.env.example with placeholder values (never the real .env):
       INFLUXDB_URL=http://localhost:8086
       INFLUXDB_TOKEN=ieee33-dev-token
       INFLUXDB_ORG=ieee33
       Add a comment line: "# LOCAL DEV ONLY — ieee33-dev-token is a fixed development credential, not a production secret."
    5. Also create the real system1-measurement-source/.env by copying .env.example values (so uv run ingest/sim work locally); it will be gitignored in the next step.
    6. Append to the repo-root .gitignore these entries (they are missing): a section header "# Phase 8 System 1" then:
       system1-measurement-source/.env
       system1-measurement-source/.venv/
       *.egg-info/
       (keep uv.lock TRACKED — do not ignore it; pinned lock is required by D-10.)
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv sync && uv run python -c "import pandapower, influxdb_client, pandas, numpy, requests, dotenv; print('deps OK')" && test -f uv.lock && echo LOCK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `system1-measurement-source/pyproject.toml` exists and `grep -c 'pandapower==3.4.0' system1-measurement-source/pyproject.toml` returns 1
    - `grep -c 'influxdb-client==1.50.0' system1-measurement-source/pyproject.toml` returns 1
    - All three entry points present: `grep -cE '^(ingest|sim|validate) *=' system1-measurement-source/pyproject.toml` returns 3
    - `system1-measurement-source/uv.lock` exists (file present)
    - `cd system1-measurement-source && uv run python -c "import pandapower, influxdb_client"` exits 0
    - `system1-measurement-source/.env.example` contains `INFLUXDB_TOKEN=ieee33-dev-token`
    - `grep -c 'system1-measurement-source/.env' .gitignore` returns at least 1
    - `grep -c 'uv.lock' .gitignore` returns 0 (uv.lock NOT ignored)
  </acceptance_criteria>
  <done>uv project builds, deps install, uv.lock pinned and tracked, package importable, .env gitignored, .env.example committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author docker-compose.yml (InfluxDB 2.9.1 + Grafana 11.6.15, infra-only, localhost-bound)</name>
  <files>system1-measurement-source/docker-compose.yml</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 5 lines 452-493 — full compose template + bootstrap-order + second-bucket landmines)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-CONTEXT.md (D-06 two buckets, D-11 infra-only, D-13 Grafana provisioning)
  </read_first>
  <action>
    Write system1-measurement-source/docker-compose.yml deploying INFRA ONLY (D-11 — the sim is NOT containerized). Two services, copying RESEARCH §Pattern 5 with concrete values:

    services:
      influxdb:
        image: influxdb:2.9.1
        ports:
          - "127.0.0.1:8086:8086"      # SECURITY: bind to localhost only, not 0.0.0.0
        environment:
          DOCKER_INFLUXDB_INIT_MODE: setup
          DOCKER_INFLUXDB_INIT_USERNAME: admin
          DOCKER_INFLUXDB_INIT_PASSWORD: ieee33-admin-pw   # local dev only
          DOCKER_INFLUXDB_INIT_ORG: ieee33
          DOCKER_INFLUXDB_INIT_BUCKET: profiles            # creates ONLY profiles; 'state' created programmatically (Plan 03)
          DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: ieee33-dev-token
        volumes:
          - influxdb-data:/var/lib/influxdb2
          - influxdb-config:/etc/influxdb2
        healthcheck:
          test: ["CMD", "influx", "ping"]
          interval: 5s
          timeout: 3s
          retries: 10

      grafana:
        image: grafana/grafana:11.6.15
        ports:
          - "127.0.0.1:3000:3000"      # SECURITY: localhost only
        environment:
          GF_SECURITY_ADMIN_PASSWORD: admin
          GF_AUTH_ANONYMOUS_ENABLED: "true"   # local-only convenience; no login for demo
        volumes:
          - ./grafana/provisioning:/etc/grafana/provisioning   # Plan 05 fills this dir
          - grafana-data:/var/lib/grafana
        depends_on:
          influxdb:
            condition: service_started

    volumes:
      influxdb-data:
      influxdb-config:
      grafana-data:

    Add a top-of-file comment: "# LOCAL DEV STACK ONLY. ieee33-dev-token / admin passwords are fixed dev credentials, NOT production secrets. Ports bound to 127.0.0.1." (Honors the threat model: dev token marked local-only, ports localhost-bound.)
    Note: the ./grafana/provisioning directory is created by Plan 05; create an empty placeholder system1-measurement-source/grafana/provisioning/.gitkeep here so the bind mount path exists and `docker compose config` validates.
  </action>
  <verify>
    <automated>cd system1-measurement-source && docker compose config >/dev/null && echo COMPOSE_VALID</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f system1-measurement-source/docker-compose.yml config` exits 0
    - `grep -c 'influxdb:2.9.1' system1-measurement-source/docker-compose.yml` returns 1
    - `grep -c 'grafana/grafana:11.6.15' system1-measurement-source/docker-compose.yml` returns 1
    - `grep -c '127.0.0.1:8086:8086' system1-measurement-source/docker-compose.yml` returns 1 (localhost-bound)
    - `grep -c '127.0.0.1:3000:3000' system1-measurement-source/docker-compose.yml` returns 1
    - `grep -c 'DOCKER_INFLUXDB_INIT_BUCKET: profiles' system1-measurement-source/docker-compose.yml` returns 1
    - `grep -c 'ieee33-dev-token' system1-measurement-source/docker-compose.yml` returns 1
    - placeholder exists: `test -f system1-measurement-source/grafana/provisioning/.gitkeep`
  </acceptance_criteria>
  <done>`docker compose config` validates; both pinned images present; ports localhost-bound; only `profiles` bucket auto-created; provisioning mount path exists.</done>
</task>

<task type="auto">
  <name>Task 3: Select + pin TARGET_DATE (D-15) via one-time data inspection, and author config.py with all constants</name>
  <files>system1-measurement-source/scripts/inspect_opsd_day.py, system1-measurement-source/src/ieee33/config.py</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 3 lines 338-393 OPSD columns + day-selection; Open Question #1 lines 734-737 inspection recipe; Pitfall 7 lines 651-656 NaN check; §Pattern 1 constants)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (config.py constants-block pattern lines 234-249; module docstring convention lines 310-329)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-CONTEXT.md (D-01 high-DER day, D-02 scaling columns, D-03 peak load anchor, D-04 DG scaling, D-05 solar/wind bus split, D-15 hard-code day)
    - docs/demos/ekf_line_temp_demo.py (constants-block style lines 40-48 — units in inline comments)
  </read_first>
  <action>
    PART A — system1-measurement-source/scripts/inspect_opsd_day.py (one-time, run once to PICK the day):
    Write a standalone script (module docstring per PATTERNS convention: description, Dependencies, Run: `uv run python scripts/inspect_opsd_day.py`, Effect: prints candidate high-DER days). It:
    - HEAD-checks OPSD_URL (https://data.open-power-system-data.org/time_series/2020-10-06/time_series_15min_singleindex.csv); exit non-zero with clear message if unreachable (no fallback — SPEC req 3 ethos).
    - Chunked-reads (pd.read_csv usecols=["utc_timestamp","DE_load_actual_entsoe_transparency","DE_solar_profile","DE_wind_profile"], parse_dates, chunksize=50_000), groups by date for summer 2017-2019 (months 6,7,8), computes per-day: count of rows (must==96), solar_profile daily mean+max, wind_profile daily mean, and NaN count across the three columns.
    - Prints the top ~10 days ranked by (high solar mean AND wind mean > 0.3 AND zero NaN AND exactly 96 rows). Recommends the single best day.
    RUN this script during execution, read its output, and choose the concrete best day (a real 2017-2019 DE summer day with high solar CF + wind mean > 0.3 + exactly 96 non-null rows). Record that chosen date.

    PART B — system1-measurement-source/src/ieee33/config.py (pure constants, no I/O side effects beyond dotenv load; PATTERNS constants-block style with inline unit comments):
    """ module docstring: config constants; Dependencies: python-dotenv; Effect: provides constants """
    from dotenv import load_dotenv; import os; load_dotenv()
    Define exactly (copy concrete values; bus indices are pandapower 0-indexed = article N-1):
    - OPSD_URL = "https://data.open-power-system-data.org/time_series/2020-10-06/time_series_15min_singleindex.csv"
    - TARGET_DATE = "<the date chosen from Part B inspection output>"   # D-15: high-DER DE day, 96 non-null rows, hard-coded
    - OPSD_COLS = ["utc_timestamp","DE_load_actual_entsoe_transparency","DE_solar_profile","DE_wind_profile"]
    - N_STEPS = 96                       # 96 × 15-min = one day
    - PEAK_LOAD_MW = 3.715               # D-03: article total demand anchored as daily PEAK
    - PEAK_LOAD_MVAR = 2.3               # article total reactive demand at peak
    - DG_NAMEPLATE_MW = 0.2              # case33.xlsx generator data: 200 kW each (buses 18/22/25/33)
    - DG_SCALE_FACTOR = 2.8              # D-04: scale 0.2 MW -> 0.56 MW (~60% of peak / 4 units) for visible midday physics; documented deliberate deviation
    - DG_EFFECTIVE_MW = DG_NAMEPLATE_MW * DG_SCALE_FACTOR   # = 0.56 MW per unit
    - SOLAR_BUSES = [17, 21]            # D-05: article buses 18, 22 (pandapower idx)
    - WIND_BUSES  = [24, 32]            # D-05: article buses 25, 33 (pandapower idx)
    - RPC_SHUNTS = {17: -0.4, 32: -0.6} # bus 18: 0.4 MVAr cap, bus 33: 0.6 MVAr cap; capacitive = NEGATIVE q_mvar
    - TIE_LINE_IDX = [32, 33, 34]       # article branches 33/34/35 = pandapower line idx; OPEN for radial
    - OLTC params: TRAFO_SN_MVA=10.0, TRAFO_VK_PERCENT=4.0, TRAFO_VKR_PERCENT=0.5, TAP_MIN=-5, TAP_MAX=5, TAP_STEP_PERCENT=1.0, TAP_NEUTRAL=0, VM_LOWER_PU=0.95, VM_UPPER_PU=1.05, SHIFT_DEGREE=0.0
    - VALIDATION: BARANWU_VMIN_PU=0.913, BARANWU_VMIN_BUS=17, BARANWU_TOL=0.005, VBAND_LOW=0.95, VBAND_HIGH=1.05
    - InfluxDB: INFLUXDB_URL=os.getenv("INFLUXDB_URL","http://localhost:8086"); INFLUXDB_TOKEN=os.getenv("INFLUXDB_TOKEN","ieee33-dev-token"); INFLUXDB_ORG=os.getenv("INFLUXDB_ORG","ieee33"); PROFILES_BUCKET="profiles"; STATE_BUCKET="state"
    Add a comment above DG_SCALE_FACTOR documenting it as a deliberate, explicit deviation from the article's 200 kW baseline (D-04), with rationale (visible midday reverse flow / voltage rise / OLTC tapping).
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run python -c "from ieee33 import config as c; assert c.N_STEPS==96; assert abs(c.DG_EFFECTIVE_MW-0.56)<1e-9; assert c.SOLAR_BUSES==[17,21] and c.WIND_BUSES==[24,32]; assert c.RPC_SHUNTS=={17:-0.4,32:-0.6}; import datetime; datetime.date.fromisoformat(c.TARGET_DATE); print('config OK', c.TARGET_DATE)"</automated>
  </verify>
  <acceptance_criteria>
    - `system1-measurement-source/scripts/inspect_opsd_day.py` exists and `grep -c 'DE_solar_profile' system1-measurement-source/scripts/inspect_opsd_day.py` >= 1
    - config import succeeds and `c.TARGET_DATE` parses as an ISO date in 2017-2019 summer (verify the chosen value is a real date, format YYYY-MM-DD)
    - `grep -c 'DG_SCALE_FACTOR' system1-measurement-source/src/ieee33/config.py` >= 1 AND a comment line referencing "D-04" / "deviation" is present (`grep -ci 'deviation' system1-measurement-source/src/ieee33/config.py` >= 1)
    - `grep -c 'TIE_LINE_IDX' system1-measurement-source/src/ieee33/config.py` returns at least 1 and the value list is `[32, 33, 34]`
    - `grep -c 'RPC_SHUNTS' system1-measurement-source/src/ieee33/config.py` >= 1 with negative MVAr values (`-0.4`, `-0.6`)
    - Assertions in the verify command all pass (N_STEPS==96, DG_EFFECTIVE_MW≈0.56, bus splits, shunt map)
  </acceptance_criteria>
  <done>TARGET_DATE chosen from real OPSD inspection and hard-coded; config.py holds every downstream constant with correct pandapower indices and signs; import-time assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host sim → InfluxDB container (localhost:8086) | Local-only auth via fixed dev token; ports bound to 127.0.0.1 |
| host browser → Grafana container (localhost:3000) | Anonymous read enabled for local demo; localhost-bound |
| repo → committed secrets | Dev token + admin password live in compose/config (local-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | InfluxDB/Grafana ports exposed on all interfaces | mitigate | Bind both ports to `127.0.0.1` in docker-compose.yml (not `0.0.0.0`); verified by grep gate in Task 2 |
| T-08-02 | Information Disclosure | `ieee33-dev-token` / admin password committed to repo | accept | Local single-user interview demo; token explicitly marked "LOCAL DEV ONLY — not a production secret" in compose comment + .env.example; documented as such in README (Plan 05). No real data, no remote exposure |
| T-08-03 | Spoofing/Elevation | Grafana anonymous auth enabled | accept | Anonymous access is read-only on a localhost-bound demo; rationale documented; not for any real deployment |
| T-08-04 | Tampering | Real `.env` committed leaking local config | mitigate | `.gitignore` excludes `system1-measurement-source/.env`; only `.env.example` (placeholders) is tracked; grep gate in Task 1 |
</threat_model>

<verification>
- `cd system1-measurement-source && uv sync` succeeds and uv.lock is generated and tracked.
- `cd system1-measurement-source && docker compose config` exits 0 with both pinned images and localhost-bound ports.
- `cd system1-measurement-source && uv run python -c "from ieee33 import config"` succeeds; TARGET_DATE is a real high-DER DE day.
- `.gitignore` excludes the real `.env` and `.venv/` but tracks `uv.lock`.
</verification>

<success_criteria>
- Dedicated top-level folder system1-measurement-source/ created (D-09).
- uv project with pinned deps + uv.lock; package `ieee33` importable (D-10).
- Infra-only Docker Compose validates (InfluxDB 2.9.1 + Grafana 11.6.15), ports localhost-bound (D-11, threat model).
- TARGET_DATE selected from a real OPSD inspection and hard-coded (D-15); DG scaling decided and documented (D-04); all downstream constants pinned in config.py with correct pandapower indices and shunt signs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ieee-33-bus-der-measurement-source/08-01-SUMMARY.md`
</output>
