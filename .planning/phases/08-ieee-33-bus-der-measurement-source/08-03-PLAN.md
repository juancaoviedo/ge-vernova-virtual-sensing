---
phase: 08-ieee-33-bus-der-measurement-source
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - system1-measurement-source/src/ieee33/influx.py
  - system1-measurement-source/src/ieee33/ingest.py
autonomous: true
requirements: [SPEC-3, SPEC-6]

must_haves:
  truths:
    - "uv run ingest fetches the OPSD 15-min DE profiles ONCE and writes exactly 96 timestamped (load_pu, solar_pu, wind_pu) points to the InfluxDB profiles bucket (SPEC-3)"
    - "If the OPSD endpoint is unreachable, ingest exits non-zero with a clear 'provide the file' message and writes NO substitute data — no synthetic/xlsx fallback (SPEC-3)"
    - "The state bucket is created programmatically (existence-checked) since Docker init only creates the profiles bucket — yielding the two-bucket layout profiles + state (D-06, SPEC-6, RESEARCH Pitfall 4)"
    - "influx.py state-write helpers use per-entity-type measurements (bus, line, sgen, system) with bus_id/line_id tags so a single-variable 96-point query works (D-07)"
    - "A Flux query against the profiles bucket returns 96 points for the chosen day (SPEC-6)"
  artifacts:
    - path: "system1-measurement-source/src/ieee33/influx.py"
      provides: "Shared InfluxDB helpers: client factory, readiness poll, ensure_bucket(), profiles read query, state write helpers"
      contains: "def ensure_bucket"
    - path: "system1-measurement-source/src/ieee33/ingest.py"
      provides: "ingest.main() — one-time OPSD fetch → normalize → write profiles bucket; halt+notify on unreachable"
      contains: "def main"
  key_links:
    - from: "system1-measurement-source/src/ieee33/ingest.py"
      to: "InfluxDB profiles bucket"
      via: "influxdb-client write_api SYNCHRONOUS Point writes"
      pattern: "write_api|Point"
    - from: "system1-measurement-source/src/ieee33/ingest.py"
      to: "OPSD CSV endpoint"
      via: "requests.head reachability check + pd.read_csv chunked"
      pattern: "requests.head|read_csv"
    - from: "system1-measurement-source/src/ieee33/influx.py"
      to: "InfluxDB state bucket"
      via: "buckets_api create_bucket with existence check"
      pattern: "buckets_api|create_bucket"
---

<objective>
Implement the shared InfluxDB helper module and the one-time OPSD profile-ingestion entry point: fetch the locked TARGET_DATE's DE 15-min load/solar/wind, normalize, and write exactly 96 points to the `profiles` bucket — halting loudly (no fallback) if the endpoint is unreachable — and programmatically create the `state` bucket the sim will use.

Purpose: SPEC-3 (one-time ingest into InfluxDB; no fallback) and SPEC-6 (local InfluxDB persistence; programmatic second bucket). The sim (Plan 04) reads profiles FROM InfluxDB, so the data and the schema/bucket plumbing must exist first. The halt+notify-no-fallback behavior is an explicit, locked SPEC boundary.
Output: influx.py (reusable client/bucket/query/write helpers) and ingest.py (the ingest entry point).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-SPEC.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md
@system1-measurement-source/src/ieee33/config.py

<interfaces>
<!-- config.py constants consumed (created in Plan 01) -->
OPSD_URL, TARGET_DATE, OPSD_COLS, N_STEPS=96, PEAK_LOAD_MW=3.715
INFLUXDB_URL, INFLUXDB_TOKEN, INFLUXDB_ORG, PROFILES_BUCKET="profiles", STATE_BUCKET="state"

<!-- influxdb-client 1.50.0 contracts (RESEARCH §Pattern 4-5, verified Context7 2026-06-23) -->
from influxdb_client import InfluxDBClient, Point
from influxdb_client.client.write_api import SYNCHRONOUS
client = InfluxDBClient(url=..., token=..., org=...)
client.write_api(write_options=SYNCHRONOUS).write(bucket=..., org=..., record=[Point(...)...])
client.buckets_api().find_bucket_by_name(name) -> bucket|None ; .create_bucket(bucket_name=..., org_id=...)
client.query_api().query_data_frame(flux) -> pandas DataFrame
client.ping() / GET {url}/ping -> 204 when ready
Idempotency (D-08): same measurement+field+timestamp overwrites in place (no duplicate rows).

<!-- OPSD contract (RESEARCH §Pattern 3, verified HTTP 200 2026-06-23) -->
URL covers 2015-01-01..~2020-10-06; utc_timestamp ISO8601 UTC, 15-min steps.
DE_solar_profile / DE_wind_profile are pre-normalized 0-1. load_pu = DE_load / daily peak.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement influx.py — client factory, readiness poll, ensure_bucket, profiles read, state write helpers</name>
  <files>system1-measurement-source/src/ieee33/influx.py</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 4 lines 395-449 write helpers; §Pattern 5 lines 491-493 second-bucket landmine; Pitfall 3 readiness poll; Pitfall 4 state bucket; §Pattern 7 lines 569-578 profiles Flux query)
    - system1-measurement-source/src/ieee33/config.py (INFLUXDB_* + bucket names)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (module docstring convention; fail-loud ethos)
  </read_first>
  <action>
    Write system1-measurement-source/src/ieee33/influx.py as a shared helper LIBRARY module (no __main__ runner). Module docstring (Dependencies: influxdb-client, pandas; Effect: InfluxDB connection + bucket + read/write helpers shared by ingest.py and sim.py). Imports: `import time`, `import requests`, `from influxdb_client import InfluxDBClient, Point`, `from influxdb_client.client.write_api import SYNCHRONOUS`, `from ieee33 import config`.

    Implement:
    1. `def get_client() -> InfluxDBClient:` returns InfluxDBClient(url=config.INFLUXDB_URL, token=config.INFLUXDB_TOKEN, org=config.INFLUXDB_ORG).
    2. `def wait_for_influx(timeout_s=60) -> None:` (Pitfall 3 bootstrap order) poll `GET {config.INFLUXDB_URL}/ping` in a loop with 1s sleep until HTTP 204 (or 200), raising RuntimeError after timeout_s. This guards first-run `docker compose up` + immediate ingest.
    3. `def ensure_bucket(client, name: str) -> None:` (Pitfall 4) bucket_api = client.buckets_api(); if bucket_api.find_bucket_by_name(name) is None: resolve org id via client.organizations_api().find_organizations(org=config.INFLUXDB_ORG)[0].id and bucket_api.create_bucket(bucket_name=name, org_id=<id>). Idempotent: do nothing if it already exists. Print "bucket '<name>' ready".
    4. `def read_profiles(client) -> pandas.DataFrame:` build the Flux query over PROFILES_BUCKET for TARGET_DATE (RESEARCH §Pattern 7 lines 572-578): range start `<TARGET_DATE>T00:00:00Z` stop `<TARGET_DATE + 1 day>T00:00:00Z`, filter _measurement=="profiles", pivot rowKey ["_time"] columnKey ["_field"], sort by _time. Return query_api().query_data_frame(flux) with columns load_pu/solar_pu/wind_pu + _time. Assert exactly config.N_STEPS rows; raise with a clear message otherwise.
    5. `def count_profiles(client) -> int:` returns the number of rows from read_profiles (used by ingest idempotency + the SPEC-6 96-point acceptance check).
    6. `def write_profiles(write_api, df) -> None:` per RESEARCH §Pattern 4 lines 403-414: one Point("profiles") per row with float fields load_pu/solar_pu/wind_pu and .time(utc timestamp, UTC tz); write to PROFILES_BUCKET, org=config.INFLUXDB_ORG, SYNCHRONOUS.
    7. State-write helpers for Plan 04 (RESEARCH §Pattern 4 lines 419-449), to keep the schema in one place (D-07 per-entity measurements with tags):
       `def write_state_step(write_api, timestamp, res_bus, res_line, res_sgen, res_ext_grid, res_trafo, tap_pos, shift_degree, system_dict) -> None:` build Points:
         - measurement "bus", tag bus_id, fields vm_pu, va_degree (one per bus row)
         - measurement "line", tag line_id, fields p_from_mw, q_from_mvar, p_to_mw, q_to_mvar, loading_percent, pl_mw, ql_mvar (one per in-service line)
         - measurement "sgen", tag sgen_id, fields p_mw, q_mvar (one per sgen)
         - measurement "system", fields total_load_mw, total_gen_mw, total_loss_mw, vmin_pu, vmax_pu, slack_p_mw, slack_q_mvar, tap_pos, shift_degree
         all .time(timestamp). Write to STATE_BUCKET, org=config.INFLUXDB_ORG, SYNCHRONOUS.
       Document the exact field list in the docstring as the forward contract for System 2.
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run python -c "from ieee33 import influx; assert all(hasattr(influx,f) for f in ['get_client','wait_for_influx','ensure_bucket','read_profiles','count_profiles','write_profiles','write_state_step']); print('influx helpers OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def ensure_bucket' system1-measurement-source/src/ieee33/influx.py` returns 1
    - `grep -c 'def wait_for_influx' system1-measurement-source/src/ieee33/influx.py` returns 1 (readiness poll, Pitfall 3)
    - `grep -c 'def write_state_step' system1-measurement-source/src/ieee33/influx.py` returns 1 (forward-contract schema for Plan 04 / System 2)
    - `grep -c 'SYNCHRONOUS' system1-measurement-source/src/ieee33/influx.py` >= 1
    - `grep -c 'find_bucket_by_name' system1-measurement-source/src/ieee33/influx.py` returns 1 (existence-checked create)
    - import verify command exits 0 with all 7 helper names present
    - `grep -c 'if __name__' system1-measurement-source/src/ieee33/influx.py` returns 0 (library module)
  </acceptance_criteria>
  <done>influx.py provides client factory, readiness poll, idempotent ensure_bucket, profiles read/count, profiles write, and the per-entity state-write helper that encodes the D-07 schema (forward contract for System 2).</done>
</task>

<task type="auto">
  <name>Task 2: Implement ingest.py — one-time OPSD fetch → normalize → write profiles bucket (halt+notify, no fallback)</name>
  <files>system1-measurement-source/src/ieee33/ingest.py</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 3 lines 338-393 fetch+normalize+halt; Pitfall 7 NaN check; §Pattern 4 idempotency lines 417)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (ingest.py entry-point + fail-loud + validation-gate pattern lines 89-152)
    - system1-measurement-source/src/ieee33/influx.py (helpers this calls: get_client, wait_for_influx, ensure_bucket, write_profiles, count_profiles)
    - system1-measurement-source/src/ieee33/config.py (OPSD_URL, TARGET_DATE, OPSD_COLS, N_STEPS, PEAK_LOAD_MW, bucket names)
    - docs/build_site.py (main() fail-loud + validation-gate ethos: lines 1016-1076, validate_links 945-1009)
  </read_first>
  <action>
    Write system1-measurement-source/src/ieee33/ingest.py with the PATTERNS entry-point shape (module docstring with Run: `uv run ingest`, Effect: writes 96 profile rows to InfluxDB profiles bucket; the standard try/except `if __name__=="__main__"` wrapper printing FATAL to stderr + sys.exit(1)). Imports: `import sys`, `import requests`, `import pandas as pd`, `from datetime import timezone`, `from influxdb_client.client.write_api import SYNCHRONOUS`, `from ieee33 import config`, `from ieee33 import influx`.

    `def fetch_opsd_day(target_date) -> pd.DataFrame:` (RESEARCH §Pattern 3 lines 357-390):
      - requests.head(config.OPSD_URL, timeout=15); on ANY exception or non-2xx: print to stderr "ERROR: OPSD dataset unreachable (<e>). Download time_series_15min_singleindex.csv manually and provide it — NO substitute data will be written." and sys.exit(1). This IS the no-fallback halt (SPEC-3).
      - Chunked read: pd.read_csv(config.OPSD_URL, usecols=config.OPSD_COLS, parse_dates=["utc_timestamp"], chunksize=10_000); accumulate rows where utc_timestamp.dt.date == TARGET_DATE; break once >= 96 collected.
      - df = pd.concat(...).head(96).reset_index(drop=True); assert len(df)==config.N_STEPS else raise.
      - Pitfall 7: assert the three source columns have ZERO NaN across the 96 rows; raise a clear error naming the bad column if not (do NOT fill — a NaN day means the wrong TARGET_DATE was pinned).
      - Normalize (D-02/D-03): df["load_pu"] = DE_load_actual_entsoe_transparency / DE_load.max() (peak-normalized; PEAK_LOAD_MW is the physical anchor applied later in sim); df["solar_pu"] = DE_solar_profile (already 0-1); df["wind_pu"] = DE_wind_profile (already 0-1).
      - return df[["utc_timestamp","load_pu","solar_pu","wind_pu"]].

    `def main() -> None:`:
      1. client = influx.get_client(); influx.wait_for_influx() (Pitfall 3 — survives first `docker compose up`).
      2. influx.ensure_bucket(client, config.PROFILES_BUCKET); influx.ensure_bucket(client, config.STATE_BUCKET)  (create state bucket now so sim never hits Pitfall 4).
      3. Idempotency (D-08): try influx.count_profiles(client); if it already returns 96, print "profiles already present (96 points) — skipping fetch" and proceed to the verification gate (do NOT re-download — Anti-Pattern line 586). Otherwise fetch.
      4. df = fetch_opsd_day(config.TARGET_DATE); write_api = client.write_api(write_options=SYNCHRONOUS); influx.write_profiles(write_api, df).
      5. VALIDATION GATE (build_site.py ethos): n = influx.count_profiles(client); if n != 96: print "--- INGEST VALIDATION FAILED ---" + f"expected 96 profile points, found {n}" to stderr and sys.exit(1). Else print f"ingest OK — {n} profile points in bucket '{config.PROFILES_BUCKET}' for {config.TARGET_DATE}".
      6. Close client.
  </action>
  <verify>
    <automated>cd system1-measurement-source && docker compose up -d && uv run ingest && uv run python -c "
from ieee33 import influx, config
c = influx.get_client()
n = influx.count_profiles(c)
assert n == 96, f'expected 96, got {n}'
print('PROFILES OK:', n, 'points for', config.TARGET_DATE)
c.close()
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def main' system1-measurement-source/src/ieee33/ingest.py` >= 1
    - `grep -c 'requests.head' system1-measurement-source/src/ieee33/ingest.py` returns 1 (reachability check) AND a `sys.exit(1)` on the unreachable branch (`grep -c 'sys.exit(1)' system1-measurement-source/src/ieee33/ingest.py` >= 1)
    - `grep -ci 'no substitute\|provide it\|manually' system1-measurement-source/src/ieee33/ingest.py` >= 1 (halt+notify message; no-fallback ethos)
    - `grep -c 'chunksize' system1-measurement-source/src/ieee33/ingest.py` returns 1 (chunked 107 MB read)
    - `grep -ci 'nan\|notna\|isna' system1-measurement-source/src/ieee33/ingest.py` >= 1 (Pitfall 7 NaN guard)
    - After `docker compose up -d` + `uv run ingest`, a profiles count query returns exactly 96 (verify command exits 0)
    - `grep -c 'if __name__' system1-measurement-source/src/ieee33/ingest.py` returns 1 with FATAL try/except
  </acceptance_criteria>
  <done>`uv run ingest` (with infra up) writes exactly 96 DE profile points for TARGET_DATE to the profiles bucket, creates the state bucket programmatically, is idempotent on re-run, NaN-guarded, and halts non-zero with a clear no-fallback message if OPSD is unreachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host ingest → OPSD CSV endpoint (HTTPS) | Untrusted remote data fetched once over HTTPS |
| host ingest → InfluxDB container (localhost:8086) | Local token auth; localhost-bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering / Spoofing | OPSD endpoint serves wrong/corrupt data or is MITM'd | mitigate | HTTPS endpoint (HTTP auto-upgraded); strict row-count (==96) + NaN-zero assertions reject malformed/partial data; no silent acceptance |
| T-08-08 | Denial of Service | OPSD endpoint unreachable stalls the pipeline | mitigate | requests.head timeout=15s; on failure exit non-zero with a clear "provide the file" message — NO synthetic/xlsx fallback written (SPEC-3 locked) |
| T-08-09 | Information Disclosure | InfluxDB token in client connection | accept | Same fixed local-only dev token as Plan 01 threat model (T-08-02); localhost-bound; documented as non-production |
| T-08-10 | Tampering (data integrity) | Re-running ingest duplicates profile points | mitigate | InfluxDB overwrites identical measurement+field+timestamp (D-08); idempotent skip when 96 already present |
</threat_model>

<verification>
- `cd system1-measurement-source && uv run python -c "from ieee33 import influx"` imports all 7 helpers.
- With infra up: `cd system1-measurement-source && uv run ingest` writes exactly 96 profile points; re-running skips fetch (idempotent) and still reports 96.
- A profiles count via influx.count_profiles returns 96 (SPEC-6 query acceptance).
- The state bucket exists after ingest (find_bucket_by_name("state") is not None).
</verification>

<success_criteria>
- SPEC-3: OPSD 15-min DE profiles ingested once into InfluxDB (96 points); unreachable endpoint halts non-zero with notify, writes no substitute data.
- SPEC-6: local InfluxDB persistence working; state bucket created programmatically; a 96-point profiles query confirmed.
- D-06/D-07/D-08 honored: two buckets, per-entity state schema encoded in write_state_step, idempotent overwrite-in-place.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ieee-33-bus-der-measurement-source/08-03-SUMMARY.md`
</output>
