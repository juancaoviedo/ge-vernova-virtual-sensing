---
phase: 08-ieee-33-bus-der-measurement-source
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - system1-measurement-source/src/ieee33/network.py
  - system1-measurement-source/src/ieee33/validate.py
autonomous: true
requirements: [SPEC-1, SPEC-2]

must_haves:
  truths:
    - "build_enhanced_33bus() returns a radial balanced enhanced IEEE 33-bus net where pp.runpp converges (SPEC-1)"
    - "The net contains DG sgen at buses 18/22/25/33, RPC shunts at 18 (0.4 MVAr) & 33 (0.6 MVAr), and a series feeder OLTC transformer with DiscreteTapControl (SPEC-1, SPEC-2)"
    - "The plain case33bw base case reproduces Baran & Wu min voltage ≈ 0.913 pu at pandapower bus 17 within tolerance (D-14, SPEC-1)"
    - "Tie-lines (pandapower line idx 32/33/34) are open → topology is radial (RESEARCH Open Q #3)"
    - "Total enhanced-net load ≈ 3.715 MW + 2.3 MVAr at peak (SPEC-1 acceptance)"
  artifacts:
    - path: "system1-measurement-source/src/ieee33/network.py"
      provides: "build_enhanced_33bus() -> (net, trafo_idx); pure builder library module"
      contains: "def build_enhanced_33bus"
    - path: "system1-measurement-source/src/ieee33/validate.py"
      provides: "validate.main() base-case Baran & Wu assertion entry point"
      contains: "def main"
  key_links:
    - from: "system1-measurement-source/src/ieee33/network.py"
      to: "system1-measurement-source/src/ieee33/config.py"
      via: "import config constants (DG buses, scaling, shunts, OLTC params)"
      pattern: "from ieee33 import config|import.*config"
    - from: "system1-measurement-source/src/ieee33/validate.py"
      to: "pandapower case33bw"
      via: "pn.case33bw() + pp.runpp() + vm_min assertion"
      pattern: "case33bw"
---

<objective>
Build the enhanced IEEE 33-bus pandapower network (DG + RPC shunts + series feeder OLTC with tap control) as a pure library function, and a validate entry point that anchors correctness against the published Baran & Wu base-case minimum voltage.

Purpose: SPEC-1 and SPEC-2 — the model must build, be radial/balanced, carry the article-faithful DER/shunt/OLTC topology, and converge. D-14 requires a quantitative correctness anchor (Baran & Wu ≈ 0.913 pu at bus 18) before any DER is added, so wrong impedances or a misplaced OLTC are caught immediately.
Output: network.py (build_enhanced_33bus) and validate.py (base-case assertion), both consuming constants from config.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-SPEC.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md
@.planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md
@system1-measurement-source/src/ieee33/config.py

<interfaces>
<!-- config.py constants this plan consumes (created in Plan 01). Use these directly. -->
DG_NAMEPLATE_MW=0.2, DG_SCALE_FACTOR=2.8, DG_EFFECTIVE_MW=0.56
SOLAR_BUSES=[17,21], WIND_BUSES=[24,32]
RPC_SHUNTS={17:-0.4, 32:-0.6}            # capacitive = NEGATIVE q_mvar
TIE_LINE_IDX=[32,33,34]                   # open for radial
PEAK_LOAD_MW=3.715, PEAK_LOAD_MVAR=2.3
TRAFO_SN_MVA=10.0, TRAFO_VK_PERCENT=4.0, TRAFO_VKR_PERCENT=0.5
TAP_MIN=-5, TAP_MAX=5, TAP_STEP_PERCENT=1.0, TAP_NEUTRAL=0
VM_LOWER_PU=0.95, VM_UPPER_PU=1.05, SHIFT_DEGREE=0.0
BARANWU_VMIN_PU=0.913, BARANWU_VMIN_BUS=17, BARANWU_TOL=0.005

<!-- pandapower contracts (RESEARCH §Pattern 1, verified Context7 2026-06-23) -->
pn.case33bw() -> net with ext_grid at bus 0, 32 lines, indices 0..32.
pp.create_bus(net, vn_kv=12.66, name=...)
pp.create_transformer_from_parameters(net, hv_bus, lv_bus, sn_mva, vn_hv_kv, vn_lv_kv,
    vkr_percent, vk_percent, pfe_kw, i0_percent, shift_degree, tap_side, tap_neutral,
    tap_min, tap_max, tap_step_percent, tap_pos, name) -> trafo_idx
pandapower.control.DiscreteTapControl(net, element_index, vm_lower_pu, vm_upper_pu, side)
pp.create_sgen(net, bus, p_mw, q_mvar, type, name)   # type "PV"/"WP"; p_mw>0 injects
pp.create_shunt(net, bus, q_mvar, p_mw, name)         # capacitive q_mvar<0
pp.runpp(net, algorithm="nr", calculate_voltage_angles=True[, run_control=True])
Read tap from net.trafo.at[idx,"tap_pos"] (NOT res_trafo). Read shift from net.trafo.at[idx,"shift_degree"].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement build_enhanced_33bus() in network.py (case33bw + OLTC-in-series + DG + RPC shunts + radial tie-lines)</name>
  <files>system1-measurement-source/src/ieee33/network.py</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Pattern 1 lines 218-291 full builder; Pitfall 1 bus offset; Pitfall 2 OLTC-in-series; Pitfall 5 DG scaling; Anti-Patterns lines 580-588)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (network.py pattern lines 31-86: imports order, constants from config, pure function, NO __main__ block)
    - system1-measurement-source/src/ieee33/config.py (all constants this builder uses)
    - docs/demos/dc_powerflow_baddata_demo.py (pure-function + docstring style analog)
  </read_first>
  <action>
    Write system1-measurement-source/src/ieee33/network.py as a pure LIBRARY module (NO `if __name__ == "__main__"` block — PATTERNS line 84). Module docstring per PATTERNS convention (description, Dependencies: pandapower, Effect: returns built net). Imports: `import pandapower as pp`, `import pandapower.networks as pn`, `import pandapower.control as ctrl`, `from ieee33 import config`.

    Implement `def build_enhanced_33bus() -> tuple[pp.pandapowerNet, int]:` following RESEARCH §Pattern 1 exactly:
    1. net = pn.case33bw()  (ext_grid at bus 0; article bus N = idx N-1).
    2. Open tie-lines: enforce `net.line.loc[config.TIE_LINE_IDX, "in_service"] = False` (RESEARCH Open Q #3 — set explicitly even if case33bw already opens them; do NOT assume). Capture pre-open base load for the assertion below.
    3. Capture base per-load arrays BEFORE scaling so the sim can scale them later: store `net.load["p_mw"]` and `net.load["q_mvar"]` as-is (case33bw default = peak). Add an assertion that total `net.load["p_mw"].sum()` ≈ config.PEAK_LOAD_MW within 0.05 MW and `net.load["q_mvar"].sum()` ≈ config.PEAK_LOAD_MVAR within 0.1 MVAr (SPEC-1 acceptance: total load ≈ 3.715 MW + 2.3 MVAr).
    4. Insert OLTC IN SERIES (Pitfall 2): hv_bus = pp.create_bus(net, vn_kv=12.66, name="feeder_hv"); net.ext_grid.bus = hv_bus; trafo_idx = pp.create_transformer_from_parameters(net, hv_bus=hv_bus, lv_bus=0, sn_mva=config.TRAFO_SN_MVA, vn_hv_kv=12.66, vn_lv_kv=12.66, vkr_percent=config.TRAFO_VKR_PERCENT, vk_percent=config.TRAFO_VK_PERCENT, pfe_kw=0.0, i0_percent=0.0, shift_degree=config.SHIFT_DEGREE, tap_side="hv", tap_neutral=config.TAP_NEUTRAL, tap_min=config.TAP_MIN, tap_max=config.TAP_MAX, tap_step_percent=config.TAP_STEP_PERCENT, tap_pos=0, name="feeder_OLTC").
    5. Attach controller: ctrl.DiscreteTapControl(net, element_index=trafo_idx, vm_lower_pu=config.VM_LOWER_PU, vm_upper_pu=config.VM_UPPER_PU, side="lv").
    6. Add DG sgens with a `p_mw_nameplate` custom column so the sim can scale by profile later: for b in config.SOLAR_BUSES: pp.create_sgen(net, bus=b, p_mw=config.DG_EFFECTIVE_MW, q_mvar=0.0, type="PV", name=f"solar_b{b+1}"); for b in config.WIND_BUSES: pp.create_sgen(net, bus=b, p_mw=config.DG_EFFECTIVE_MW, q_mvar=0.0, type="WP", name=f"wind_b{b+1}"). After creating, set `net.sgen["p_mw_nameplate"] = net.sgen["p_mw"]` (records the per-unit nameplate the sim multiplies by the 0-1 profile).
    7. RPC shunts (capacitive = NEGATIVE q_mvar): for bus, qmvar in config.RPC_SHUNTS.items(): pp.create_shunt(net, bus=bus, q_mvar=qmvar, p_mw=0.0, name=f"cap_b{bus+1}").
    8. return net, trafo_idx.
    Inline-comment the bus-index offset and the capacitive-sign landmine. Keep it a pure function (no I/O, no prints).
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run python -c "
import pandapower as pp
from ieee33.network import build_enhanced_33bus
from ieee33 import config
net, t = build_enhanced_33bus()
assert (net.line.loc[config.TIE_LINE_IDX,'in_service']==False).all(), 'tie-lines not open'
assert len(net.sgen)==4, f'expected 4 sgen, got {len(net.sgen)}'
assert set(net.sgen.bus)=={17,21,24,32}, net.sgen.bus.tolist()
assert len(net.shunt)==2 and (net.shunt.q_mvar<0).all(), 'shunts wrong'
assert len(net.trafo)==1, 'no feeder trafo'
pp.runpp(net, algorithm='nr', calculate_voltage_angles=True, run_control=True)
assert net.converged, 'runpp did not converge'
assert net.res_bus.vm_pu.notna().all(), 'NaN voltages'
print('NETWORK OK: converged, sgen@', sorted(net.sgen.bus.tolist()), 'tap', net.trafo.at[t,\"tap_pos\"])
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def build_enhanced_33bus' system1-measurement-source/src/ieee33/network.py` returns 1
    - `grep -c 'if __name__' system1-measurement-source/src/ieee33/network.py` returns 0 (library module, no runner — PATTERNS line 84)
    - `grep -c 'create_transformer_from_parameters' system1-measurement-source/src/ieee33/network.py` returns 1 (OLTC in series)
    - `grep -c 'DiscreteTapControl' system1-measurement-source/src/ieee33/network.py` returns 1
    - `grep -c 'p_mw_nameplate' system1-measurement-source/src/ieee33/network.py` >= 1 (sim contract)
    - verify command exits 0: net converges, 4 sgen at buses {17,21,24,32}, 2 negative-q shunts, 1 trafo, tie-lines open, no NaN voltages
  </acceptance_criteria>
  <done>build_enhanced_33bus() returns a converging radial enhanced net with OLTC-in-series + DiscreteTapControl, 4 DG, 2 capacitive shunts, tie-lines open, and a p_mw_nameplate column for downstream scaling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement validate.py — Baran & Wu base-case anchor + enhanced-net convergence/band gate</name>
  <files>system1-measurement-source/src/ieee33/validate.py</files>
  <read_first>
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-RESEARCH.md (§Code Examples lines 681-700 base-case validation; §Validation Architecture lines 773-795)
    - .planning/phases/08-ieee-33-bus-der-measurement-source/08-PATTERNS.md (validate.py fail-loud pattern lines 207-230; entry-point try/except wrapper lines 331-344)
    - system1-measurement-source/src/ieee33/network.py (build_enhanced_33bus signature this validates)
    - system1-measurement-source/src/ieee33/config.py (BARANWU_* + VBAND_* constants)
    - docs/build_site.py (validate_links fail-loud ethos: accumulate issues, fail loud with detail, print OK)
  </read_first>
  <action>
    Write system1-measurement-source/src/ieee33/validate.py as an entry point (`def main()` + the standard try/except `if __name__ == "__main__"` wrapper from PATTERNS lines 338-344, printing FATAL to stderr and sys.exit(1)). Module docstring per convention (Run: `uv run validate`, Effect: asserts model correctness, exits non-zero on failure). Imports: `import sys`, `import pandapower as pp`, `import pandapower.networks as pn`, `from ieee33 import config`, `from ieee33.network import build_enhanced_33bus`.

    main() runs two gates, accumulating failures (fail-loud-with-detail ethos):
    GATE 1 — Baran & Wu base case (D-14, no DER/caps/OLTC):
      net = pn.case33bw(); pp.runpp(net, algorithm="nr", calculate_voltage_angles=True)
      vm_min = net.res_bus["vm_pu"].min(); vm_min_bus = int(net.res_bus["vm_pu"].idxmin())
      Assert abs(vm_min - config.BARANWU_VMIN_PU) < config.BARANWU_TOL AND vm_min_bus == config.BARANWU_VMIN_BUS.
      On pass print: f"Base case OK: vm_min={vm_min:.4f} pu at pp_bus={vm_min_bus} (article bus {vm_min_bus+1})".
    GATE 2 — Enhanced-net single-step sanity (peak load, no DER scaling yet so this is the most-stressed worst case):
      net, trafo_idx = build_enhanced_33bus(); pp.runpp(net, algorithm="nr", calculate_voltage_angles=True, run_control=True)
      Assert net.converged; assert net.res_bus["vm_pu"].notna().all() (no NaN); record vm_min/vm_max; assert tap_pos within [TAP_MIN, TAP_MAX].
      NOTE: do NOT require the 0.95-1.05 band on this unscaled peak step (peak with no DER may dip below 0.95 — that is the legitimate worst case the OLTC and midday DER relieve; the per-step band gate over the 96-step DER-driven run lives in Plan 04). Print vm_min/vm_max + tap_pos so the operator sees the worst case.
      Print "Enhanced net OK: converged, vm_min=.. vm_max=.. tap_pos=..".
    Accumulate any failures into a list; if non-empty, print "--- VALIDATION FAILED ---" + each issue to stderr and sys.exit(1). Else print a final "VALIDATION OK" line.
  </action>
  <verify>
    <automated>cd system1-measurement-source && uv run validate && echo VALIDATE_EXIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def main' system1-measurement-source/src/ieee33/validate.py` >= 1
    - `grep -c 'if __name__' system1-measurement-source/src/ieee33/validate.py` returns 1 with try/except FATAL pattern (`grep -c 'FATAL' system1-measurement-source/src/ieee33/validate.py` >= 1)
    - `grep -c 'case33bw' system1-measurement-source/src/ieee33/validate.py` >= 1 (base-case gate)
    - `uv run validate` exits 0 and prints a line matching `Base case OK: vm_min=0.91` (Baran & Wu ≈ 0.913 within tol at pp_bus 17)
    - `uv run validate` prints an "Enhanced net OK" line confirming convergence + no NaN
  </acceptance_criteria>
  <done>`uv run validate` passes both gates: base case reproduces Baran & Wu ≈ 0.913 pu at bus 17, and the enhanced net converges with no NaN and tap within range; exits non-zero with detail on any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure host-side computation — no network, no DB, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering (data integrity) | Wrong bus index / shunt sign silently produces a plausible-but-wrong "ground truth" | mitigate | D-14 Baran & Wu base-case assertion (vm_min ≈ 0.913 at bus 17) + sgen-bus / shunt-sign / tie-line assertions in Task 1 verify catch off-by-one and sign errors before any data is persisted |
| T-08-06 | Denial of Service | Non-converging power flow halts the pipeline | accept | Newton-Raphson convergence is asserted; failure exits non-zero loudly (no silent bad data) — acceptable for a deterministic offline batch demo |
</threat_model>

<verification>
- `cd system1-measurement-source && uv run python -c "from ieee33.network import build_enhanced_33bus; build_enhanced_33bus()"` builds without exception.
- `cd system1-measurement-source && uv run validate` exits 0; base case prints ≈ 0.913 pu at bus 17; enhanced net converges with no NaN.
- Topology assertions hold: tie-lines open, 4 sgen at {17,21,24,32}, 2 negative-q shunts, 1 series feeder trafo with DiscreteTapControl.
</verification>

<success_criteria>
- SPEC-1: enhanced IEEE 33-bus net builds + `pp.runpp` converges from a single function; total load ≈ 3.715 MW + 2.3 MVAr; all 33 buses present.
- SPEC-2: a regulating feeder transformer (OLTC tap 0.95-1.05 via DiscreteTapControl) + scheduled phase shifter (shift_degree) is in series at the substation; tap/shift readable as state.
- D-14: base case reproduces Baran & Wu ≈ 0.913 pu at bus 18 within tolerance.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ieee-33-bus-der-measurement-source/08-02-SUMMARY.md`
</output>
