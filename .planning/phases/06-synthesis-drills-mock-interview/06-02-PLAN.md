---
phase: 06-synthesis-drills-mock-interview
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-synthesis-drills-mock-interview/notes/REFRAME.md
autonomous: true
requirements: [BRG-01, BRG-02]
must_haves:
  truths:
    - "Juan can deliver each vocabulary-bridge sentence within ~10 s of being prompted with the left-hand term (BRG-01, success criterion 1)"
    - "The bridge table has both a Layer-A plain-language 'HR translation' and a Layer-B technical T&D + tool-bridge layer (D-06)"
    - "Juan can deliver a tiered OSED pitch — ≤90 s plain, ~2–3 min plain, ~10 min technical — all opening with a deployed outcome, no false production-FL claim (BRG-02, D-07, success criterion 2)"
  artifacts:
    - path: ".planning/phases/06-synthesis-drills-mock-interview/notes/REFRAME.md"
      provides: "Two-layer vocabulary-bridge table (BRG-01) + the tiered outcome-first OSED pitch (BRG-02)"
      contains: "## Layer A"
  key_links:
    - from: "REFRAME.md Layer B tool bridges"
      to: "the 'Bridge to your work' callouts in Phase 1–5 notes"
      via: "extraction (lift, do not re-author — D-16)"
      pattern: "NATS"
    - from: "REFRAME.md tiered OSED pitch"
      to: "KAL-01 / DSSE-04 say-aloud tracks + the 21% deployed outcome"
      via: "outcome-first opening across all three tiers"
      pattern: "21%"
---

<objective>
Author `REFRAME.md` — the vocabulary-bridge table (BRG-01) and the tiered OSED pitch (BRG-02).
This is the narrative engine that lets Juan translate his OSED/HEMS/SI-MAPPER work into both
plain-language impact (for the HR screen) and GE Vernova T&D / tool vocabulary (for technical
rounds). It pairs with PHONE-SCREEN.md as the screen-critical Wave-1 deliverable: the screen
needs Layer-A translations and the ≤90 s pitch tier; the technical rounds need Layer-B and the
full ~10 min pitch.

Purpose: Make every Juan term retrievable in two registers within ~10 s, and give the OSED story
a single outcome-first spine across three time budgets.
Output: `.planning/phases/06-synthesis-drills-mock-interview/notes/REFRAME.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative spec — D-06 (two-layer bridges), D-07 (tiered pitch), D-15 (style), D-16 (no new content)
@.planning/phases/06-synthesis-drills-mock-interview/06-CONTEXT.md
# Layer-A seed analogies + Layer-B raw-bridge inventory + the tiered-pitch structure (lift, do not re-author)
@.planning/phases/06-synthesis-drills-mock-interview/06-RESEARCH.md
# Layer-B tool one-liners (Summary Reference Table): MQTT→NATS, K8s→K3s, InfluxDB→Prometheus, Pub/Sub→Kafka
@CLAUDE.md
# Role vocabulary the bridges target
@docs/job-requirements.md

# The RAW MATERIAL to lift Layer-B bridges and pitch content from (D-16 — extract, don't re-derive):
@.planning/phases/01-kalman-state-estimation/notes/KAL-01-virtual-sensing-fusion-engine.md
@.planning/phases/02-distribution-virtual-sensing/notes/DSSE-04-virtual-sensing-in-agms-and-federated-dsse.md
@.planning/phases/04-protocols-stack-architecture/notes/STK-03-messaging-orchestration.md
@.planning/phases/04-protocols-stack-architecture/notes/STK-04-observability.md
@.planning/phases/01-kalman-state-estimation/notes/KAL-04-ieee738-asset-health-ekf.md
@.planning/phases/02-distribution-virtual-sensing/notes/DSSE-02-side-information-taxonomy.md
@.planning/phases/05-federated-architectures-security/notes/FED-03-edge-security.md

<interfaces>
<!-- Every Phase 1–5 note carries a `**→ Bridge to your work**` callout — these ARE the Layer-B -->
<!-- bridges in draft form. grep "Bridge to your work" across the source notes to locate them, -->
<!-- then lift/tighten — do NOT re-author technical content (D-16). -->

<!-- Layer-B tool-bridge one-liners already in CLAUDE.md Summary Reference Table: -->
<!--   MQTT → NATS JetStream   | "MQTT for devices, NATS for federated edge-cloud pipelines" -->
<!--   Kubernetes → K3s        | "Same API, 4x lighter, designed for air-gap field deployment" -->
<!--   InfluxDB → Prometheus   | "Prometheus pull-scrapes K8s pods; Grafana visualizes via PromQL" -->
<!--   Pub/Sub → Kafka         | "Kafka at the cloud tier; too heavy for edge" -->

<!-- Layer-A seed analogies (06-RESEARCH §"Vocabulary Bridge"): expand to cover ALL terms -->
<!--   "MQTT + K8s edge orchestration" → "I make thousands of field devices coordinate in real time" -->
<!--   "EKF / state estimation"        → "Software that infers a reading where there is no sensor" -->
<!--   "federated learning"            → "Train at each site; share the learning, never the raw data" -->
<!--   "convex optimization / MPC"     → "Software that makes the optimal control decision every few seconds" -->

<!-- D-07 honest framing (carried from Phase 5 D-07): OSED edge inference is an ANALOG to -->
<!-- production federated learning, NOT a false claim that Juan ran production FL at scale. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-layer vocabulary-bridge table (BRG-01)</name>
  <read_first>
    - .planning/phases/06-synthesis-drills-mock-interview/06-CONTEXT.md (D-06: Layer A = plain HR translation ≤10 s; Layer B = T&D analog + tool bridges)
    - .planning/phases/06-synthesis-drills-mock-interview/06-RESEARCH.md (§"Vocabulary Bridge — Inventory of Raw Bridges to Lift": the Layer-A seed table + the Layer-B WRITTEN-in-note inventory naming exact source notes)
    - CLAUDE.md (Summary Reference Table — the canonical tool one-liners for Layer B)
    - The seven source notes in <context> — each carries a `**→ Bridge to your work**` callout to lift for Layer B
  </read_first>
  <action>
    Create `.planning/phases/06-synthesis-drills-mock-interview/notes/REFRAME.md` with the
    For:/Purpose: header (D-15) — For: vocabulary reframing for both screen and technical rounds;
    Purpose: deliver each bridge within ~10 s (success criterion 1). Then author TWO bridge tables
    (D-06):
      - `## Layer A — HR Translation Bridges (plain language, ≤10 s each)` — a table
        `| Your term | Plain-language "HR translation" | (optional) GE Vernova term |`. EXPAND the
        06-RESEARCH Layer-A seed set (MQTT+K8s edge orchestration, EKF/state estimation, federated
        learning, convex optimization/MPC, IEC 61850 GOOSE, ORACS/CaCSM) to cover the full term set.
        Plain-language only — NO LaTeX, NO acronyms left unexpanded. This is the PRIMARY screen layer.
      - `## Layer B — Technical Tool & T&D Bridges (technical rounds)` — a table
        `| Your tool/concept | GE Vernova / T&D analog | One-liner justification |`. LIFT from the
        `**→ Bridge to your work**` callouts in the seven source notes + the CLAUDE.md tool
        one-liners: MQTT→NATS JetStream, K8s→K3s, InfluxDB push→Prometheus pull, Pub/Sub→Kafka,
        Modbus→DNP3, Zigbee→LoRa, OSED edge inference→virtual-sensing fusion engine (KAL-01),
        OSED anomaly detection→DLR asset-health (KAL-04), AMI/inverter self-reports→OSED telemetry
        ingestion (DSSE-02), MQTT device fleet→SPIFFE/SPIRE workload identity (FED-03). LaTeX
        permitted here only where a formula genuinely helps (D-15) — but keep it minimal.
    D-16: these bridges already exist in the notes — extract and tighten, do not re-derive.
  </action>
  <acceptance_criteria>
    - `REFRAME.md` exists and contains the heading `## Layer A` (plain-language HR-translation layer)
    - File contains the heading `## Layer B` (technical/tool-bridge layer)
    - Layer A and Layer B are BOTH Markdown tables: `grep -c '^|' REFRAME.md` returns ≥ 8 (two multi-row tables)
    - Layer B contains the tool bridges `NATS`, `K3s`, and `Prometheus` (MQTT→NATS / K8s→K3s / InfluxDB→Prometheus present)
    - File contains a `**For:**` and a `**Purpose:**` header line (D-15)
  </acceptance_criteria>
  <done>Two-layer bridge table written: Layer A plain-language HR translations (≤10 s, no jargon) and Layer B technical T&D + tool bridges lifted from the Phase 1–5 callouts and CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Author the tiered outcome-first OSED pitch (BRG-02)</name>
  <read_first>
    - .planning/phases/06-synthesis-drills-mock-interview/06-CONTEXT.md (D-07: three tiers — ≤90 s plain / ~2–3 min plain / ~10 min technical; all open with a deployed outcome; honest framing, no false production-FL claim)
    - .planning/phases/06-synthesis-drills-mock-interview/06-RESEARCH.md (§"OSED pitch" tiered structure; the deployed-outcome opening hook; Pitfall 5 = no LaTeX in the screen tier)
    - .planning/phases/01-kalman-state-estimation/notes/KAL-01-virtual-sensing-fusion-engine.md (say-aloud track — the virtual-sensing fusion-engine framing for the technical tier)
    - .planning/phases/02-distribution-virtual-sensing/notes/DSSE-04-virtual-sensing-in-agms-and-federated-dsse.md (say-aloud track — AGMS placement + federated DSSE for the technical tier)
    - docs/Juan Carlos Oviedo Cepeda - 2026.pdf (the 21% outcome + OSED architecture facts)
  </read_first>
  <action>
    Append to `notes/REFRAME.md` a `## Tiered OSED Pitch` section with three sub-versions (D-07),
    ALL opening with the deployed outcome (the 21% isolated-community energy-cost result) and using
    GE Vernova vocabulary, never PhD/distribution-first framing:
      - `### Version 1 — ≤90 s (screen)` — plain-language, NO LaTeX. Lead with the outcome, say what
        it does in plain terms, then why it maps to GE Vernova's decentralized-grid mission.
      - `### Version 2 — ~2–3 min (first technical touch)` — plain-language but add the stack shape
        (edge orchestration, telemetry, local inference, control loop) without deep math.
      - `### Version 3 — ~10 min (deep technical round)` — full outcome-first technical pitch. LIFT
        the virtual-sensing fusion-engine framing from KAL-01 and the AGMS-placement / federated-DSSE
        framing from DSSE-04 say-aloud tracks (D-16 — extract, don't re-derive). LaTeX permitted here
        only where it genuinely helps (D-15). HONEST framing (D-07): OSED edge inference is an analog
        to production FL, NOT a claim that production FL was shipped at scale.
    Each version states its target time inline (≤90 s / ~2–3 min / ~10 min) to drive REHEARSAL-TRACKER.
  </action>
  <acceptance_criteria>
    - `REFRAME.md` contains the heading `## Tiered OSED Pitch`
    - File contains all three version headings: `### Version 1` , `### Version 2` , `### Version 3` (or the substrings "≤90 s", "2–3 min", "10 min")
    - The string `21%` appears in the pitch section (deployed-outcome opening present across tiers)
    - The screen tier (Version 1) section contains NO `$` LaTeX before the next `###` heading (Pitfall 5 — screen tier is plain-language)
    - File references the honest-framing constraint (substring "analog" or "honest" present near the technical tier — no false production-FL claim per D-07)
  </acceptance_criteria>
  <done>Tiered OSED pitch written: ≤90 s plain (no LaTeX), ~2–3 min plain, ~10 min technical (lifted from KAL-01 + DSSE-04 say-aloud tracks), all outcome-first with honest framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime | N/A — single local Markdown document. No code, network, input, auth, or secrets. STRIDE runtime categories do not apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering (integrity of claims) | Tiered pitch making a false production-FL claim | mitigate | D-07 honest-framing constraint enforced in Task 2: OSED edge inference framed as an analog, not a production-FL claim. |
| T-06-04 | Information Disclosure | REFRAME.md content | accept | File lives under `.planning/` (private); MUST NOT be added to the published `docs/` HTML site. Bridge content is non-sensitive (no PII/visa/salary). |
</threat_model>

<verification>
- Both Layer A and Layer B tables present; Layer B contains NATS/K3s/Prometheus tool bridges
- Tiered pitch has three versions, all opening with the 21% outcome
- Version 1 (screen tier) is LaTeX-free
- Honest-framing constraint referenced (no false production-FL claim)
</verification>

<success_criteria>
REFRAME.md gives Juan a two-register vocabulary system (Layer A plain ≤10 s, Layer B technical)
and a single outcome-first OSED story in three time budgets — satisfying BRG-01, BRG-02 and
ROADMAP success criteria 1 and 2.
</success_criteria>

<output>
After completion, create `.planning/phases/06-synthesis-drills-mock-interview/06-02-SUMMARY.md`
</output>
