---
phase: 01-kalman-state-estimation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md
autonomous: true
requirements: [KAL-03]

must_haves:
  truths:
    - "Juan can map the IEEE 738 conductor thermal ODE to the EKF predict step"
    - "Juan can name Q and R for the line-temperature EKF and explain the innovation sequence"
    - "Juan can describe divergence detection (NIS / chi-squared) for the line-temperature EKF"
    - "Juan can develop the building-RC <-> conductor-ODE isomorphism explicitly and aloud"
  artifacts:
    - path: ".planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md"
      provides: "Worked line-temperature EKF example: IEEE 738 ODE -> predict step, one numeric step traced, Q/R tuning, innovation/NIS, divergence detection, building-RC bridge"
      contains: "IEEE 738"
      min_lines: 60
  key_links:
    - from: "KAL-03-ieee738-ekf-worked-example.md"
      to: "building RC thermal model"
      via: "structural mapping table (C<->mCp, Q_HVAC<->I^2R, etc.)"
      pattern: "RC|building"
    - from: "KAL-03-ieee738-ekf-worked-example.md"
      to: "EKF predict step"
      via: "discretized IEEE 738 ODE f(Tc,u)"
      pattern: "predict"
---

<objective>
Produce KAL-03: a fully worked line-temperature EKF example that maps the IEEE 738 conductor
thermal ODE to the EKF predict step, traces actual numbers through one predict-update step,
covers Q/R tuning and the innovation/NIS divergence test, and develops the building-RC <->
conductor-ODE isomorphism explicitly.

Purpose: This is the single highest-reward interview artifact — an exact structural analogy
between Juan's building thermal estimator and the IEEE 738 DLR model that almost no candidate
will have. It must be deliverable as a numeric walk-through, not just notation.

Output: One markdown note under notes/ that Juan can narrate step by step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kalman-state-estimation/01-RESEARCH.md

Interview-prep project: write study material drawn directly from 01-RESEARCH.md. The numeric
worked example (with traced numbers) already exists in the research — reproduce and narrate it,
do not invent new numbers. This note may reference KAL-02 Kalman concepts but is written
independently from 01-RESEARCH.md (no dependency on the KAL-02 file existing).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write KAL-03 IEEE 738 EKF worked example</name>
  <files>.planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md</files>
  <read_first>
    - 01-RESEARCH.md section "Domain Knowledge: IEEE 738 Conductor Thermal Model (KAL-03 Core)" (the ODE mCp dTc/dt = qi + qs - qc - qr, ACSR Drake parameter table, the building-to-conductor bridge mapping table)
    - 01-RESEARCH.md section "Domain Knowledge: EKF for Line Temperature (KAL-03 Worked Example)" (state-space formulation, EKF Jacobians A and H, the full Worked Numerical Example with traced numbers, the Q/R tuning table)
    - 01-RESEARCH.md "Innovation Sequence and Divergence Detection" and "Common Pitfalls" (esp. Pitfall 5: DLR can DECREASE rating)
    - The target file path being written
  </read_first>
  <action>
    Create notes/KAL-03-ieee738-ekf-worked-example.md (per KAL-03), reproducing and narrating the
    research's worked example. Cover, drawn directly from 01-RESEARCH.md:
    (1) The IEEE 738 ODE mCp dTc/dt = qi + qs - qc - qr with each term explained (qi = I^2 R(Tc)
        resistive, qs solar, qc convective, qr radiative) and the ACSR Drake parameter table.
    (2) State-space mapping to EKF: state x = [Tc], control input u = [I, Ta, v_wind, qs],
        discretized predict f(Tc,u) (Euler, Dt=1s), measurement model (use architecture 3:
        apparent resistance h(Tc) = R25[1+alpha_R(Tc-25)]), and the process/measurement Jacobians
        A = df/dTc and H = dh/dTc = R25*alpha_R.
    (3) The numeric worked step traced through actual numbers (reproduce from research): heat
        balance at I=500A, Tc=45C (qi≈19.67, qs≈12.65, qc≈5.96, qr≈2.93 W/m, net≈23.43, dTc/dt≈+0.044 C/s);
        steady-state Tc≈73C; DLR I_max≈531A; then the one EKF predict-update step
        (Tc_pred≈45.044, P_pred≈4.026, innovation y, S, gain K, Tc_post≈47.5C, P_post≈0.09).
        Narrate WHY each quantity is computed.
    (4) Q/R tuning subsection: name Q (model uncertainty — unmodeled wind gusts, conductor aging;
        ~0.01-0.1 C^2/step) and R (sensor/resistance-inference noise), reproduce the tuning table
        (too-large / too-small effects), and the Q=0 trap.
    (5) Innovation sequence + divergence detection: y_k zero-mean/white/Gaussian; NIS_k = y^T S^-1 y
        ~ chi2(m); the 95th-percentile gate (scipy chi2.ppf); practical response to divergence.
    (6) DLR caveat (Pitfall 5): DLR can be LOWER than static nameplate on hot/calm days — state explicitly.
    (7) An explicit building-RC <-> conductor-ODE isomorphism subsection: reproduce the structural
        mapping table (C<->mCp, Q_HVAC<->I^2R, UA(T-Ta)<->qc+qr, T_building<->Tc, T_ambient<->Ta,
        sensor-noise R<->PMU-current R) and the key bridge sentence about parameter substitution
        not a conceptual leap. Make "building" and "RC" appear so the bridge is greppable.
    Cover all seven elements fully — this is one substantial note; do not abbreviate.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md && grep -q "IEEE 738" .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md && grep -qi "innovation" .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md && grep -qiE "RC|building" .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md && grep -qi "predict" .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md && [ $(grep -vc '^[[:space:]]*$' .planning/phases/01-kalman-state-estimation/notes/KAL-03-ieee738-ekf-worked-example.md) -ge 60 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "IEEE 738", "innovation", the building-RC bridge (matches RC or
    building), and "predict"; at least 60 non-blank lines. Command prints OK.
  </acceptance_criteria>
  <done>KAL-03 note exists mapping the IEEE 738 ODE to the EKF predict step, with a fully traced numeric step, Q/R tuning, innovation/NIS divergence detection, the DLR caveat, and the explicit building-RC isomorphism.</done>
</task>

</tasks>

<threat_model>
N/A — this plan produces an interview study note (plain markdown the candidate reads aloud).
No software service, network surface, untrusted input, or data store exists. STRIDE threat
modeling does not apply to static study documents.
</threat_model>

<verification>
- KAL-03 note exists with the IEEE 738 ODE, the EKF state-space mapping, a traced numeric step,
  Q/R tuning, innovation/NIS divergence detection, and the building-RC isomorphism table.
</verification>

<success_criteria>
Juan can narrate the worked example aloud end to end — naming Q and R, explaining the innovation
sequence, describing divergence detection — and articulate the building-RC <-> conductor-ODE
isomorphism as parameter substitution rather than a conceptual leap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-kalman-state-estimation/01-02-SUMMARY.md`
</output>
