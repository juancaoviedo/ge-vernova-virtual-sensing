---
phase: 01-kalman-state-estimation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md
  - .planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md
autonomous: true
requirements: [KAL-01, KAL-02]

must_haves:
  truths:
    - "Juan can verbally explain WLS / Gauss-Newton power-system state estimation (objective function, iteration) in plain language"
    - "Juan can state the chi-squared bad-data test and name leverage measurements as its critical limitation"
    - "Juan can make the 'OSED convex optimization IS structurally WLS' bridge explicit"
    - "Juan can describe KF predict/update equations, the EKF Jacobian, and UKF sigma points, and say when to choose each"
  artifacts:
    - path: ".planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md"
      provides: "Plain-English WLS / Gauss-Newton notes with observability + bad-data vocabulary and the convex-optimization bridge"
      contains: "Gauss-Newton"
    - path: ".planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md"
      provides: "KF -> EKF -> UKF progression with full predict/update equations, EKF Jacobian, UKF sigma points, decision table"
      contains: "sigma point"
  key_links:
    - from: "KAL-01-wls-state-estimation.md"
      to: "OSED convex optimization"
      via: "explicit bridge sentence"
      pattern: "WLS|convex"
    - from: "KAL-02-kalman-family-kf-ekf-ukf.md"
      to: "EKF vs UKF decision"
      via: "when-to-use-which table"
      pattern: "EKF.*UKF|decision"
---

<objective>
Produce the two foundational study-notes files for Phase 1: KAL-01 (WLS / Gauss-Newton
power-system state estimation, plain interview-ready language) and KAL-02 (the Kalman
family KF -> EKF -> UKF with full equations, EKF Jacobian, UKF sigma points, and an
EKF-vs-UKF decision table).

Purpose: These two notes are the conceptual spine of the disqualifying Gap 1. KAL-01 gives
Juan the WLS vocabulary (observability, residuals, bad-data, leverage measurements) plus the
"my OSED convex optimization IS structurally WLS" bridge. KAL-02 gives him the exact Kalman
equations he must be able to recite and the EKF-vs-UKF judgment interviewers probe.

Output: Two markdown files under notes/ that Juan can study and deliver aloud.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kalman-state-estimation/01-RESEARCH.md

This is an INTERVIEW-PREP project. "Executing" means writing study material Juan can read and
rehearse aloud — not building software. Draw the technical substance directly from
01-RESEARCH.md (it contains verified equations and vocabulary); do not improvise equations.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write KAL-01 WLS / Gauss-Newton notes</name>
  <files>.planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md</files>
  <read_first>
    - 01-RESEARCH.md section "Domain Knowledge: WLS / Gauss-Newton Power System State Estimation (KAL-01)" (measurement model z=h(x)+e, WLS objective J(x), Gauss-Newton iteration, key vocabulary, bridge sentence)
    - 01-RESEARCH.md "Common Pitfalls" / PITFALLS context on leverage measurements (Q3 chi-squared limitations)
    - The target file path being written (so structure is consistent)
  </read_first>
  <action>
    Create notes/KAL-01-wls-state-estimation.md as a plain, interview-ready explainer (per KAL-01).
    Mental-model-first, light on notation. Cover, drawing directly from 01-RESEARCH.md:
    (1) What WLS state estimation is: grid state vector x = [V, theta] per bus, measurement
        model z = h(x) + e with e ~ N(0,R), and the "best guess from redundant noisy SCADA" framing.
    (2) The WLS objective J(x) = [z-h(x)]^T W [z-h(x)] with W = R^-1 (trust better sensors more).
    (3) The Gauss-Newton iteration in plain steps: Jacobian H = dh/dx, gain G = H^T W H,
        update Dx = G^-1 H^T W [z-h(x̂)], iterate until ||Dx|| < tol (2-4 iterations).
    (4) Interview vocabulary section: observability (H full column rank, unobservable islands),
        residuals, bad-data detection via chi-squared on J(x̂) with m-n DOF, the Largest
        Normalized Residual (LNR) test, and the CRITICAL limitation: leverage measurements have
        zero normalized residual even when corrupted (this is the trap interviewers probe).
    (5) Static vs dynamic SE: WLS is a snapshot (4s SCADA scan); Kalman-based dynamic SE adds
        time (PMU 30-120 Hz) — this sets up KAL-02.
    (6) An explicit bridge subsection titled so it is greppable, using the research bridge
        sentence: "OSED's convex optimization formulates a quadratic objective over building
        state variables from sensor measurements — same minimize-weighted-squared-residuals
        logic, just swapping AC power-flow for an RC thermal model."
    Write so each section can be read aloud in interview cadence. Do NOT produce a "v1" or
    abbreviated version — cover all six elements fully.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md && grep -q "Gauss-Newton" .planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md && grep -qi "leverage" .planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md && grep -qiE "WLS|convex" .planning/phases/01-kalman-state-estimation/notes/KAL-01-wls-state-estimation.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "Gauss-Newton"; contains "leverage" (the bad-data limitation);
    contains the WLS/convex-optimization bridge (matches WLS or convex). Command prints OK.
  </acceptance_criteria>
  <done>KAL-01 note exists with WLS objective, Gauss-Newton iteration, observability + bad-data vocabulary (incl. leverage measurements), and the explicit OSED-convex-optimization bridge.</done>
</task>

<task type="auto">
  <name>Task 2: Write KAL-02 Kalman family (KF -> EKF -> UKF) notes</name>
  <files>.planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md</files>
  <read_first>
    - 01-RESEARCH.md section "Domain Knowledge: Kalman Filter Family KF -> EKF -> UKF (KAL-02)" (linear KF predict/update equations, EKF nonlinear state-space + Jacobians A and H, UKF sigma-point generation and alpha/beta/kappa, EKF-vs-UKF decision table, "when EKF fails")
    - 01-RESEARCH.md "Q and R Tuning Intuition" and "Innovation Sequence and Divergence Detection" subsections
    - The target file path being written
  </read_first>
  <action>
    Create notes/KAL-02-kalman-family-kf-ekf-ukf.md as a precise mathematical progression (per KAL-02),
    drawing equations verbatim from 01-RESEARCH.md (do not re-derive). Cover:
    (1) Linear KF: state-space (x_k = F x + B u + w, z = H x + v), full Predict step
        (x̂_{k|k-1}, P_{k|k-1} = F P F^T + Q) and Update step (innovation y, S = H P H^T + R,
        gain K = P H^T S^-1, x̂_{k|k}, P_{k|k}), plus the K-balances-model-vs-sensor intuition.
    (2) EKF: nonlinear f and h; Predict via propagating x through f and Jacobian
        A = df/dx; Update via Jacobian H = dh/dx; the full equation block. Include "when EKF
        fails" (highly nonlinear -> poor Jacobian -> divergence; ill-conditioned near voltage
        collapse; positive-feedback divergence as P grows).
    (3) UKF: sigma-point generation (2n+1 points, lambda = alpha^2(n+kappa)-n), the alpha/beta/kappa
        parameter meanings (beta=2 optimal Gaussian), and UKF predict/update via weighted sigma
        propagation through f and h with cross-covariance Pxz. State that it avoids Jacobians.
    (4) An EKF-vs-UKF "when to use which" decision table (reproduce the research table:
        approach, accuracy, Jacobian needed, compute cost, grid use case, failure mode) and the
        "when to choose" narrative (EKF for mildly-nonlinear line temp; UKF for strongly-nonlinear
        power flow near voltage limits; particle filter for non-Gaussian fallback).
    (5) A short Q/R tuning + innovation-sequence/divergence (NIS chi-squared) subsection so the
        note connects to KAL-03 — name Q (model uncertainty), R (sensor uncertainty), the Q=0
        trap (K->0, filter ignores measurements), and NIS_k = y^T S^-1 y ~ chi2(m).
    Ensure the literal phrases "sigma point" and the EKF/UKF decision framing appear so they are
    greppable. Cover all five elements fully — no abbreviated version.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md && grep -qi "sigma point" .planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md && grep -qi "Jacobian" .planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md && grep -qi "innovation" .planning/phases/01-kalman-state-estimation/notes/KAL-02-kalman-family-kf-ekf-ukf.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    File exists; contains "sigma point" (UKF), "Jacobian" (EKF), and "innovation" (KF update /
    divergence). Command prints OK.
  </acceptance_criteria>
  <done>KAL-02 note exists with linear KF, EKF (Jacobian + failure modes), UKF (sigma points + alpha/beta/kappa), an EKF-vs-UKF decision table, and a Q/R + innovation/divergence subsection.</done>
</task>

</tasks>

<threat_model>
N/A — this plan produces interview study notes (plain markdown the candidate reads aloud).
There is no software service, no network surface, no untrusted input, and no data store.
STRIDE threat modeling does not apply to static study documents.
</threat_model>

<verification>
- Both note files exist under notes/ with the required greppable terms.
- KAL-01 includes the WLS-to-convex-optimization bridge and the leverage-measurement limitation.
- KAL-02 includes the EKF Jacobian, UKF sigma points, and the EKF-vs-UKF decision table.
</verification>

<success_criteria>
Juan can read each note aloud and, without the notes, explain WLS/Gauss-Newton in plain
language, recite the KF/EKF/UKF predict-update equations, and state when to pick EKF vs UKF.
</success_criteria>

<output>
After completion, create `.planning/phases/01-kalman-state-estimation/01-01-SUMMARY.md`
</output>
