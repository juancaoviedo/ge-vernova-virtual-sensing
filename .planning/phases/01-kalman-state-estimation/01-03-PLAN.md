---
phase: 01-kalman-state-estimation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-kalman-state-estimation/demo/ekf_line_temp_demo.py
  - .planning/phases/01-kalman-state-estimation/demo/README.md
autonomous: true
requirements: [KAL-04]

must_haves:
  truths:
    - "The Python EKF mini-demo runs to completion and outputs a time-series temperature/ampacity estimate"
    - "The demo writes a plot (PNG) showing true Tc vs EKF estimate, innovation sequence, and uncertainty band"
    - "The demo is from-scratch numpy/scipy (no filterpy dependency)"
    - "Juan has a short README with run instructions and 2-3 interview talking points"
  artifacts:
    - path: ".planning/phases/01-kalman-state-estimation/demo/ekf_line_temp_demo.py"
      provides: "Self-contained from-scratch EKF estimating conductor temperature from current + weather telemetry"
      contains: "def ekf_step"
    - path: ".planning/phases/01-kalman-state-estimation/demo/README.md"
      provides: "How to run the demo, what it demonstrates, and 2-3 interview narration talking points"
      contains: "talking point"
  key_links:
    - from: "ekf_line_temp_demo.py"
      to: "ekf_line_temp.png"
      via: "matplotlib savefig"
      pattern: "savefig"
    - from: "ekf_line_temp_demo.py"
      to: "IEEE 738 physics"
      via: "ieee738_rhs predict function"
      pattern: "ieee738"
---

<objective>
Produce KAL-04: a runnable, self-contained Python EKF mini-demo (numpy/scipy/matplotlib, no
filterpy) that estimates conductor/line temperature from simulated current + weather telemetry,
produces a time-series temperature estimate (and ampacity context) and a plot, plus a short
README with run instructions and 2-3 interview talking points.

Purpose: This converts textbook Kalman knowledge into claimed hands-on experience — the weakest
gap. A running from-scratch EKF demonstrates deeper understanding than a filterpy wrapper.

Output: ekf_line_temp_demo.py (runs, exits 0, writes a PNG) and README.md under demo/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kalman-state-estimation/01-RESEARCH.md

Interview-prep project: this is the ONE hands-on demo. Base the implementation on the
"Domain Knowledge: Python EKF Implementation Design (KAL-04)" section of 01-RESEARCH.md, which
contains a near-complete reference implementation. Verified environment: numpy 1.26.4,
scipy 1.11.4, matplotlib 3.6.3 are installed; filterpy is NOT installed and MUST NOT be used.

<interfaces>
Reference design from 01-RESEARCH.md (executor should adapt, fixing the API issues noted below):

def ieee738_rhs(t, Tc, I, Ta, v_wind=0.0) -> float   # returns dTc/dt from IEEE 738 heat balance
def ekf_step(Tc_hat, P, I, Ta, z_meas, Q, R, dt=1.0) -> (Tc_post, P_post, innov, S)  # one predict+update
def run_demo()                                        # simulate truth, run EKF, plot, savefig

Drake ACSR constants: mCp=534.0, R25=7.28e-5, alpha_R=0.00403, D=0.0281, eps=0.5,
alpha_s=0.5, Qse=900.0. Measurement model: apparent resistance z = R25*(1+alpha_R*(Tc-25)).
</interfaces>

<known_issues>
The reference snippet in 01-RESEARCH.md has two matplotlib-3.6 API bugs the executor MUST fix:
- `ax2.axhline(0, 'k--')` is invalid — use `ax2.axhline(0, color='k', linestyle='--')`.
- `ax1.set(...)` with a `title=` kwarg is fine, but confirm every `ax.set(...)` uses valid keys.
Run the script and fix any runtime error until it exits 0 and the PNG is written.
</known_issues>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the from-scratch EKF line-temperature demo</name>
  <files>.planning/phases/01-kalman-state-estimation/demo/ekf_line_temp_demo.py</files>
  <read_first>
    - 01-RESEARCH.md section "Domain Knowledge: Python EKF Implementation Design (KAL-04)" (the full reference ieee738_rhs / ekf_step / run_demo implementation and the three-subplot design)
    - 01-RESEARCH.md "Don't Hand-Roll" table (use np.linalg.solve over inv; scipy.stats.chi2.ppf for thresholds) and "Code Examples" (chi-squared divergence gate)
    - 01-RESEARCH.md "EKF for Line Temperature" worked example for the expected numeric behavior (estimate should track true Tc, innovation roughly zero-mean)
    - The <interfaces> and <known_issues> blocks above
  </read_first>
  <action>
    Create demo/ekf_line_temp_demo.py as a single self-contained script using ONLY numpy, scipy,
    and matplotlib (no filterpy). Implement per the research reference design:
    (1) ieee738_rhs(t, Tc, I, Ta, v_wind=0.0): IEEE 738 heat balance returning dTc/dt with the
        Drake ACSR constants, resistive qi=I^2 R(Tc), solar qs, natural-convection qc, radiation qr.
    (2) ekf_step(...): one EKF cycle — Euler predict via ieee738_rhs, process Jacobian A,
        P_pred = A^2 P + Q; update with measurement Jacobian H=R25*alpha_R, innovation,
        S, gain K, Tc_post, P_post. Scalar state; use guarded math (e.g., max(Tc-Ta, 0.1)) to
        avoid invalid powers.
    (3) run_demo(): simulate a current profile (e.g., 400->600 A over an hour), integrate true Tc,
        add Gaussian measurement noise to apparent resistance, run the EKF starting from a wrong
        initial guess + high P0, collect Tc_ekf, P_ekf, innovation. Produce a matplotlib figure with
        three subplots — (a) true Tc vs EKF estimate with ±2σ band, (b) innovation sequence,
        (c) uncertainty σ over time — and savefig to ekf_line_temp.png. Print a confirmation line.
    (4) Add a brief ampacity readout: compute/print real-time DLR I_max for the final conditions
        (research provides ieee738_ampacity) so the demo outputs an ampacity estimate, not just temperature.
    Fix the two matplotlib API issues from <known_issues>. Prefer scipy.stats.chi2.ppf if you add
    a divergence gate. The script must run as `python3 ekf_line_temp_demo.py`, exit 0, and write the PNG.
    Run it during the task and fix any error until it succeeds. Do NOT add a filterpy code path.
  </action>
  <verify>
    <automated>cd .planning/phases/01-kalman-state-estimation/demo && python3 ekf_line_temp_demo.py && test -f ekf_line_temp.png && ! grep -qi "filterpy" ekf_line_temp_demo.py && grep -q "def ekf_step" ekf_line_temp_demo.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    `python3 ekf_line_temp_demo.py` exits 0; ekf_line_temp.png is written; the source contains
    "def ekf_step" and does NOT contain "filterpy". Command prints OK.
  </acceptance_criteria>
  <done>The from-scratch EKF demo runs to completion, prints a temperature/ampacity readout, and writes the three-subplot PNG using only numpy/scipy/matplotlib.</done>
</task>

<task type="auto">
  <name>Task 2: Write the demo README with run steps and talking points</name>
  <files>.planning/phases/01-kalman-state-estimation/demo/README.md</files>
  <read_first>
    - The just-created demo/ekf_line_temp_demo.py (to describe what it actually does and its output filenames)
    - 01-RESEARCH.md "System Architecture for KAL-04 Demo" data-flow diagram and the building-RC bridge sentence (for the talking points)
    - The target README path being written
  </read_first>
  <action>
    Create demo/README.md covering:
    (1) What it demonstrates: a from-scratch EKF estimating conductor temperature from current +
        weather telemetry using the IEEE 738 thermal ODE as the predict step and an
        apparent-resistance measurement update — i.e., a Dynamic Line Rating virtual sensor.
    (2) How to run: prerequisites (numpy/scipy/matplotlib already installed; explicitly NO filterpy
        needed), the exact command `python3 ekf_line_temp_demo.py`, and the output (console
        temperature/ampacity readout + ekf_line_temp.png with the three subplots described).
    (3) 2-3 explicitly labeled interview talking points for narrating the demo aloud — e.g.,
        "I implemented this from scratch in numpy to show I understand the predict/update mechanics,
        not just a library API"; the building-RC <-> IEEE 738 conductor bridge; what the innovation
        subplot tells you about filter health / divergence. Use a greppable "talking point" label.
    Keep it concise and interview-oriented.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-kalman-state-estimation/demo/README.md && grep -qi "talking point" .planning/phases/01-kalman-state-estimation/demo/README.md && grep -q "python3 ekf_line_temp_demo.py" .planning/phases/01-kalman-state-estimation/demo/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    README exists; contains a "talking point" label; contains the exact run command
    `python3 ekf_line_temp_demo.py`. Command prints OK.
  </acceptance_criteria>
  <done>README explains what the demo shows, how to run it (no filterpy), and gives 2-3 labeled interview talking points.</done>
</task>

</tasks>

<threat_model>
N/A — this plan produces a local, single-file illustrative demo script and a README for interview
prep. It runs on Juan's machine against self-generated synthetic data, exposes no network surface,
accepts no untrusted external input, and persists no sensitive data. STRIDE threat modeling does
not apply.
</threat_model>

<verification>
- `python3 ekf_line_temp_demo.py` exits 0 and writes ekf_line_temp.png.
- The implementation uses only numpy/scipy/matplotlib (no filterpy).
- README documents the run command and includes labeled interview talking points.
</verification>

<success_criteria>
The demo runs and outputs a time-series temperature/ampacity estimate plus a plot; Juan can run
it on demand and narrate it in an interview using the README talking points.
</success_criteria>

<output>
After completion, create `.planning/phases/01-kalman-state-estimation/01-03-SUMMARY.md`
</output>
